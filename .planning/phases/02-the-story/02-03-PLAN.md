---
phase: 02-the-story
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - site/index.html
  - site/styles.css
  - site/main.js
  - site/method.js
autonomous: true
requirements: [CONT-03, NAV-01, MOTION-01]

must_haves:
  truths:
    - "A visitor scrolling past Publications lands on a full-height METHOD section and reads Sirio's AI philosophy as a short manifesto"
    - "The manifesto opens on the calculator story from Sirio's childhood and lands on 'the direction is still mine'"
    - "The section owns an indigo/cyan accent distinct from Publications' violet on one side"
    - "A calculator beside the manifesto mashes keys, lands on an answer that means nothing, holds on it, then resolves to MAP -> STRATEGY -> EXECUTE and THE DIRECTION IS MINE"
    - "Under prefers-reduced-motion the calculator shows the resolved chain immediately — the argument still lands, only its pacing is lost"
    - "The Latin phrase 'cum grano salis' appears nowhere on the page"
  artifacts:
    - path: "site/index.html"
      provides: "The #method section shell, the manifesto, the calculator markup"
      contains: "id=\"method\""
    - path: "site/method.js"
      provides: "initMethod() and playCalc() — the timer-driven calculator choreography"
      contains: "export function initMethod("
    - path: "site/styles.css"
      provides: "--star-rigel token, #method accent entry, calculator + manifesto CSS"
      contains: "--star-rigel"
  key_links:
    - from: "site/main.js boot()"
      to: "site/method.js initMethod()"
      via: "ES module import — index.html:320 ships exactly ONE script tag and no new <script src> is permitted"
      pattern: "from '\\./method\\.js'"
    - from: "site/method.js playCalc()"
      to: "site/styles.css [data-rev-state] rules"
      via: "timer-written data-rev-state reusing the Plan 01 vocabulary — no new state attribute"
      pattern: "dataset\\.revState"
    - from: "site/index.html #method"
      to: "site/styles.css #method { --accent: var(--star-rigel) }"
      via: "the section accent map — omitting it renders the section's furniture transparent with no console error"
      pattern: "#method\\s*\\{[^}]*--accent"
---

<objective>
Ship the first act of **METHOD** (CONT-03): the "How I Work With AI" section, opening on the
calculator story that is the origin of the whole philosophy, carrying the five-stanza manifesto,
and resolving on *the direction is still mine*.

**D-20 is CLOSED.** Sirio asked to be surprised and to choose from `/gsd-sketch` variants; he
chose **variant D**. Structure, objects and animation are locked by
`.planning/sketches/001-method-section/`. His explicit caveat: *"right now this is not the same
theme as the rest of the webpage — which is fine for a sketch… I take this only for the actual
parts and objects and animation, not for details like font."* **Structure/objects/animation:
port. Colour and type: this phase's call, from the live tokens and UI-SPEC T-3.**

Implements D-03 (the indigo/cyan stop), D-10 (between explicit and philosophy-first),
D-11 (short manifesto), D-12 (open on the calculator story), D-13 (drop the Latin),
D-14 (the teacher's rephrased line), D-15 (the approved manifesto draft), D-16 (the bridge),
D-20 (closed by sketch 001 variant D), NAV-01 (the floor).

Purpose: `profile.md:375` already makes the lab↔AI bridge explicitly — *"Like physical industrial
automation, tools are transformative when directed by genuine understanding, but counterproductive
when used blindly."* ORIGIN ends on *repetition in the lab is the bottleneck → so I automate it*.
METHOD is the same insight one layer up. That structural bridge (D-16) is what makes CONT-03 the
second half of one argument rather than a bolt-on topic.

Output: `#method` in `site/index.html` with its manifesto and calculator; `site/method.js` with
`initMethod()` and `playCalc()`; the `--star-rigel` token and `#method` accent entry; calculator
and manifesto CSS.
</objective>

<execution_context>
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/workflows/execute-plan.md
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-the-story/02-CONTEXT.md
@.planning/phases/02-the-story/02-RESEARCH.md
@.planning/phases/02-the-story/02-UI-SPEC.md
@.planning/phases/02-the-story/02-PATTERNS.md
@.planning/sketches/001-method-section/README.md
@.planning/phases/02-the-story/02-02-SUMMARY.md
</context>

## Phase Goal

**As a** recruiter who has just read how Sirio left repetitive lab work behind, **I want to** watch
a calculator type meaningless numbers until it resolves into map → strategy → execute **so that** I
understand — without being told — that he directs the tool rather than being directed by it.

## Artifacts this phase produces

Every symbol below is **created by Phase 2** and does not exist in the codebase today. The
source-grounding pass must treat these as new, not as drift.

**New files (whole phase):** **`site/method.js` (this plan)** · `site/assets/media/ot2-timelapse.mp4` ·
`site/assets/media/ot2-timelapse-poster.jpg` · `site/assets/media/ot2-part-adapter.png` ·
`site/assets/media/ot2-part-pcr-strip-racks.png` · `site/assets/media/ot2-part-buffer-ladder-rack.png` ·
`site/assets/media/ot2-part-rack-holder.png`

**JS functions (whole phase):** `initReveals()` (plan 01, `site/main.js`) · **`initMethod()` ·
`playCalc()` (this plan, `site/method.js`)** · `playTrack()` · `initArc()` (plan 04, `site/method.js`)

**JS constants (whole phase):** **`D` — the locked calculator timing object (this plan,
`site/method.js`)**

**CSS custom properties (whole phase):** `--rev-stagger` · `--rev-i` (plan 01) · `--star-zuben`
(plan 02) · **`--star-rigel` (this plan)** · `--len` (plan 04)

**HTML attributes (whole phase):** `data-rev-state` with values `hidden` | `shown` (plan 01) ·
`data-reveal-kind` with values `draw` | `assemble` | `snap` (plan 01) · `data-track` with values
`digital` | `physical` (plan 04) · `data-phases` (plan 04)

**New element ids (whole phase):** `#origin` (plan 02) · **`#method` · `#calc-ghost` · `#calc-out` ·
`#calc-keys` (this plan)** · `#loop-arc` · `#arc-path` · `#arc-head` · `#doc-rail` (plan 04)

**New CSS classes (whole phase):** `.section--full` · `.section__label--interlude` · `.narr` ·
`.narr--land` · `.origin__beats` · `.contact__links` · `.contact__link` (plan 02) ·
**`.method__act` · `.calc-wrap` · `.calc` · `.calc__screen` · `.calc__ghost` · `.calc__out` ·
`.calc__out--resolved` · `.keys` · `.key` · `.key--op` · `.key--press` · `.manifesto` (this plan)** ·
`.tracks` · `.track` · `.track--digital` · `.track--physical` · `.track__head` · `.track__kind` ·
`.track__for` · `.track__title` · `.track__foot` · `.phases` · `.phases--line` · `.phases--loop` ·
`.ph` · `.ph__dot` · `.ph__wire` · `.ph__n` · `.ph__name` · `.ph__desc` · `.ph__note` · `.ph__em` ·
`.loop-arc` · `.doc-rail` · `.doc-rail__tag` · `.doc-rail__txt` · `.evidence` · `.evidence__label` ·
`.ev-grid` · `.ev` · `.deck-link` (plan 04) · `.media-slot--composite` · `.blueprint` ·
`.blueprint__part` (plan 05)

<tasks>

<task type="auto">
  <name>Task 1: #method ships readable — the indigo interlude and the five-stanza manifesto</name>

  <files>site/index.html, site/styles.css</files>

  <read_first>
    - `.planning/sketches/001-method-section/index.html:627-662` — **variant D's Act 1 markup, LOCKED.**
      Port the structure; do not port the theme
    - `.planning/sketches/001-method-section/README.md` — the whole file, especially § "⚠ DO NOT COPY
      THIS SKETCH'S REVEAL MECHANISM" and § "Content Provenance"
    - `../00_Profile/profile.md:373` — **the calculator story, verbatim and complete.** D-15's approved
      draft is a faithful compression of it. ⚠ This passage also contains the banned Latin
    - `../00_Profile/profile.md:375` — the AI philosophy and the lab↔AI bridge (D-16's grounding)
    - `site/index.html:278-299` — `#publications` (the insertion point is after its `</section>`) and
      the `04 CONTACT` comment
    - `site/index.html` — the `#origin` section as built in plan 02: **this is the interlude shell to
      match exactly**
    - `site/styles.css` — the `:root` stops (with `--star-zuben` and the rewritten `PAL` comment from
      plan 02), the accent map, `.section--full`, `.narr`, `.narr--land`, `.section__label--interlude`
    - `.planning/phases/02-the-story/02-UI-SPEC.md` § Section Anatomy → `#method`, § Color Cl-2 / Cl-3,
      § Typography T-3, § Copywriting Cw-2 / Cw-3 / Cw-4, § NAV-01
    - `.planning/phases/02-the-story/02-RESEARCH.md` § Content Sourcing Map, § Common Pitfalls 6, 7, 8
    - `.planning/phases/02-the-story/02-PATTERNS.md` § `site/index.html` — `#method`
  </read_first>

  <action>
    Insert a new `#method` section into `site/index.html` **between the closing `</section>` of
    `#publications` (currently `:294`) and the `04 CONTACT` comment block (currently `:296`)** —
    D-01's locked order puts AI between Publications and Contact.

    **The shell — match the `#origin` interlude shape built in plan 02 exactly:**

    - `<section id="method" class="section section--full">`.
    - ⚠ **No `.section__numeral`** (D-02 — unnumbered interlude, same as ORIGIN). The Mission pill
      index does not gain an entry.
    - `.section__blob` with `data-para`, plus a `.section__blob--method` modifier.
    - `.section__head` with no `data-reveal` on the container;
      `<span data-reveal="0" class="section__label section__label--interlude">// METHOD</span>` and
      `<span data-reveal="0" data-reveal-kind="draw" class="section__rule"></span>`.
    - `<h2 data-reveal="0" data-reveal-kind="assemble"><span>How I work with AI</span></h2>` — the h2
      text is the sketch's, verbatim. It inherits the shared `.section h2` token; **no size override**.
    - ⚠ **One `<section>`, two acts.** The sketch used two stacked `<section data-sec="d">` /
      `data-sec="d2"` elements **only because it was a sketch**. Production is **one** section (one
      accent-map entry, one floor) with `.method__act` children. Act 1 (this task and Task 2) is
      `.method__act--story`; Act 2 (plan 04) will be `.method__act--tracks`.

    **The manifesto — D-15's approved draft, five stanzas, VERBATIM.** Sirio picked this direction
    over the alternative. Each stanza is its own `<p>` inside a `.manifesto` container, each carrying
    its own `data-reveal` index (`1`–`5`), `data-reveal-kind="assemble"`, and a `<span>` wrapper.
    Stanzas 4 and 5 additionally carry `.narr--land` — they are the two lines the section exists for.
    Reuse `.narr` from plan 02; do not define a second narrative body role.

    > I was ten, and I hated long division. The calculator could just do it.
    >
    > My teacher said I could use it — but only if I already knew what I was asking it.
    > Otherwise it wouldn't help me.
    >
    > I ignored her, and spent a year typing numbers until they matched the answers in
    > the back of the book. I understood nothing.
    >
    > So I learned the order: map the problem, choose the strategy, then let the tool
    > execute. Never the reverse.
    >
    > Same bottleneck, one layer up. AI is the biggest calculator I've been handed.
    > The direction is still mine.

    - ⚠ **D-14 — the teacher's line is the rephrased one above**, not `profile.md:373`'s original
      (*"Yes, but you must use the calculator with grano salis, otherwise it won't help you."*). It was
      chosen because it states the **method** (map → strategise → execute) rather than a virtue, so the
      AI parallel lands with no explaining: knowing what to ask *is* the job.
    - In stanza 5, wrap *The direction is still mine.* in a `.ph__em`-style emphasis element — **a
      class, never an inline `style=`**. The sketch used `<em style="font-style:normal;color:…">`
      (`sketch:725`); the live site has **zero inline `style=` on content**. Use `<em class="ph__em">`
      and give `.ph__em` `font-style: normal; color: rgba(var(--accent), 0.9);` in CSS.
    - ⚠ **L-4 — no text splitting.** D-15 gives five discrete stanzas; our lines are **already separate
      elements**, so the masked-slide reveal needs zero splitting machinery. Every Codrops tutorial for
      this effect reaches for SplitText/Splitting.js; a library violates D-35 and runtime splitting
      breaks on resize and font swap.

    ⛔ **THE BANNED PHRASE (D-13 / Cw-3) — the trap is inside the exact source this section opens on.**
    `profile.md:373` uses *"cum grano salis"*; **the page must not.** This is stronger than a
    readability call: in English *"take it with a grain of salt"* means **be sceptical of it**, which
    is **not** what Sirio means — he means **use it with your judgment engaged**. The idiom would
    actively mislead. **The philosophy stays; the phrase goes.** CONT-03 asks the section to convey the
    philosophy, not the phrase. ⚠ This **supersedes the wording in `PROJECT.md:48` and in `CLAUDE.md`**,
    which both still name the phrase — do not "restore" it from either. No Latin of any kind.

    ⚠ **D-10 — the claim sits BETWEEN explicit and philosophy-first.** Sirio's exact words: *"In
    between the explicit and the philosophy first option."* Lead with the philosophy; own the site as
    evidence **plainly but late and understated**. **Do not headline "this site was built with AI"**,
    and do not bury it as a footnote either. The evidence strip lands in plan 04, deliberately last in
    the section.

    ⚠ **Cw-4 — do not fabricate.** The design snapshots' copy invents roles and publications and omits
    AGC entirely. Take structure from the sketch, every fact from `profile.md` and Sirio's own deck.
    This has bitten twice already; when tempted to invent, check the source.

    **Carry a "why" comment** above the section in this file's convention. Record: **D-13** (no Latin —
    and that `profile.md:373`, `PROJECT.md:48` and `CLAUDE.md` all still name the phrase, so its
    absence is deliberate, not an oversight), **D-02** (unnumbered on purpose), and **D-16** (the bridge
    from ORIGIN is load-bearing — this section is the same insight one layer up, not a new topic).

    **CSS — `site/styles.css`:**

    - **Token.** Add `--star-rigel: 96, 214, 255;` (indigo/cyan) to the `:root` stops beside
      `--star-zuben`. Naming follows the shipped convention — **Rigel**, a blue-cyan supergiant.
      ⚠ The `PAL` divergence comment was rewritten in plan 02; **`--star-rigel` falls under it — do NOT
      add it to `PAL` in `star-engine.js`** (L-3 / Pitfall 7). `star-engine.js` gets no edit this phase.
    - **Accent map entry.** Add `#method { --accent: var(--star-rigel); }` beside `#origin`. ⚠ Required,
      not polish — omit it and every accent-derived surface renders transparent with no console error
      (P-4 / Pitfall 6 / gate G-4).
    - ⚠ **The rigel→sirius adjacency at Contact is the closest colour pairing on the page.** Rigel is
      markedly more cyan and saturated than `--star-sirius` (150,196,255) so it should read — but
      UI-SPEC **OI-1** flags this as the one pairing to check on a render. Record it.
    - `.method__act` — the two-act container. Vertical rhythm from the **macro** (fluid `clamp()`) tier.
    - `.manifesto` — stanza rhythm from the **micro** 4-multiple tier {4, 8, 12, 16, 20, 24, 32, 48}.
      ⚠ The sketch used `26px`, `22px`, `10px`, `6px` freely — normalise every new fixed value.
    - `.calc-wrap` — the Act 1 two-column layout: the calculator beside the manifesto. Collapse to one
      column at the sketch's `max-width: 860px` breakpoint.
    - `.ph__em` — `font-style: normal; color: rgba(var(--accent), 0.9);`
    - ⚠ **Weights {300, 400} only.** The live site never uses 700 (T-1).
    - ⚠ **Type runs BIGGER than the V4 baseline across this section** — Sirio: *"The font, in general,
      should be bigger."* Reuse plan 02's `.narr` / `.narr--land` rather than inventing new sizes.
  </action>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && test "$(grep -c 'id="method"' site/index.html)" = "1" && test "$(grep -c '// METHOD' site/index.html)" = "1" && test "$(grep -c 'section__numeral' site/index.html)" = "3" && test "$(grep -c 'How I work with AI' site/index.html)" = "1" && test "$(grep -c 'class="manifesto"' site/index.html)" = "1" && test "$(grep -c -- '--star-rigel' site/styles.css)" -ge "2" && grep -qE '#method[[:space:]]*\{' site/styles.css && grep -A2 -E '#method[[:space:]]*\{' site/styles.css | grep -q -- '--accent: var(--star-rigel)' && test "$(grep -ciE '\bego\b|grano|salis' site/index.html)" = "0" && test "$(grep -c 'style="' site/index.html)" = "0" && test "$(grep -c 'data-sec=' site/index.html)" = "0" && git diff --quiet site/star-engine.js && test "$(grep -c '\[data-reveal\]' site/styles.css)" = "0" && test ! -f site/package.json && echo GATES-PASS</automated>
    <human-check>Scroll to METHOD. It must sit between Publications and Contact, fill the viewport height, and read **indigo/cyan** — distinct from Publications' violet immediately above it. Read the five stanzas: they must be D-15's approved draft word for word, with no Latin anywhere. Check the accent against Contact's blue further down — UI-SPEC OI-1 flags rigel→sirius as the closest pairing on the page; does it read as two colours or one? In DevTools run `getComputedStyle(document.querySelector('#method')).getPropertyValue('--accent')` — must return a triplet (gate G-4).</human-check>
  </verify>

  <acceptance_criteria>
    - Source: `site/index.html` contains exactly one `id="method"`, positioned after `#publications`'s closing `</section>` and before the `04 CONTACT` comment
    - Source: the `#method` section carries `class="section section--full"` and contains **zero** `section__numeral` elements — `grep -c 'section__numeral' site/index.html` still returns `3`
    - Source: `site/index.html` contains the literal label text `// METHOD` exactly once, on an element carrying `section__label--interlude`
    - Source: `#method` contains exactly one `.manifesto` container holding exactly five `<p>` stanzas, carrying `data-reveal="1"` through `"5"`, each with `data-reveal-kind="assemble"` and a single `<span>` wrapper
    - Source: the five stanzas match D-15's approved draft verbatim (compare word for word against `02-CONTEXT.md` D-15; record the comparison in the SUMMARY)
    - Source: stanzas 4 and 5 carry `narr--land`
    - Source: `grep -ciE '\bego\b|grano|salis' site/index.html` returns `0` (gate G-8, D-13)
    - Source: `grep -c 'style="' site/index.html` returns `0` — no inline styles were introduced; the sketch's `<em style=…>` became `<em class="ph__em">`
    - Source: `grep -c 'data-sec=' site/index.html` returns `0` — the sketch's two-section split did not survive into production; `#method` is one section
    - Source: `site/styles.css` `:root` contains `--star-rigel: 96, 214, 255;`
    - Source: `site/styles.css` contains a `#method` rule whose body includes `--accent: var(--star-rigel)` (gate G-4)
    - CLI: `git diff --quiet site/star-engine.js` succeeds — `--star-rigel` was **not** added to `PAL` (L-3)
    - Source: every **fixed** spacing value introduced by this task is a member of {4, 8, 12, 16, 20, 24, 32, 48}px
    - Source: no font-weight above `400` was introduced
    - Source: `site/styles.css` still contains **zero** occurrences of the bare bracketed reveal selector (gate G-2)
    - CLI: `test ! -f site/package.json` succeeds (gate G-6)
    - Behavior: `getComputedStyle(document.querySelector('#method')).getPropertyValue('--accent')` returns a non-empty RGB triplet (gate G-4)
    - Behavior: with JS disabled, all five manifesto stanzas are fully readable
  </acceptance_criteria>

  <done>
    A visitor scrolling past Publications reads a full-height, indigo/cyan METHOD interlude carrying
    D-15's five-stanza manifesto verbatim, with no Latin, no inline styles, no numeral, and the accent
    resolving to a real triplet.
  </done>
</task>

<task type="auto">
  <name>Task 2: The calculator, as an object — markup and CSS</name>

  <files>site/index.html, site/styles.css</files>

  <read_first>
    - `.planning/sketches/001-method-section/index.html:639-660` — **variant D's calculator markup,
      LOCKED.** `.calc-wrap`, `.calc--big`, `.calc__screen`, `.calc__ghost`, `.calc__out`, the 4×4
      `.keys` grid
    - `.planning/sketches/001-method-section/index.html:270-313` — the calculator CSS, including the
      `word-break: break-all` rule and the `.calc__out.resolved` override that fixes sketch bug 3
    - `site/styles.css:12-40` — the surface tokens (`--bg-0`, `--glass-bg`, `--glass-border`,
      `--text-body`, `--text-hi`, `--font-mono`)
    - `site/styles.css:900-932` — `.media-slot`, for the shipped glass/dashed-border surface idiom
    - `.planning/phases/02-the-story/02-UI-SPEC.md` § `#method` (Act 1), § Typography T-3, § Spacing S-1
    - `.planning/phases/02-the-story/02-PATTERNS.md` § `site/index.html` — `#method` (the four
      mechanical conversions table)
  </read_first>

  <action>
    Add the calculator to `#method`'s Act 1, beside the manifesto. It ships **static** in this task —
    the sequence that drives it lands in Task 3. A static calculator showing `0` is a legitimate
    end-state: the manifesto already carries the argument, and the calculator dramatises it.

    **Markup — port `sketch:639-660`'s structure into `.calc-wrap` inside `.method__act--story`:**

    - `.calc` (drop the sketch's `.calc--big` modifier — there is no small variant in production;
      fold its values into `.calc`) containing `.calc__screen` → `.calc__ghost` + `.calc__out`, then
      `.keys` → sixteen `.key` elements in a 4×4 grid: `7 8 9 ÷ / 4 5 6 × / 1 2 3 − / 0 . = +`, with
      the four operator keys carrying `.key--op`.
    - **ids:** `#calc-ghost`, `#calc-out`, `#calc-keys` (the sketch used `d-ghost` / `d-out` /
      `d-keys` — the `d-` prefix was the variant selector and has no meaning in production).
    - `.calc__ghost` ships with `&nbsp;` as its content and `.calc__out` ships with `0`, exactly as the
      sketch does — the non-breaking space reserves the ghost line's height so the screen does not jump
      when the sequence writes into it.
    - `.calc` carries `data-reveal="1" data-reveal-kind="snap"` — this is the `snap` kind's first
      consumer (defined in plan 01 per UI-SPEC C-2). The calculator **pops** in with the overshoot
      curve, which suits an object appearing. ⚠ `snap` uses **no** `overflow: hidden`, so L-2's
      glow-clipping problem does not apply. ⚠ Do **not** wrap it in an `assemble` frame.
    - ⛔ **Drop the sketch's `<button class="replay" onclick="play('d',true)">`** (`sketch:772`). It is a
      sketch-only affordance and the sketch's **only inline event handler**; the live site has none.
    - ⚠ **Aria:** the calculator is decorative narration, not an interactive control. The `.keys` grid
      has no button semantics and no keyboard affordance. Mark the `.calc` `aria-hidden="true"` so a
      screen reader gets the manifesto — which carries the entire argument in text — rather than a
      soup of loose digits.

    **CSS — port the sketch's calculator geometry; re-theme colour and type from the live tokens:**

    - `.calc` — `max-width: 420px`; glass surface built from `--glass-bg` / `--glass-border` (the
      shipped idiom, cf. `.media-slot` at `:895-898`), not the sketch's ad-hoc `rgba(255,255,255,0.03)`.
    - `.calc__screen` — `min-height: 132px`, padding on the micro 4-multiple scale (the sketch's
      `20px 18px` → normalise the `18px`).
    - `.calc__ghost` — `12px` mono, `min-height: 16px`, `margin-bottom: 8px` (the sketch's `10px` →
      normalised), colour `rgba(var(--accent), 0.72)`.
    - `.calc__out` — `font: 400 clamp(28px, 3vw, 40px) var(--font-mono)`, colour `var(--text-hi)`, with
      `word-break: break-all` — **required** while the digit mash overflows the screen.
    - ⚠ **Sketch bug 3 — scope `word-break` off the resolved state.** `word-break: break-all` also
      splits the resolved *words*, producing `THE DIRECTION IS M / INE`. `.calc__out--resolved` must
      reset `word-break: normal; overflow-wrap: normal;` and drop to
      `font-size: clamp(15px, 1.5vw, 19px); letter-spacing: 0.14em; line-height: 1.75;`. This bug
      shipped in the sketch's first cut and was caught by rendering — **do not re-introduce it.**
    - `.key` — `aspect-ratio: 1`, `border-radius: 8px`, `1px solid var(--glass-border)`,
      `font: 400 17px var(--font-mono)`, `color` from `--text-body`, `gap: 8px` on the `.keys` grid.
      `cursor: default` — these are not buttons.
    - `.key--op` — `color: rgba(var(--accent), 0.75)`.
    - `.key--press` — the lit state: `background: rgba(var(--accent), 0.9); color: var(--bg-0);
      border-color: rgb(var(--accent)); transform: translateY(2px) scale(0.96); box-shadow: 0 0 22px
      rgba(var(--accent), 0.7);`.
    - ⚠ **The sketch's bare state classes are converted to BEM modifiers** — `.press` → `.key--press`,
      `.op` → `.key--op`, `.resolved` → `.calc__out--resolved`. This is a deliberate departure from
      "port verbatim", justified because the **live site** is the binding analog and it uses
      `--modifier` throughout (`.nav--docked` `main.js:117`, `.plate--flip`, `.section__numeral--right`,
      `.section__label--contact`). 02-PATTERNS.md's rule is "match the neighbouring code"; the sketch
      is not the neighbouring code.
    - ⚠ **`.key`'s `transition: all .1s` (`sketch:295`) → name the properties explicitly**
      (`background-color`, `color`, `border-color`, `transform`, `box-shadow`). `transition: all` is a
      performance footgun on a page with a live canvas rAF loop, and the live site never uses it
      (`styles.css:895-897`, `:1119-1121` both enumerate).
  </action>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && test "$(grep -c 'id="calc-out"' site/index.html)" = "1" && test "$(grep -c 'id="calc-ghost"' site/index.html)" = "1" && test "$(grep -c 'id="calc-keys"' site/index.html)" = "1" && test "$(grep -c 'class="key' site/index.html)" = "16" && test "$(grep -c 'key--op' site/index.html)" = "4" && test "$(grep -c 'onclick' site/index.html)" = "0" && test "$(grep -c 'class="replay"' site/index.html)" = "0" && test "$(grep -c 'calc--big' site/index.html)" = "0" && grep -q 'calc__out--resolved' site/styles.css && grep -A4 'calc__out--resolved' site/styles.css | grep -q 'word-break: normal' && test "$(grep -c 'transition: all' site/styles.css)" = "0" && test "$(grep -c 'style="' site/index.html)" = "0" && echo GATES-PASS</automated>
    <human-check>Look at the calculator beside the manifesto at 1280px. It should read as a real object on the page — glass surface, indigo operator keys — not as a widget bolted on. Narrow the window below 860px: the two columns must stack, not squash. Confirm the screen shows `0` and does not jump in height. Nothing about it should invite a click (no cursor change, no hover state) — it is narration, not a control.</human-check>
  </verify>

  <acceptance_criteria>
    - Source: `site/index.html` contains `id="calc-ghost"`, `id="calc-out"` and `id="calc-keys"`, one each
    - Source: `#calc-keys` contains exactly 16 `.key` elements, of which exactly 4 carry `key--op`
    - Source: `.calc` carries `data-reveal="1" data-reveal-kind="snap"` and `aria-hidden="true"`
    - Source: `grep -c 'onclick' site/index.html` returns `0` — the sketch's replay button and its inline handler were dropped
    - Source: `grep -c 'calc--big' site/index.html` returns `0` — the variant modifier was folded into `.calc`
    - Source: `site/styles.css` contains `.calc__out` with `word-break: break-all` **and** `.calc__out--resolved` with `word-break: normal` (sketch bug 3 fix)
    - Source: `site/styles.css` contains `.key--press` and `.key--op`; it contains **no** bare `.press`, `.op` or `.resolved` class selectors
    - Source: `grep -c 'transition: all' site/styles.css` returns `0` — every new transition enumerates its properties
    - Source: `grep -c 'style="' site/index.html` returns `0`
    - Source: every **fixed** spacing value introduced by this task is a member of {4, 8, 12, 16, 20, 24, 32, 48}px
    - Behavior: at ≤860px viewport width the calculator and manifesto stack into one column with no horizontal overflow (`document.scrollingElement.scrollWidth === document.scrollingElement.clientWidth`)
    - Behavior: with JS disabled, the calculator renders as a static object showing `0` and the manifesto beside it is fully readable
  </acceptance_criteria>

  <done>
    The calculator sits beside the manifesto as a themed object built from the live tokens, stacks
    cleanly under 860px, is hidden from screen readers, carries no inline handler or inline style, and
    its resolved state cannot split words mid-word.
  </done>
</task>

<task type="auto">
  <name>Task 3: The calculator plays — site/method.js, the locked timings, the reduced branch</name>

  <files>site/method.js, site/main.js</files>

  <read_first>
    - `.planning/sketches/001-method-section/index.html:962-1034` — **`playD()` and the `D` timing
      object, LOCKED.** This is the pacing Sirio approved after rejecting the first cut
    - `.planning/sketches/001-method-section/index.html:1062-1085` — the play-once guard and why it
      exists (sketch bug 2)
    - `.planning/sketches/001-method-section/index.html:1099-1123` — the sketch's IO wiring and `load`
      handler (both need converting)
    - `.planning/sketches/001-method-section/README.md` § "⚠ DO NOT COPY THIS SKETCH'S REVEAL
      MECHANISM" and § "Bugs found and fixed during verification"
    - `site/main.js:1-11` — the module header contract and the `REDUCED` / `DPR` / `tintAt` import line
    - `site/main.js:182-210` — `boot()` and the `document.fonts.ready` gate
    - `site/main.js:189-197` — the shipped `REDUCED` early-return gate shape
    - `site/star-engine.js:1-13` — `REDUCED` is exported here. **This is the single source of truth**
    - `site/star-engine.js:96-110` — the typewriter's `textContent` writes (the shipped no-`innerHTML`
      precedent)
    - `site/index.html:320` — **exactly one `<script type="module">` ships, and gate G-6 forbids adding
      another**
    - `.planning/phases/02-the-story/02-UI-SPEC.md` § C-1 (the arming contract — applies to METHOD's
      internals too), § C-3 (the locked `D` object), § C-4 (play-once guard), § C-5 (the reduced end
      state table)
    - `.planning/phases/02-the-story/02-PATTERNS.md` § `site/main.js` — METHOD sequence (the
      conversions table)
    - `.planning/phases/02-the-story/02-01-SUMMARY.md` — the reveal vocabulary as actually built
  </read_first>

  <action>
    Create **`site/method.js`** and wire it into `boot()`. This is the choreography that makes the
    calculator play.

    **Module placement — this is a real decision, recorded here.** `main.js`'s own header contract
    (`:1-9`) states it owns the cosmic engine, the palette, the tempo, the deep field and ONE rAF loop.
    METHOD's calculator is timer-driven section choreography, not engine work, so it gets its own
    module. ⚠ **But `index.html:320` ships exactly one `<script type="module">` and gate G-6 forbids a
    new `<script src>`** — therefore `method.js` must be reached by an **import from `main.js`**, never
    a second script tag. `method.js` imports `REDUCED` **directly from `./star-engine.js`**, not
    re-exported through `main.js` — `star-engine.js:12` is the single source of truth (D-36).

    **`site/method.js` contents:**

    - A file header comment in this codebase's convention (`main.js:1-9`) naming what the module owns
      (METHOD's timer-driven choreography), what it must never do (add a second rAF loop — D-37; add a
      second reduced-motion source — D-36; write `innerHTML` — L-5), and that it is imported by
      `main.js` rather than script-tagged because only one module tag ships.
    - **The `D` timing object at the very top of the module, ported VERBATIM from `sketch:966-972`:**
      `keyStep: 240` · `mashPause: 3400` · `preOrder: 1200` · `lineHold: 2600` · `finalHold: 2800`.
      ⚠ **Keep them in one object, at the top, with the sketch's comments recording each first-cut
      value.** Timing is the whole point of this rebuild: **Sirio could not read the first cut's
      sentences** and every hold was roughly tripled (~7s → ~21s). Re-pacing must stay a **numbers-only
      edit**, and expect him to want them slower still, not faster.
    - **`playCalc()`** — port `sketch:974-1034`'s beats exactly:
      1. Clear and reset the tracked timer array; remove `key--press` from every key; remove
         `calc__out--resolved`; set `white-space: pre-line` on the output (the resolved chain stacks on
         newlines).
      2. ⚠ **The reduced branch, FIRST, before any timer is scheduled** (`sketch:985-990`, UI-SPEC C-5):
         if `REDUCED`, set the ghost to `MAP → STRATEGY → EXECUTE`, add `calc__out--resolved`, set the
         output to `THE DIRECTION IS MINE`, and `return`. **It skips to the RESOLVED state, not a blank
         one — the argument still lands; only its pacing is lost.** That is the C-5 test: if any claim
         exists only in motion, the section is wrong.
      3. Beat 1 — ghost `873 ÷ 41 = ?`, output `0`, then mash the digit sequence
         `7 3 8 4 1 9 2 6 5 0 3 7` at `D.keyStep` intervals starting at `900ms`, adding `key--press` to
         the matching key for `130ms` each and appending to a rolling 8-character output string.
      4. Beat 2 — after `D.mashPause`: ghost `ANSWER MATCHED. UNDERSTOOD NOTHING.`, output `21.29268`.
         ⚠ **The long hold IS the point** — it is the moment the whole section argues for.
      5. Beat 3 — after `D.preOrder`: ghost `THEN I LEARNED THE ORDER`, add `calc__out--resolved`,
         clear the output, then write `MAP THE PROBLEM`, `CHOOSE THE STRATEGY`, `LET THE TOOL EXECUTE`
         one per `D.lineHold`, **stacking with `\n` so they stay on screen** — there must be time to
         read them.
      6. Finally, after `3 * D.lineHold + D.finalHold`: ghost `NEVER THE REVERSE`, output
         `THE DIRECTION IS MINE`.
    - ⚠ **`textContent`, never `innerHTML`** (L-5). The sketch's variant D uses `textContent`
      throughout (`sketch:1002`, `:1010`, `:1025`), matching `star-engine.js:103-104`.
      ⚠ **Variant A is NOT the locked variant and must not be ported from** — `sketch:859-860` builds
      an `ACC` HTML string and renders it. `01-PATTERNS.md:366-368`: *"'Improving' it to `innerHTML`
      adds an injection sink for free."*
    - **Timer hygiene** (`sketch:973-975`): track every `setTimeout` handle in a module-scope array and
      clear them all at the start of a run.
    - **The play-once guard** (`sketch:1062-1069`) — ⚠ **sketch bug 2:** the `load` handler and the
      IntersectionObserver both fired the sequence, and the second run cleared the first's timers
      mid-play, so the animation visibly stalled. Guard with a module-scope `played` Set (or boolean)
      so the sequence runs once per page load.
    - **`initMethod()`** — the module's single export. It:
      - Early-returns if `#method` is absent (the module must be inert on any page without it).
      - If `REDUCED`: call `playCalc()` immediately and `return` — **do not attach an observer.** ⚠ The
        sketch learned this the hard way (`sketch:1111-1117`): under reduced motion there is no
        sequence to play, so a missed or late IO callback would leave the section unresolved forever.
      - Otherwise: observe the `.calc` (or `.method__act--story`) with an `IntersectionObserver` at
        `{ threshold: 0.18 }` — the sketch's value — and call `playCalc()` on the first intersection,
        through the guard.
      - ⚠ **Never call `getBoundingClientRect()` in the IO callback** (Pitfall 2 / gate G-5).
      - ⚠ **No rAF loop, ever** (D-37).

    **Conversions the sketch requires before it is production code:**

    | Sketch | Production | Why |
    |---|---|---|
    | `body.js .ph__wire { transform: scaleX(0) }` scoping | **the `data-rev-state` contract** | ⛔ `body.js` **shipped a dark page twice** — it only avoids the invisible page by winning a CSS specificity race `(0,4,1)` vs `(0,3,1)`, which a future edit silently loses. Gate G-2 greps `site/` for `body.js` and requires **zero hits**. This plan's calculator uses `data-reveal-kind="snap"` + the plan-01 vocabulary and needs no bespoke state at all |
    | `isReduced()` = `RQ.matches \|\| FORCED_REDUCED` (`sketch:800-802`) | `REDUCED` from `./star-engine.js` | `FORCED_REDUCED` is the sketch toolbar's override — sketch-only. D-36: one source of truth |
    | `@media (prefers-reduced-motion)` at `sketch:391` | **drop it** | D-36 forbids a second query; `styles.css:1156` already exists |
    | `window.addEventListener('load', …)` (`sketch:1123`) | called from `boot()` | `boot()` is gated on `document.fonts.ready` (`main.js:206`) |
    | `RQ.addEventListener('change', …)` (`sketch:1119`) | **drop it** | sketch toolbar plumbing; `REDUCED` is read once at load, matching `star-engine.js:12` |

    **`site/main.js` — two lines only:**
    - Add `import { initMethod } from './method.js';` beside the existing `./star-engine.js` import at
      `:11`.
    - Call `initMethod()` from `boot()`, after `initReveals()`.
    - ⚠ **Do NOT touch `loop()` (`:158-163`) or `onScroll()` (`:165-169`).** METHOD is observer- and
      timer-driven (D-37, gate G-5).
    - **Listener convention:** every listener in `main.js` is `{ passive: true }` (`:186-187`, `:195`,
      `:199`). Match it if `method.js` adds any.
  </action>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && node --check site/method.js && node --check site/main.js && test "$(grep -c 'export function initMethod(' site/method.js)" = "1" && grep -q "from './star-engine.js'" site/method.js && grep -q "from './method.js'" site/main.js && test "$(grep -c 'initMethod()' site/main.js)" = "1" && test "$(grep -c 'innerHTML' site/method.js)" = "0" && test "$(grep -c 'getBoundingClientRect' site/method.js)" = "0" && test "$(grep -c 'requestAnimationFrame' site/method.js)" = "0" && test "$(grep -c 'matchMedia' site/method.js)" = "0" && test "$(grep -c 'keyStep:   240' site/method.js)" = "1" && test "$(grep -c 'mashPause: 3400' site/method.js)" = "1" && test "$(grep -c 'lineHold:  2600' site/method.js)" = "1" && test "$(grep -c 'finalHold: 2800' site/method.js)" = "1" && test "$(grep -c 'preOrder:  1200' site/method.js)" = "1" && test "$(grep -rc 'body\.js' site/ 2>/dev/null | grep -vc ':0')" = "0" && test "$(grep -c '<script' site/index.html)" = "1" && test "$(grep -c 'getBoundingClientRect' site/main.js)" = "3" && echo GATES-PASS</automated>
    <human-check>⚠ **This is the section Sirio asked to be surprised by — watch it, do not just verify it.** Reload and scroll to METHOD. Time the whole sequence: it should run ~21 seconds. The keys must mash visibly, the meaningless answer `21.29268` must sit for a full 3.4 seconds (that hold IS the argument), then the three order lines must stack and stay long enough to read each one before the next lands, ending on `THE DIRECTION IS MINE` on one line — **not** split as `THE DIRECTION IS M / INE`. Scroll away and back: it must not restart mid-play. Then toggle `prefers-reduced-motion: reduce` and reload: the calculator must show `MAP → STRATEGY → EXECUTE` and `THE DIRECTION IS MINE` immediately, with the argument fully intact. **If any of it still reads too fast, the fix is the `D` object and nothing else** — Sirio could not read the first cut, and the lesson is that reveal timings tuned by eye run far too fast.</human-check>
  </verify>

  <acceptance_criteria>
    - CLI: `node --check site/method.js` exits 0
    - CLI: `node --check site/main.js` exits 0
    - Source: `site/method.js` contains `export function initMethod(` exactly once
    - Source: `site/method.js` imports `REDUCED` from `'./star-engine.js'` — not from `main.js`, and not via a second `matchMedia` (D-36); `grep -c 'matchMedia' site/method.js` returns `0`
    - Source: `site/main.js` contains `from './method.js'` and calls `initMethod()` exactly once, inside `boot()`
    - Source: `site/method.js` contains the `D` object with exactly the locked values `keyStep: 240`, `mashPause: 3400`, `preOrder: 1200`, `lineHold: 2600`, `finalHold: 2800`, declared as one object at the top of the module
    - Source: `grep -c 'innerHTML' site/method.js` returns `0` (L-5)
    - Source: `grep -c 'getBoundingClientRect' site/method.js` returns `0` (gate G-5)
    - Source: `grep -c 'requestAnimationFrame' site/method.js` returns `0` (D-37 — no second loop)
    - Source: `site/main.js` `getBoundingClientRect` count is still exactly `3`, and `loop()` / `onScroll()` are byte-identical to their pre-task state (D-37, gate G-5)
    - Source: `grep -rn 'body\.js' site/` returns zero hits (gate G-2)
    - CLI: `grep -c '<script' site/index.html` returns `1` — `method.js` is reached by import, not a second script tag (gate G-6)
    - Source: `playCalc()`'s `REDUCED` branch appears **before** any `setTimeout` is scheduled and sets the ghost to `MAP → STRATEGY → EXECUTE` and the output to `THE DIRECTION IS MINE`
    - Source: a module-scope play-once guard exists and the sequence cannot be started twice by a single page load (sketch bug 2)
    - Behavior: scrolling `#method` into view once plays the full sequence to `THE DIRECTION IS MINE`; scrolling away and back does not restart or stall it
    - Behavior: the final `THE DIRECTION IS MINE` renders on one line, not split mid-word (sketch bug 3)
    - Behavior: with `prefers-reduced-motion: reduce`, no `IntersectionObserver` is constructed by `method.js` and the calculator shows the resolved chain on load
  </acceptance_criteria>

  <done>
    The calculator plays its ~21-second sequence once, on its own scroll position, from a `method.js`
    imported by `main.js`; the locked `D` timings are one editable object at the top; reduced motion
    resolves immediately with the argument intact; `loop()` and `onScroll()` are untouched and no
    second script tag, rAF loop, `matchMedia` or `innerHTML` was introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages CDN → visitor's browser | The only boundary that exists. Static, client-side-only, no server runtime, no auth, no sessions, no data store, **no input fields**, no secrets in client code (`PROJECT.md:37`). |
| Google Fonts → page | Third-party subresource, shipped in Phase 1 (`index.html:9-12`, `crossorigin`). Untouched — this plan adds **no** `<script src>` and no new third-party resource. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | `site/method.js` — the calculator's type-on writes | mitigate | **DOM XSS via `innerHTML`.** The type-on writes text into `#calc-ghost` and `#calc-out` on a timer. All strings are author-controlled literals, so exploitability is ~nil today — but `innerHTML` would add an injection sink for free and the next contributor inherits it. Mitigation: **`textContent` only**, matching `star-engine.js:103-104`. Enforced by the acceptance criterion `grep -c 'innerHTML' site/method.js` = 0 (L-5). ⚠ Variant A of the sketch builds an HTML string (`sketch:859-860`); it is **not** the locked variant and must not be ported from. |
| T-02-11 | Tampering | `#method` markup | mitigate | The sketch ships an inline event handler (`onclick="play('d',true)"`, `sketch:772`) and an inline `style=` attribute (`sketch:725`). Both are dropped: the replay button entirely, and the inline style into `.ph__em`. Enforced by `grep -c 'onclick' site/index.html` = 0 and `grep -c 'style="' site/index.html` = 0. This keeps the page compatible with a strict CSP should Phase 5 add one. |
| T-02-12 | Tampering | dependency surface | mitigate | **Zero packages installed.** `02-RESEARCH.md` § Package Legitimacy Audit records zero packages phase-wide. `method.js` is a first-party ES module reached by import from `main.js`; **no new `<script src>`** (gate G-6, `grep -c '<script' site/index.html` = 1). D-35's no-library rule is incidentally the strongest security control in this phase — it eliminates the supply-chain surface entirely. |
| T-02-13 | Denial of Service (self-inflicted) | scroll path on mid-range mobile | mitigate | `method.js` adds **no rAF loop** (`grep -c 'requestAnimationFrame'` = 0), **no `getBoundingClientRect`** (= 0), and does not touch `loop()` or `onScroll()`. The calculator is `setTimeout`-driven and its observer resolves geometry in the browser's own layout phase. Timer handles are tracked and cleared, so a re-entry cannot leak timers (sketch bug 2). |
| T-02-14 | Denial of Service (self-inflicted) | every visitor, JS-disabled or module-load failure | mitigate | ⛔ **The `body.js` pattern is forbidden.** It shipped a dark page **twice** in the sketch by winning/losing a CSS specificity race. `#method` uses only the plan-01 `data-rev-state` vocabulary, whose two states sit at identical specificity on a JS-created attribute. Verified by `grep -rn 'body\.js' site/` = 0 and by the JS-disabled criterion: all five manifesto stanzas readable. Severity would be **high** if missed. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | **Not applicable — this plan runs zero package-manager installs.** If any task is later amended to add a dependency (GSAP, Lenis, Splitting.js, AOS, ScrollMagic) it contradicts D-35 and must be **rejected, not audited** — escalate to Sirio (`02-RESEARCH.md` planner guard). |

**No `high`-severity threat is left unmitigated.** `security_block_on: high` is satisfied.

**Explicitly not applicable on this stack:** Spoofing, Repudiation, Elevation of Privilege, and ASVS
V2/V3/V4/V6. ⚠ **ASVS V5 (Input Validation) has no target here — this plan adds zero input fields.**
CONT-03 is a *narrative section about how Sirio works with AI*. The **AI Concierge (ASKAI-\*) is
Phase 6** and is the phase that introduces an input surface. **Do not conflate them.**
</threat_model>

<verification>
Gates from `02-UI-SPEC.md` § Verification Gates reachable in this plan:

- **G-1 (JS disabled):** all five manifesto stanzas readable; the calculator renders static showing `0`.
- **G-2 (greps clean):** `grep -c '\[data-reveal\]' site/styles.css` → `0`; `grep -rn 'body\.js' site/`
  → zero hits.
- **G-3 (reduced motion):** METHOD fully readable; the calculator shows `MAP → STRATEGY → EXECUTE` and
  `THE DIRECTION IS MINE`; **nothing lost**.
- **G-4 (accent resolves):** `getComputedStyle(document.querySelector('#method')).getPropertyValue('--accent')`
  returns a triplet.
- **G-5 (no loop reads):** `grep -c 'getBoundingClientRect' site/main.js` → `3`; `site/method.js` → `0`;
  `loop()` / `onScroll()` byte-identical.
- **G-6 (zero dependencies):** no `site/package.json`; `grep -c '<script' site/index.html` → `1`.
- **G-8 (no banned phrasing):** `grep -ciE '\bego\b|grano|salis' site/index.html` → `0`.

Not reachable in this plan: G-7 (no new outbound links until plan 04), G-9 (plan 02), G-10 (the OI-2
skills line lands in plan 04).
</verification>

<success_criteria>
- CONT-03's first act ships: the manifesto carries the philosophy in Sirio's terse voice, opening on
  his own childhood calculator story, landing on *the direction is still mine*.
- D-13 holds against three sources that all name the banned phrase (`profile.md:373`,
  `PROJECT.md:48`, `CLAUDE.md`): no Latin reaches the page.
- D-20 is closed by sketch 001 variant D, and the sketch's **structure/objects/animation** ported
  while its **theme, `body.js` plumbing, inline handler, inline style and toolbar** did not.
- The three sketch bugs stay fixed: no dark page (bug 1 — `data-rev-state`, not `body.js`), no stall
  (bug 2 — play-once guard), no `M / INE` split (bug 3 — `word-break` scoped off the resolved state).
- D-36/D-37 hold: one reduced-motion source, one rAF loop, `loop()` and `onScroll()` untouched.
- Zero dependencies, one script tag.
</success_criteria>

<output>
Create `.planning/phases/02-the-story/02-03-SUMMARY.md` when done.

Record explicitly:
- **The module-placement decision** — `site/method.js` imported from `main.js` rather than living in
  `main.js` or getting its own `<script src>` — and why (02-PATTERNS.md flagged this as a genuine open
  planner decision; the next plan extends this module and must not re-litigate it).
- The manifesto compared **word for word** against D-15, with any deviation justified.
- The `--star-rigel` value shipped, and that Sirio has **not yet seen either new stop on a render**
  (OI-1) — including the rigel→sirius adjacency at Contact.
- The BEM conversion of the sketch's state classes (`.press` → `.key--press`, `.op` → `.key--op`,
  `.resolved` → `.calc__out--resolved`), so plan 04 follows the same convention.
- The measured wall-clock duration of the calculator sequence, and whether Sirio wanted it slower still.
</output>
