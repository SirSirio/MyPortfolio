---
phase: 02-the-story
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - site/index.html
  - site/styles.css
  - site/assets/media/README.md
  - site/assets/media/ot2-timelapse.mp4
  - site/assets/media/ot2-timelapse-poster.jpg
  - site/assets/media/ot2-part-adapter.png
  - site/assets/media/ot2-part-pcr-strip-racks.png
  - site/assets/media/ot2-part-buffer-ladder-rack.png
  - site/assets/media/ot2-part-rack-holder.png
autonomous: true
requirements: [MEDIA-02]

must_haves:
  truths:
    - "The Automated Gel Electrophoresis card shows the real OT-2 robot running in the real DTU lab instead of a striped placeholder"
    - "The clip is under 2MB and shows a poster frame before it decodes, so a recruiter on cellular is not made to download 6.66MB to see a still"
    - "Beside the clip, the four CAD renders of Sirio's own printed parts sit on a blueprint plate — the card says 'I designed the parts AND automated the run' in one glance"
    - "The renders are not cropped by the 16:9 slot"
    - "The other two Work cards keep their labelled placeholders and are not faked"
  artifacts:
    - path: "site/assets/media/ot2-timelapse.mp4"
      provides: "The re-encoded OT-2 timelapse, under 2MB"
    - path: "site/assets/media/ot2-timelapse-poster.jpg"
      provides: "The poster frame, mandated by the MEDIA-01 contract"
    - path: "site/styles.css"
      provides: ".media-slot--composite and .blueprint — the acknowledged MEDIA-01 deviation"
      contains: "media-slot--composite"
  key_links:
    - from: "site/index.html plate 002 figure"
      to: "site/assets/media/ot2-timelapse.mp4"
      via: "a relative src, committed to the repo — never a remote hot-link"
      pattern: "\\./assets/media/ot2-timelapse\\.mp4"
    - from: "site/styles.css .media-slot--composite > video"
      to: "site/styles.css .media-slot > video"
      via: "a (0,2,1) override of the shipped (0,1,1) object-fit:cover rule"
      pattern: "media-slot--composite\\s*>\\s*video"
---

<objective>
Fill the Automated Gel Electrophoresis card with Sirio's real, first-party OT-2 media — the
timelapse of the actual robot running in the actual DTU lab, beside the four CAD renders of the
actual green parts it is handling.

**This is in scope and it is not scope creep.** `ROADMAP.md:5` and `:104` pre-authorize exactly
this — *"media can be dropped in **before** this phase at any time"*, *"not a gate on Sirio"* — and
MEDIA-01 built the slots to accept it. Sirio asked for real media so the site is shareable right
after this phase. **Phase 4 still owns the formal MEDIA-02 encoding pass** (compression targets,
responsive `srcset`, lazy-loading); this plan only fills one existing slot.

Implements D-25 (the renders have genuine alpha), D-27 (the baked-in shadows need eyes),
D-28 (the blueprint plate), D-29 (the composite — Sirio's own idea), D-30 (the timelapse is
first-party and verified), D-31 (6.66MB must not ship), D-32 (the other two cards keep placeholders).

⚠ **Requirement note:** this plan advances **MEDIA-02** partially — it fills the OT-2 home-card slot.
`REQUIREMENTS.md:126` traces MEDIA-02 to Phase 4, which remains true for the case-study slots and the
formal encoding pass. Flag this to the orchestrator: the traceability table should record Phase 2 as
partially advancing MEDIA-02, or MEDIA-02 should be split.

Output: the re-encoded clip + poster + four renders in `site/assets/media/`; the composite slot on
plate 002; `.media-slot--composite` and `.blueprint` CSS.
</objective>

<execution_context>
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/workflows/execute-plan.md
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-the-story/02-CONTEXT.md
@.planning/phases/02-the-story/02-RESEARCH.md
@.planning/phases/02-the-story/02-UI-SPEC.md
@.planning/phases/02-the-story/02-PATTERNS.md
@.planning/phases/02-the-story/02-04-SUMMARY.md
</context>

## Phase Goal

**As a** recruiter reading the Automated Gel Electrophoresis card, **I want to** see the actual robot
actually running with the actual parts he printed **so that** "144 samples per run" stops being a
number on a page and becomes a thing he demonstrably built.

## Artifacts this phase produces

Every symbol below is **created by Phase 2** and does not exist in the codebase today. The
source-grounding pass must treat these as new, not as drift.

**New files (whole phase):** `site/method.js` (plan 03) · **`site/assets/media/ot2-timelapse.mp4` ·
`site/assets/media/ot2-timelapse-poster.jpg` · `site/assets/media/ot2-part-adapter.png` ·
`site/assets/media/ot2-part-pcr-strip-racks.png` · `site/assets/media/ot2-part-buffer-ladder-rack.png` ·
`site/assets/media/ot2-part-rack-holder.png` (this plan)**

**JS functions (whole phase):** `initReveals()` (plan 01) · `initMethod()` · `playCalc()` (plan 03) ·
`playTrack()` · `initArc()` (plan 04). **This plan adds no JavaScript at all.**

**CSS custom properties (whole phase):** `--rev-stagger` · `--rev-i` (plan 01) · `--star-zuben`
(plan 02) · `--star-rigel` (plan 03) · `--len` (plan 04)

**HTML attributes (whole phase):** `data-rev-state` · `data-reveal-kind` (plan 01) · `data-track` ·
`data-phases` · `data-ph-state` · `data-arc-state` · `data-rail-state` (plan 04)

**New element ids (whole phase):** `#origin` (plan 02) · `#method` · `#calc-ghost` · `#calc-out` ·
`#calc-keys` (plan 03) · `#loop-arc` · `#arc-path` · `#arc-head` · `#doc-rail` (plan 04)

**New CSS classes (whole phase):** `.section--full` · `.section__label--interlude` · `.narr` ·
`.narr--land` · `.origin__beats` · `.contact__links` · `.contact__link` (plan 02) · `.method__act` ·
`.calc-wrap` · `.calc` · `.calc__screen` · `.calc__ghost` · `.calc__out` · `.calc__out--resolved` ·
`.keys` · `.key` · `.key--op` · `.key--press` · `.manifesto` · `.ph__em` (plan 03) · `.tracks` ·
`.track` · `.track--digital` · `.track--physical` · `.track__head` · `.track__kind` · `.track__for` ·
`.track__title` · `.track__foot` · `.phases` · `.phases--line` · `.phases--loop` · `.ph` ·
`.ph__dot` · `.ph__wire` · `.ph__n` · `.ph__name` · `.ph__desc` · `.ph__note` · `.loop-arc` ·
`.doc-rail` · `.doc-rail__tag` · `.doc-rail__txt` · `.evidence` · `.evidence__label` · `.ev-grid` ·
`.ev` · `.deck-link` (plan 04) · **`.media-slot--composite` · `.blueprint` · `.blueprint__part`
(this plan)**

<tasks>

<task type="auto">
  <name>Task 1: The real robot runs on the card — re-encode the timelapse and fill the slot</name>

  <files>site/assets/media/ot2-timelapse.mp4, site/assets/media/ot2-timelapse-poster.jpg, site/index.html, site/styles.css</files>

  <read_first>
    - `site/assets/media/README.md` — **the MEDIA-01 contract. Read it before dropping any asset.**
      Especially: no stock and no AI-generated imagery ever; first-party only; commit the files and use
      relative paths, never a remote `src`; always pair a clip with a `poster`; naming is lowercase and
      hyphenated matching the project slug
    - `site/index.html:178-190` — **plate 002 (Automated Gel Electrophoresis), the target.** The
      `<figure data-para="0.05" class="media-slot" data-label="[ project video / image ]">` at `:183`
    - `site/styles.css:880-932` — `.media-slot`, the `:not(:has(> img, > video))` empty state, and the
      `.media-slot > img, .media-slot > video { object-fit: cover }` filled state at `:921-927`
    - `.planning/phases/02-the-story/02-CONTEXT.md` D-29, D-30, D-31
    - `.planning/phases/02-the-story/02-RESEARCH.md` § Code Examples → D-31 video re-encode (the exact
      ffmpeg invocations), § Common Pitfalls 9, § Environment Availability
    - `.planning/phases/02-the-story/02-PATTERNS.md` § `site/assets/media/*`
  </read_first>

  <action>
    Put Sirio's real OT-2 timelapse on the Gel Electrophoresis card, in the composite's video column.
    The blueprint plate beside it lands in Task 2 — this task delivers the working, visible half.

    **Re-encode first.** ⚠ **D-31 / Pitfall 9 is an ordering constraint:** the source is
    `Opentrons-OT2 DALSA media/TimelapseOT2.mp4` at **6,656,975 bytes** — 720×1280 (9:16), H.264,
    16.9s, 30fps. **The re-encode must land before or with the slot fill**, or the phase ships a home
    page autoplaying 6.66MB to a recruiter on cellular. Recruiters opening links on phones is a core
    project constraint, not a nicety.

    - ffmpeg and ffprobe are **verified available** (`2023-06-19-git-1617d1a752-full_build`, gyan.dev).
    - Encode to `site/assets/media/ot2-timelapse.mp4` with `libx264`, `-crf 28`, `-preset slow`,
      `-pix_fmt yuv420p`, `-an`, `-movflags +faststart`. `-an` drops audio — it is a silent timelapse,
      so that is free bytes. `+faststart` moves the moov atom to the front so it streams progressively
      rather than needing the whole file before the first frame.
    - Extract the poster to `site/assets/media/ot2-timelapse-poster.jpg` with `-ss 00:00:02
      -frames:v 1`. ⚠ **The poster is mandated by `site/assets/media/README.md`, always** — it is what
      shows before the video decodes and on any connection that never gets there.
    - **Target: under 2MB.** Verify with `ffprobe` / file size and **eyeball a frame before
      committing**. If CRF 28 misses, adjust CRF — do **not** build a pipeline. **D-31: do not
      over-engineer here; Phase 4 owns the formal MEDIA-02 pass.**
    - ⚠ Check the poster frame at `00:00:02` actually shows the robot rather than an empty deck; if it
      does not, pick a better timestamp. The frames are confirmed to show **the real OT-2 running in
      the real DTU lab with Sirio's actual green 3D-printed parts** — the same parts as the CAD renders
      — plus pink PCR strip racks and a blue gel tray. **That render → real-thing pairing is the
      strongest asset in the set**; the poster should show it.
    - Both files are **committed to the repo** and referenced with **relative paths**. ⚠ Never
      hot-link a remote `src` — the README states why: it hands a third party the ability to change
      what the portfolio shows.

    **Markup — plate 002's `<figure>` at `index.html:183`:**

    - Add `media-slot--composite` to the figure's class list. Keep `data-para="0.05"` and keep
      `data-label` — the README says the label costs nothing and comes back automatically if the asset
      is ever removed.
    - Inside it, a `<video>` with a **relative** `src="./assets/media/ot2-timelapse.mp4"`,
      `poster="./assets/media/ot2-timelapse-poster.jpg"`, and `muted loop playsinline autoplay`,
      matching the README's clip example exactly. `playsinline` is what stops iOS taking the video
      fullscreen.
    - Give the `<video>` a real `alt`-equivalent: since `<video>` takes no `alt`, add an
      `aria-label` describing what it shows (the OT-2 running the gel-electrophoresis protocol with
      Sirio's printed parts).
    - Leave a placeholder child element (or nothing) for the blueprint column in this task — Task 2
      fills it.

    **CSS — the composite. ⚠ This is a DELIBERATE, ACKNOWLEDGED MEDIA-01 DEVIATION (D-29) and it must
    be named as such in the SUMMARY.** MEDIA-01's contract is *"drop-in plus one line, no CSS change"*;
    a composite needs new CSS. Sirio accepted this knowingly.

    ⚠ **The surviving reason is aspect ratio, not background.** The slots are 16:9 (1.78:1) with
    `object-fit: cover`; the timelapse is 9:16 (0.5625:1) and the four renders are 1.13:1, 1.68:1,
    1.26:1 and 1.48:1. A raw cover-crop of a 9:16 clip in a 16:9 box shows only a narrow horizontal
    band of it — the composite is what makes both assets legible.

    - `.media-slot--composite` — `display: grid; grid-template-columns: auto 1fr;` with a
      micro-tier gap and padding from {4, 8, 12, 16, 20, 24, 32, 48}, on a `var(--bg-0)` ground. The
      geometry is Sirio's own: **a 9:16 video at full height inside a 16:9 box occupies 81/256 ≈ 31.6%
      of the width**, leaving ~68% for the parts plate. `grid-template-columns: auto 1fr` with a
      height-driven video reproduces that without hard-coding the percentage.
    - `.media-slot--composite > video` — `height: 100%; width: auto; aspect-ratio: 9 / 16;
      object-fit: cover;` plus a small `border-radius`. ⚠ This is specificity **(0,2,1)** and therefore
      overrides the shipped `.media-slot > img, .media-slot > video` rule at **(0,1,1)**
      (`styles.css:921-927`) which would otherwise stretch it to `width: 100%`. **Do not edit the
      shipped rule** — other slots depend on it.
    - ⚠ **Do not touch `.media-slot:not(:has(> img, > video))`** (`:901`). The `<video>` is a **direct
      child**, so the striped placeholder switches itself off automatically — that is the mechanism
      working as designed. It uses `:has()` rather than `:empty` on purpose: whitespace counts as a
      text node, so a normally formatted `<figure>` would never be `:empty`.
    - **Responsive:** below the plate's stacking breakpoint the composite must not squash the video to
      a sliver. Collapse to a single column with the video on top, or cap the video's height — decide
      on the render. ⚠ Phase 3 owns the real-device mobile pass; do not over-tune here.

    ⚠ **D-32 — the other two Work cards keep their labelled placeholders.** Plate 001 (Portable
    Precision Liquid Dispenser, `index.html:169`) and plate 003 (EndoSense, `:198`) get **no** media in
    this plan. Sirio will supply theirs. **This does not block anything** — MEDIA-01 guarantees no
    re-engineering either way, and `site/assets/media/README.md` argues the case plainly:
    *"An empty labelled placeholder is strictly better than a fake — placeholders read as 'not shot
    yet', fakes read as 'not real'."* **It is right. Do not fake them, and do not substitute a vendor
    image.**

    ⛔ **D-33 — the vendor images are OUT OF SCOPE and must not be used.** `Opentrons-OT2.png` and
    `RunOne Gel Electrophoresis System.png` are the **manufacturers' copyrighted marketing renders**,
    not Sirio's work. Research R-02 fetched `opentrons.com/trademarks` and found **no permission grant,
    no editorial-use guideline, no press-kit licence**; Embi Tec publishes no media-resources page at
    all. **Silence is not permission** — absent an explicit grant, marketing renders are all rights
    reserved by default, and trademark registration notices govern *marks*, not *photographs*.
    Attribution and licence are separate: crediting Opentrons creates no permission. **Neither vendor
    image is needed here** — Sirio's own timelapse shows the real OT-2 he programmed, which is both
    permitted **and** better storytelling. **The whole decision is deferred to Phase 2.1.** Do not
    import, reference, or "temporarily" use either file.
  </action>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && test -f site/assets/media/ot2-timelapse.mp4 && test -f site/assets/media/ot2-timelapse-poster.jpg && SZ=$(stat -c%s site/assets/media/ot2-timelapse.mp4) && echo "clip bytes: $SZ" && test "$SZ" -lt 2097152 && ffprobe -v error -select_streams v:0 -show_entries stream=width,height,codec_name -of csv=p=0 site/assets/media/ot2-timelapse.mp4 && test "$(ffprobe -v error -select_streams a -show_entries stream=index -of csv=p=0 site/assets/media/ot2-timelapse.mp4 | wc -l)" = "0" && grep -q 'media-slot--composite' site/index.html && grep -q './assets/media/ot2-timelapse.mp4' site/index.html && grep -q 'poster="./assets/media/ot2-timelapse-poster.jpg"' site/index.html && grep -q 'playsinline' site/index.html && test "$(grep -c 'media-slot' site/index.html)" = "3" && test "$(grep -ciE 'Opentrons-OT2\.png|RunOne' site/index.html)" = "0" && test "$(grep -c 'https://' site/index.html | head -1)" -gt "0" && ! grep -qE 'src="https?://' site/index.html && grep -q 'media-slot--composite > video' site/styles.css && echo GATES-PASS</automated>
    <human-check>Open the Work section. The Gel Electrophoresis card must now show the real OT-2 running — not a striped placeholder, and not a narrow cropped band of it. Confirm the poster frame shows the robot (not an empty deck) by throttling the network in DevTools and reloading. Compare the re-encoded clip against the 6.66MB original side by side: has anything visibly degraded? If yes, lower the CRF a notch — but keep it under 2MB. Narrow to phone width: the video must not squash to a sliver.</human-check>
  </verify>

  <acceptance_criteria>
    - CLI: `site/assets/media/ot2-timelapse.mp4` exists and is **< 2,097,152 bytes** (D-31)
    - CLI: `site/assets/media/ot2-timelapse-poster.jpg` exists
    - CLI: `ffprobe` reports the clip's video stream as `h264`, 720×1280
    - CLI: `ffprobe` reports **zero** audio streams (`-an` was applied — free bytes on a silent timelapse)
    - Source: `site/index.html` plate 002's `<figure>` carries both `media-slot` and `media-slot--composite`, and retains `data-para="0.05"` and its `data-label`
    - Source: the `<video>` uses the **relative** `src="./assets/media/ot2-timelapse.mp4"` and `poster="./assets/media/ot2-timelapse-poster.jpg"`, with `muted loop playsinline autoplay` and an `aria-label`
    - Source: `grep -qE 'src="https?://' site/index.html` finds **no match** — no remote hot-linked media (the README forbids it: it hands a third party control of what the portfolio shows)
    - Source: `grep -c 'media-slot' site/index.html` returns `3` — plates 001 and 003 still carry their labelled placeholders, unfaked (D-32)
    - Source: `grep -ciE 'Opentrons-OT2\.png|RunOne' site/index.html` returns `0` — no vendor marketing render was used (D-33, deferred to Phase 2.1)
    - Source: `site/styles.css` contains `.media-slot--composite > video` with `height: 100%` and `width: auto`, at specificity (0,2,1)
    - Source: `site/styles.css` `.media-slot > img, .media-slot > video` rule at `:921-927` is byte-identical to its pre-task state — other slots depend on it
    - Source: `site/styles.css` `.media-slot:not(:has(> img, > video))` rule at `:901` is byte-identical to its pre-task state — the placeholder switches off on its own
    - Source: every **fixed** spacing value introduced by this task is a member of {4, 8, 12, 16, 20, 24, 32, 48}px
    - Behavior: the Gel Electrophoresis slot shows no striped placeholder and no `data-label` text once the video is in place
    - Behavior: the poster frame renders before the clip decodes on a throttled connection
  </acceptance_criteria>

  <done>
    The Gel Electrophoresis card plays Sirio's real OT-2 timelapse at under 2MB with a poster frame,
    from a committed relative path, in the composite's video column; the shipped `.media-slot` rules
    are untouched; the other two cards keep their honest placeholders; no vendor image was used.
  </done>
</task>

<task type="auto">
  <name>Task 2: The parts beside the robot — the blueprint plate</name>

  <files>site/assets/media/ot2-part-adapter.png, site/assets/media/ot2-part-pcr-strip-racks.png, site/assets/media/ot2-part-buffer-ladder-rack.png, site/assets/media/ot2-part-rack-holder.png, site/index.html, site/styles.css, site/assets/media/README.md</files>

  <read_first>
    - `site/assets/media/README.md` — the MEDIA-01 contract (naming, formats, first-party rule).
      ⚠ This task also **updates** it — see the action
    - `site/index.html` plate 002's figure, as built in Task 1
    - `site/styles.css` — `.media-slot--composite` as built in Task 1; `.media-slot`'s dashed-border /
      striped-placeholder idiom at `:900-918` (the visual language the blueprint plate should rhyme with)
    - `site/styles.css:568-590` — the accent map. Plate 002 lives in `#work`, so `--accent` here is
      `--star-antares` gold
    - `.planning/phases/02-the-story/02-CONTEXT.md` D-25, D-26, D-27, D-28, D-29
    - `.planning/phases/02-the-story/02-RESEARCH.md` § Assumptions Log (nothing here gates), § Open
      Questions 3 (D-27's shadows need eyes on a render)
    - `.planning/phases/02-the-story/02-PATTERNS.md` § `site/assets/media/*`
  </read_first>

  <action>
    Complete Sirio's composite (D-29 — **his own idea, unprompted**): the four CAD renders of the parts
    he designed, on a blueprint plate, beside the timelapse of the robot running them. **The card then
    tells "I designed the parts AND automated the run" in one glance.**

    **The renders — copy from `Opentrons-OT2 DALSA media/` into `site/assets/media/`, renamed per the
    README's convention (lowercase, hyphenated, project-slug-matching):**

    | Source | Destination | Aspect |
    |---|---|---|
    | `1 - Adapter.png` | `ot2-part-adapter.png` | 408×361 (1.13:1) |
    | `2 - PCR Strip Racks.png` | `ot2-part-pcr-strip-racks.png` | 569×338 (1.68:1) |
    | `3 - Rack for Loading Buffer & Molecular Weight Ladders.png` | `ot2-part-buffer-ladder-rack.png` | 338×269 (1.26:1) |
    | `4 - Rack Holder.png` | `ot2-part-rack-holder.png` | 438×295 (1.48:1) |

    ⚠ **D-25 — THE RENDERS HAVE GENUINE ALPHA. They are NOT white-background.** Claude initially
    claimed white backgrounds and **was wrong**; Sirio corrected it and verification confirmed him.
    The cause is worth remembering: **image viewers composite alpha onto white, so the two are
    indistinguishable when viewed. Parse the alpha channel; do not eyeball it.** Verified
    fully-transparent pixel share: Adapter **50.9%** · PCR Strip Racks **42.3%** · Buffer/Ladder Rack
    **32.2%** · Rack Holder **49.9%**; all four corners `a=0`. **No knock-out is needed — they drop
    onto the dark plate clean.** ⚠ **Keep them as `.png`** — the alpha is the whole point; do not
    convert to `.jpg`, which has none.

    ⛔ **The two genuinely opaque files are NOT selected and must not be used here** (D-26):
    `Opentrons Slots allocation.png` (99.8% opaque — only its rounded corners are cut out) and
    `UI protocol runner.png` (centre is `255,255,255,255`; also 371×888 portrait). Both are
    **deep-dive-page candidates for Phase 2.1**, and both would need real work first.

    **Markup — inside the composite figure, beside the `<video>`:**

    - A `.blueprint` container holding four `<img class="blueprint__part">`, each with a **relative**
      `src` and a real, specific `alt` naming the part (these are the parts Sirio designed; the alt
      text is the only place a screen reader learns that).
    - `loading="lazy"` is **not** added here — ⚠ **PERF-02 / lazy-loading is formally Phase 5's**, and
      the README says compression targets and lazy-loading are that phase's job. Do not pre-empt it.

    **CSS — the blueprint plate (D-28):**

    - `.blueprint` — **dark plate, faint grid, accent glow driven by the section's `--accent`**, so it
      reads as an engineering drawing lit from within — on-identity for the engineering-blueprint ×
      bio-glow language. Concretely: `border: 1px solid rgba(var(--accent), 0.28)`;
      `border-radius: 8px`; `background-color: rgba(var(--accent), 0.03)`; a two-axis
      `linear-gradient` grid at `rgba(var(--accent), 0.09)` with `background-size: 16px 16px`;
      `box-shadow: inset 0 0 60px rgba(var(--accent), 0.08)`. Plate 002 sits in `#work`, so `--accent`
      resolves to `--star-antares` gold — **the plate is section-driven and needs no colour of its
      own** (`styles.css:557`).
    - `.blueprint` layout — `display: grid; grid-template-columns: repeat(2, 1fr);` with a
      micro-tier gap and padding, giving a 2×2 of the four parts.
    - `.blueprint__part` — `width: 100%; height: 100%; **object-fit: contain**;` ⚠ **`contain`, not
      `cover`.** This is the entire reason the plate exists: the renders are 1.13:1 / 1.68:1 / 1.26:1 /
      1.48:1 and a 16:9 cover-crop would cut them. The plate lets them `contain` on a sized dark ground.
    - ⚠ **`.media-slot > img` (`styles.css:921`) does not match these** — the parts are children of
      `.blueprint`, not direct children of `.media-slot`. That is deliberate and it is why the shipped
      rule needs no edit. **Do not "simplify" by hoisting the imgs up a level.**
    - **Responsive:** below the plate's stacking breakpoint, the blueprint and video stack. The 2×2 may
      become 4×1 or stay 2×2 — decide on the render. ⚠ Phase 3 owns the real-device pass.

    ⚠ **D-27 — THE ONE THING RESEARCH CANNOT SETTLE. It needs eyes on a render.** The renders carry
    **baked-in soft shadows in LIGHT BLUE-GREY** (~`167,196,225` at ~24% alpha, sampled at
    `1 - Adapter.png`'s centre). Those shadows were authored for **light** backgrounds; on a dark
    ground they render as a faint **halo**, not a shadow. Sirio reports they looked fine on a dark
    presentation background, and the blueprint plate may well make the halo read as intentional glow —
    but it may also read as a dirty edge. **This is not resolvable by analysis. Render it and look**
    (human check below). If it reads dirty, the cheapest fixes in order: raise the plate's background
    lightness slightly so the halo blends; or add a subtle `filter` to the parts; **do not** attempt to
    strip the baked-in shadows — they are painted into the pixels.

    **Update `site/assets/media/README.md`** — it currently documents a single-asset drop-in contract
    and, once this ships, it will be describing a page that has one slot it does not explain. Add a
    short section recording the composite as a **named, deliberate exception**: what
    `.media-slot--composite` is, that it exists because a 9:16 clip and sub-2:1 renders cannot both
    survive a 16:9 `object-fit: cover`, and that **the plain drop-in contract still governs every other
    slot**. ⚠ **Do not weaken the README's first-party rule or its no-stock/no-AI-imagery rule** — this
    exception is about geometry, nothing else.
  </action>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && for f in ot2-part-adapter ot2-part-pcr-strip-racks ot2-part-buffer-ladder-rack ot2-part-rack-holder; do test -f "site/assets/media/$f.png" || { echo "MISSING $f"; exit 1; }; python3 -c "
import struct, zlib, sys
p = 'site/assets/media/$f.png'
d = open(p,'rb').read()
assert d[:8] == b'\x89PNG\r\n\x1a\n', p + ' is not a PNG'
w,h = struct.unpack('>II', d[16:24]); ct = d[25]
assert ct in (4,6), p + ' has no alpha channel (colour type ' + str(ct) + ') — D-25 says these have genuine alpha'
print(p, w, 'x', h, 'colour-type', ct, 'OK')
"; done && test "$(grep -c 'blueprint__part' site/index.html)" = "4" && test "$(grep -c 'class="blueprint"' site/index.html)" = "1" && test "$(grep -c 'alt=' site/index.html)" -ge "4" && ! grep -qE 'src="https?://' site/index.html && test "$(grep -ciE 'Opentrons Slots allocation|UI protocol runner|Opentrons-OT2\.png|RunOne' site/index.html)" = "0" && grep -q 'blueprint__part' site/styles.css && grep -A6 'blueprint__part' site/styles.css | grep -q 'object-fit: contain' && grep -q 'media-slot--composite' site/assets/media/README.md && grep -q 'No stock imagery and no AI-generated imagery' site/assets/media/README.md && echo GATES-PASS</automated>
    <human-check>⚠ **D-27 — this is the call research explicitly could not make, and it needs your eyes.** Look at the Gel Electrophoresis card. The four renders carry soft shadows baked in at ~`167,196,225`/24% — authored for a **light** background. On the dark blueprint plate they will render as a faint **halo** around each part. **Does that read as an on-theme glow, or as a dirty edge?** Sirio says they looked fine on a dark presentation background, so the odds are good — but this is a judgement call on a render, not an analysis. If it reads dirty, say so and the plate's background lightness gets nudged.

Then step back and read the whole card as a recruiter: does the timelapse-beside-the-parts composite say "I designed the parts **and** automated the run" in one glance? That is the entire reason it exists. Check the gold blueprint grid reads as an engineering drawing lit from within, not as a texture.</human-check>
  </verify>

  <acceptance_criteria>
    - CLI: all four `site/assets/media/ot2-part-*.png` files exist and each has PNG colour type `4` or `6` — i.e. **a real alpha channel** (D-25: verified transparent-pixel share 50.9% / 42.3% / 32.2% / 49.9%; parse the channel, do not eyeball it)
    - Source: none of the four files was converted to `.jpg` — the alpha is load-bearing on a dark ground
    - Source: `site/index.html` contains exactly one `class="blueprint"` and exactly four `blueprint__part` images, each with a **relative** `src` and a specific `alt` naming the part
    - Source: `grep -qE 'src="https?://' site/index.html` finds **no match**
    - Source: `grep -ciE 'Opentrons Slots allocation|UI protocol runner|Opentrons-OT2\.png|RunOne' site/index.html` returns `0` — neither the two opaque files (D-26) nor the two vendor renders (D-33) were used; all four are Phase 2.1 candidates
    - Source: `site/styles.css` `.blueprint__part` uses `object-fit: contain` — **not** `cover`. This is the reason the plate exists: the renders are 1.13:1 / 1.68:1 / 1.26:1 / 1.48:1 and a 16:9 cover-crop would cut them
    - Source: `site/styles.css` `.blueprint` derives every colour from `rgba(var(--accent), …)` and hard-codes no hue — it is section-driven (`styles.css:557`), resolving to `--star-antares` inside `#work`
    - Source: `site/styles.css` `.media-slot > img, .media-slot > video` at `:921-927` is still byte-identical to its Phase-1 state
    - Source: `site/assets/media/README.md` documents `.media-slot--composite` as a named deliberate exception, explains the aspect-ratio reason, and states that the plain drop-in contract still governs every other slot
    - Source: `site/assets/media/README.md` still contains the literal string `No stock imagery and no AI-generated imagery` — the first-party rule was not weakened
    - Source: every **fixed** spacing value introduced by this task is a member of {4, 8, 12, 16, 20, 24, 32, 48}px
    - Behavior: all four renders are fully visible on the plate — none is cropped at any viewport width
    - Human: D-27 resolved — the baked-in halo was looked at on a render and judged on-theme, or the plate's background was adjusted
  </acceptance_criteria>

  <done>
    The Gel Electrophoresis card carries Sirio's composite: his timelapse of the real OT-2 beside the
    four CAD renders of his own parts, uncropped on a gold blueprint plate that derives entirely from
    the section accent; the README documents the deviation without weakening its first-party rule; no
    opaque or vendor image was touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages CDN → visitor's browser | The only boundary that exists. Static, client-side-only, no server runtime, no auth, no data store, **no input fields**, no secrets in client code (`PROJECT.md:37`). |
| Repo → page (media assets) | **New this plan.** Six binary assets are committed and served from the same origin via relative paths. **No third-party media origin is introduced.** |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21 | Tampering | media `src` attributes | mitigate | **A remote hot-linked `src` hands a third party the ability to change what the portfolio shows** — `site/assets/media/README.md` states this rule explicitly. Mitigation: every asset is **committed to this repo** and referenced with a **relative path**. Enforced by the criterion `grep -qE 'src="https?://' site/index.html` finds no match. |
| T-02-22 | Denial of Service (self-inflicted) | first paint on cellular | mitigate | **The real risk in this plan.** The source clip is **6,656,975 bytes** and would autoplay on the home page. Recruiters open this on phones — a core project constraint. Mitigation (D-31 / Pitfall 9): re-encode to **< 2MB** with `-crf 28 -preset slow -an`, add `-movflags +faststart` so it streams progressively instead of needing the whole file before frame one, and pair it with a mandatory `poster`. Enforced by a hard byte-size gate and an ffprobe audio-stream check. ⚠ Ordering matters: the re-encode lands **before or with** the slot fill, never after. |
| T-02-23 | Tampering (legal/IP) | `Opentrons-OT2.png`, `RunOne Gel Electrophoresis System.png` | avoid | ⛔ **Both are the manufacturers' copyrighted marketing renders, not Sirio's work.** R-02 **fetched** `opentrons.com/trademarks` and found only a marks enumeration — **no permission grant, no editorial-use guideline, no press-kit licence**; Embi Tec publishes no media-resources page at all. **Silence is not permission**: absent an explicit grant, marketing renders are all rights reserved by default, and trademark notices govern *marks*, not *photographs* — **separate rights, separate analysis**. Attribution grants nothing. Practical risk on a personal portfolio is very low, but **low risk ≠ permitted**. Disposition: **avoid entirely.** Neither is needed — Sirio's own timelapse shows the real OT-2 he programmed, which is both permitted and better storytelling. Deferred to Phase 2.1. Enforced by a grep gate. |
| T-02-24 | Information Disclosure | the timelapse's frames | accept | The clip shows a real DTU lab bench. Frames were reviewed (D-30): the OT-2, Sirio's green printed parts, pink PCR strip racks, a blue gel tray. **No third-party faces, no personal data, no proprietary protocol text is visible**, and Sirio supplied it for exactly this purpose. Accepted. |
| T-02-25 | Tampering | `#work` plates 001 and 003 | avoid | The temptation under deadline is to fill the two empty slots with a stock photo or an AI-generated render. ⛔ **`site/assets/media/README.md`'s one non-negotiable rule forbids both, and it is right:** *"A portfolio that says 'I build things' and shows a stock photo of somebody else's pipette undoes itself."* D-32: **labelled placeholders stay.** Enforced by `grep -c 'media-slot' site/index.html` = 3. |
| T-02-26 | Tampering | dependency surface | mitigate | **Zero packages installed.** ffmpeg is an offline, build-time tool already present on the machine (`2023-06-19-git-1617d1a752-full_build`); it ships nothing to the client. **This plan adds no JavaScript at all.** `site/` still has no `package.json`. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | **Not applicable — this plan runs zero package-manager installs.** `02-RESEARCH.md` § Package Legitimacy Audit records zero packages phase-wide. ffmpeg is invoked as a local binary, not installed from a registry. |

**No `high`-severity threat is left unmitigated.** `security_block_on: high` is satisfied.

**Explicitly not applicable on this stack:** Spoofing, Repudiation, Elevation of Privilege, and ASVS
V2/V3/V4/V5/V6 — no auth, sessions, access control, input fields or cryptography exist.
</threat_model>

<verification>
Gates from `02-UI-SPEC.md` § Verification Gates reachable in this plan:

- **G-1 (JS disabled):** the composite is pure HTML/CSS — the video's `poster` and the renders show
  with no JS at all.
- **G-2 (greps clean):** unchanged — this plan adds no reveal CSS and no `body.js`.
- **G-6 (zero dependencies):** no `site/package.json`; one `<script>` tag; **no JS added**.

Plan-specific gates:
- **Byte gate:** `ot2-timelapse.mp4` < 2,097,152 bytes (D-31).
- **Alpha gate:** all four `ot2-part-*.png` have PNG colour type 4 or 6 (D-25 — parse the channel;
  Claude got this wrong by eyeballing and Sirio was right).
- **No-hot-link gate:** no `src="http…"` anywhere in `site/index.html`.
- **Vendor-image gate:** zero references to the two vendor renders or the two opaque files
  (D-26 / D-33 — both Phase 2.1).
- **Honest-placeholder gate:** `grep -c 'media-slot' site/index.html` = 3 (D-32).
</verification>

<success_criteria>
- The Gel Electrophoresis card shows the real OT-2 running in the real DTU lab with Sirio's own
  printed parts, beside CAD renders of those same parts — **the render → real-thing pairing is the
  strongest asset in the set**, and the composite is what makes it legible.
- The clip is under 2MB with a poster and `+faststart`: D-31 is honoured without building the Phase 4
  pipeline it explicitly forbids.
- D-29's MEDIA-01 deviation is **named, not smuggled** — in the plan, in the SUMMARY, and in the
  README the next contributor will actually read.
- D-25's alpha finding is verified by parsing the channel, not by looking at the picture.
- Nothing is faked, and no vendor image is used — the two things this plan could have got wrong the
  fastest.
</success_criteria>

<output>
Create `.planning/phases/02-the-story/02-05-SUMMARY.md` when done.

Record explicitly:
- **The MEDIA-01 deviation (D-29), named as deliberate and accepted**, with the aspect-ratio reason —
  not the background reason, which D-25 disproved.
- **The final encode settings and byte size**, and whether CRF 28 held or needed adjusting.
- **D-27's outcome** — did the baked-in blue-grey halo read as on-theme glow or as a dirty edge, and
  was anything adjusted? This was research Open Question 3 and could only ever be answered by looking.
- **That D-26's two opaque files and D-33's two vendor renders remain untouched and are Phase 2.1's**,
  with R-02's finding attached: neither vendor publishes an editorial-use licence, so Phase 2.1 should
  take D-33's option (b) Sirio's own line-art or (c) a frame from his own timelapse — or email both
  vendors for written permission.
- **A note for the orchestrator:** this plan partially advances **MEDIA-02**, which `REQUIREMENTS.md:126`
  traces to Phase 4. The traceability table needs updating, or MEDIA-02 needs splitting.
</output>
