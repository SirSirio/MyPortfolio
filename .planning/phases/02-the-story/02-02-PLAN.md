---
phase: 02-the-story
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - site/index.html
  - site/styles.css
autonomous: true
requirements: [CONT-01, NAV-01, CONT-04, MOTION-01]

must_haves:
  truths:
    - "A visitor scrolling past the Mission lands on a full-height ORIGIN section and reads how Sirio got from the lab to design and automation"
    - "ORIGIN narrates three beats — the lab years, the realisation that repetition is the bottleneck, and the pivot to CAD/printed parts/automation"
    - "ORIGIN owns its own light: a bio-green accent distinct from every other section"
    - "ORIGIN's beats assemble on scroll, paced slowly enough to read"
    - "The Contact section presents working email, LinkedIn, GitHub and iGEM wiki links"
    - "Every outbound link opens in a new tab and cannot reach back into this page"
    - "The word 'ego' and the phrase 'cum grano salis' appear nowhere on the page"
  artifacts:
    - path: "site/index.html"
      provides: "The #origin section and the extended #contact link set"
      contains: "id=\"origin\""
    - path: "site/styles.css"
      provides: "--star-zuben token, #origin accent entry, .section--full floor, narrative type scale"
      contains: "--star-zuben"
  key_links:
    - from: "site/index.html #origin"
      to: "site/styles.css #origin { --accent: var(--star-zuben) }"
      via: "the section accent map — omitting it renders the section's furniture transparent with no console error"
      pattern: "#origin\\s*\\{[^}]*--accent"
    - from: "site/index.html #origin beats"
      to: "site/main.js initReveals()"
      via: "data-reveal + data-reveal-kind=\"assemble\" consumed by the Plan 01 vocabulary"
      pattern: "data-reveal-kind=\"assemble\""
    - from: "site/index.html #contact outbound links"
      to: "external targets"
      via: "target=_blank rel=noopener — noopener is what blocks reverse tabnabbing"
      pattern: "rel=\"noopener\""
---

<objective>
Ship the narrative that turns "impressive" into "I want to talk to this person": the **ORIGIN**
section (CONT-01) narrating the biotech → design/automation pivot in Sirio's voice, and the
**Contact close** (CONT-04) that lets a recruiter act on it.

Implements D-01 (scroll order), D-02 (unnumbered interlude), D-03 (the bio-green stop),
D-04 (three beats), D-05/D-06/D-07 (threads in, threads out, the people framing),
D-08 (go deeper, don't echo Mission), D-09 (the one unbacked beat),
D-21/D-22/D-23/D-24 (the contact link set), NAV-01 (full-height as a floor).

Purpose: `PROJECT.md:24` locked Hero → Story/About → … → Contact at project level. Phase 1
shipped everything except the story. This is the half of the page that makes the work mean
something.

Output: `#origin` in `site/index.html` with its three beats; the `--star-zuben` token and
`#origin` accent entry; `.section--full`; the narrative type scale; three new outbound links
in `#contact`.
</objective>

<execution_context>
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/workflows/execute-plan.md
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-the-story/02-CONTEXT.md
@.planning/phases/02-the-story/02-RESEARCH.md
@.planning/phases/02-the-story/02-UI-SPEC.md
@.planning/phases/02-the-story/02-PATTERNS.md
@.planning/phases/02-the-story/02-01-SUMMARY.md
</context>

## Phase Goal

**As a** recruiter who has just been impressed by the hero, **I want to** read in 60 seconds how a
biotechnologist became someone who designs and automates lab hardware — and then find a working way
to contact him — **so that** I stop browsing and start writing an email.

## Artifacts this phase produces

Every symbol below is **created by Phase 2** and does not exist in the codebase today. The
source-grounding pass must treat these as new, not as drift.

**New files (whole phase):** `site/method.js` · `site/assets/media/ot2-timelapse.mp4` ·
`site/assets/media/ot2-timelapse-poster.jpg` · `site/assets/media/ot2-part-adapter.png` ·
`site/assets/media/ot2-part-pcr-strip-racks.png` · `site/assets/media/ot2-part-buffer-ladder-rack.png` ·
`site/assets/media/ot2-part-rack-holder.png`

**JS functions (whole phase):** `initReveals()` (plan 01, `site/main.js`) · `initMethod()` ·
`playCalc()` · `playTrack()` · `initArc()` (all `site/method.js`, plans 03/04)

**CSS custom properties (whole phase):** `--rev-stagger` · `--rev-i` (plan 01) ·
**`--star-zuben` (this plan)** · `--star-rigel` (plan 03) · `--len` (plan 04)

**HTML attributes (whole phase):** `data-rev-state` with values `hidden` | `shown` (plan 01) ·
`data-reveal-kind` with values `draw` | `assemble` | `snap` (plan 01) · `data-track` with values
`digital` | `physical` (plan 04) · `data-phases` (plan 04)

**New element ids (whole phase):** **`#origin` (this plan)** · `#method` · `#calc-ghost` ·
`#calc-out` · `#calc-keys` (plan 03) · `#loop-arc` · `#arc-path` · `#arc-head` · `#doc-rail` (plan 04)

**New CSS classes (whole phase):** **`.section--full` · `.section__label--interlude` · `.narr` ·
`.narr--land` · `.origin__beats` · `.contact__links` · `.contact__link` (this plan)** ·
`.method__act` · `.calc-wrap` · `.calc` · `.calc__screen` · `.calc__ghost` · `.calc__out` ·
`.calc__out--resolved` · `.keys` · `.key` · `.key--op` · `.key--press` · `.manifesto` (plan 03) ·
`.tracks` · `.track` · `.track--digital` · `.track--physical` · `.track__head` · `.track__kind` ·
`.track__for` · `.track__title` · `.track__foot` · `.phases` · `.phases--line` · `.phases--loop` ·
`.ph` · `.ph__dot` · `.ph__wire` · `.ph__n` · `.ph__name` · `.ph__desc` · `.ph__note` · `.ph__em` ·
`.loop-arc` · `.doc-rail` · `.doc-rail__tag` · `.doc-rail__txt` · `.evidence` · `.evidence__label` ·
`.ev-grid` · `.ev` · `.deck-link` (plan 04) · `.media-slot--composite` · `.blueprint` ·
`.blueprint__part` (plan 05)

<tasks>

<task type="auto">
  <name>Task 1: #origin ships — the bio-green interlude with the three-beat journey</name>

  <files>site/index.html, site/styles.css</files>

  <read_first>
    - `../00_Profile/profile.md:240-245` — **the only source for every factual claim in this task.**
      `:242` carries the five-years-hands-on line, the repetition-is-the-bottleneck passage and the
      domain-flexibility line verbatim; `:240` and `:245` carry the people/collaboration thread
    - `../00_Profile/profile.md:11`, `:195-204` — iGEM EndoSense / Wet Lab Lead
    - `.planning/PROJECT.md:46` — the "late nights fixing broken lab equipment" origin (⚠ D-09: this
      is the ONE beat with no `profile.md` backing)
    - `site/index.html:116-143` — the `#index` Mission section: the shell analog, the locked identity
      one-liner at `:131` (D-08 — do not echo or paraphrase it), and the "why" comment convention
    - `site/index.html:145-162` — the `#work` section head: the exact `.section` shell to copy, minus
      the numeral
    - `site/styles.css:12-40` — the `:root` token block (where `--star-zuben` lands)
    - `site/styles.css:155-170` — **the shipped `min-height: max(100vh, 620px)` / `max(100svh, 620px)`
      cascade. Reuse it verbatim; do not invent a second approach**
    - `site/styles.css:542-602` — the SECTION SYSTEM comment and the accent map (`:568-590`)
    - `site/styles.css:626-660` — `.section__inner`, `.section__head`, `.section__label`,
      `.section__rule`, `.section h2`
    - `site/star-engine.js:1-13` — `PAL` and `REDUCED`. **Read to confirm you must NOT touch this file**
    - `.planning/phases/02-the-story/02-UI-SPEC.md` § NAV-01 Full-Height Contract, § Section Anatomy
      (shared interlude shape + `#origin`), § Color Cl-2 / Cl-3, § Copywriting Cw-2 / Cw-3 / Cw-4
    - `.planning/phases/02-the-story/02-RESEARCH.md` § Content Sourcing Map, § Common Pitfalls 6 and 7
    - `.planning/phases/02-the-story/02-PATTERNS.md` § `site/index.html` — `#origin`, and
      § `site/styles.css` — tokens + accent map
    - `.planning/phases/02-the-story/02-01-SUMMARY.md` — the reveal vocabulary as actually built
  </read_first>

  <action>
    Insert a new `#origin` section into `site/index.html` **between the closing `</section>` of
    `#index` (currently `:143`) and the `01 SELECTED WORK` comment block (currently `:145`)** — D-01's
    locked order is Hero → Mission → **About** → Work → Experience → Publications → AI → Contact.

    **The section shell — copy `#work`'s shape (`index.html:154-162`) exactly, with these licensed
    changes:**

    - Wrapper: `<section id="origin" class="section section--full">`.
    - ⚠ **No `.section__numeral`.** The giant outlined sector numeral (`styles.css:593`) is what makes
      a section a *numbered sector*. D-02 ships About as an **unnumbered interlude** — quieter in
      hierarchy, deliberately not competing with the work. The existing `01 WORK / 02 EXPERIENCE /
      03 PUBLICATIONS / 04 TRANSMISSION` numbering is untouched, and the Mission pill index at
      `index.html:133-141` does **not** gain an entry (accepted consequence of D-02).
    - `.section__blob` with `data-para="0.14"` — free depth via the shipped `applyPara()`. Give it a
      `.section__blob--origin` modifier and position it opposite the Mission blob so the two do not stack.
    - `.section__head` with **no** `data-reveal` (plan 01 un-nested these — P-1), containing
      `<span data-reveal="0" class="section__label section__label--interlude">// ORIGIN</span>` and
      `<span data-reveal="0" data-reveal-kind="draw" class="section__rule"></span>`.
    - ⚠ **The label is `// ORIGIN` — the `//` prefix with no sector name.** Numbered sections carry
      `NN — NAME SECTOR`; the interludes carry a bare word. **The `//` is the interlude marker** and is
      how D-02's quieter hierarchy becomes visible in one character.
    - `<h2 data-reveal="0" data-reveal-kind="assemble"><span>How I got here</span></h2>`. The inner
      span is required by the `assemble` kind. The `h2` inherits the shared `.section h2` token
      unchanged — **do not add a size override** (UI-SPEC T-2: it is shared with Work, Experience and
      Publications).
    - A `.origin__beats` container holding **three** `<p class="narr">` beats, each its own reveal:
      `data-reveal="1"`, `"2"`, `"3"`, each with `data-reveal-kind="assemble"` and each wrapping its
      text in a `<span>` (the assemble frame animates its child). The third beat — the one that lands
      the argument — additionally carries `narr--land`.

    **The three beats (D-04) — one tight paragraph each. Write them from `profile.md`; do not port
    the design snapshots' copy, which is fabricated (Cw-4):**

    - **Beat 1 — the lab years.** Grounded in `profile.md:242`: *"After five years of hands-on lab
      work across mammalian cells, microbiology, fermentation, and cell-free systems"*. Include the
      iGEM Wet Lab Lead role (`profile.md:11`, `:195-204`; already on the page at `index.html:255`).
    - **Beat 2 — the realisation.** The spine of the whole section (D-05). Grounded verbatim in
      `profile.md:242`: *"What drains me is not physical work itself, but excessive repetition:
      running the same protocol dozens of times with limited creative input. What energizes me is the
      design layer"*. ⚠ **D-09:** the "countless late nights fixing broken lab equipment" strand comes
      from `PROJECT.md:46` **only** — a grep of `profile.md` finds no such claim. Sirio was asked and
      did not correct it, so treat it as true — but it is the **only** beat in this phase without
      profile grounding. **Do not embellish it further.** Everything else must trace to `profile.md`.
    - **Beat 3 — the pivot.** CAD, printed parts, automation, the thesis dispenser. Grounded in
      `profile.md:240`, `:378`; the dispenser is already on the page at `index.html:173-174`.

    **Threads to weave in (D-05):**
    - Repetition-is-the-bottleneck — the spine, in beat 2.
    - Domain-flexible / biotech-as-edge — `profile.md:242` verbatim: *"my deep understanding of
      laboratory needs … is a genuine competitive advantage"* and *"Product design is the constant;
      the application domain is flexible"*.
    - People/collaboration (D-07) — **max 1–2 sentences**, using this approved draft as the direction:
      *"I build things for people to use, and the best moment is always someone else using them. I
      work sharper with a team than alone — ideas survive better once someone has argued with them."*

    **Threads to leave out (D-06):** the martial-arts / Viet Vo Dao precision angle
    (`profile.md:247-248`) and the athletic awards. Sirio did not select them.

    ⚠ **THE BANNED WORD (D-07 / Cw-3) — this is the highest-probability content failure in this
    task.** The rule is "every fact from `profile.md`", and `profile.md` puts the trap **inside the
    exact passages you must draw from**: `:244` is literally headed *"Ego-Free Collaboration"`, `:245`
    says *"feedback is not about personal ego"*, `:392` says *"highly collaborative and ego-free"*.
    **The word "ego" must not appear on the page in any form.** Sirio rejected it as unprofessional
    and clichéd. **The decision wins over the source text.** This is a *phrasing* ban, not a *content*
    ban — the collaboration thread stays; only the word goes.

    ⚠ **D-08 — go deeper, do not restate.** The Mission section already carries the LOCKED verbatim
    identity one-liner at `index.html:131` (*"Biotechnology MSc at DTU Copenhagen. I design and build
    the hardware, software and automation that free scientists from repetitive lab work."*). ORIGIN
    must not paraphrase or echo it.

    ⚠ **D-16 — the bridge to METHOD is load-bearing.** ORIGIN must **end** on *repetition in the lab is
    the bottleneck → so I automate it*. Plan 02-03's METHOD section is the same insight one layer up
    (*repetition in thinking is a bottleneck too*). That structural bridge is what makes CONT-03 the
    second half of one argument rather than a bolt-on topic. `profile.md:375` already makes the lab↔AI
    bridge explicitly, so this is grounded, not invented. **Do not close beat 3 on a different note.**

    **Voice (Cw-2):** first person, plain, concrete. Sirio rejects corporate register and worries
    about anything a reader might not follow. Prefer the specific, checkable claim over the flattering
    abstraction.

    **Carry a "why" comment** above the section, in this file's convention (`index.html:116-123`,
    `:145-153`, `:210-225` — these comments are load-bearing; `:75-87` literally prevents a "fix" that
    would silently kill the hero star). Record: **D-09** (the late-nights beat has no `profile.md`
    backing) and **D-02** (unnumbered on purpose — not an omission, and the pill index deliberately
    does not link here).

    **CSS — `site/styles.css`:**

    - **Token.** Add `--star-zuben: 126, 240, 174;` (bio-green) to the `:root` stops at `:12-17`, as a
      bare `R,G,B` triplet so it composes inside `rgba(var(--x), a)`. Naming follows the shipped
      convention (sirius / antares / aldebaran / vega are all real stars) — **β Librae
      "Zubeneschamali"**, the one star widely reported as green. Green is the only hue absent from the
      spectrum, and it is **the bioluminescence this project is named for**.
    - ⚠ **Rewrite the stale comment at `styles.css:13` in place.** It currently says the stops *"mirror
      PAL in star-engine.js:4-10"* — `--star-zuben` deliberately will **not**, and that comment
      actively invites a tidy-minded contributor to "fix" the divergence by adding it to `PAL`. `PAL`
      feeds `tintAt()`, which colours **all 484 deep-field stars** (`main.js:91`), so that "fix"
      silently re-tints the whole page background and changes the approved hero composition (L-3 /
      Pitfall 7). Rewrite the comment to state that `PAL` is the **deep-field star spectrum** while
      these are the **section accent palette**, that they overlap by design coincidence rather than by
      contract (`--gold-glow 255,196,110` ≠ `--star-antares 244,182,89` already proves it), and that
      the new stops must never be added to `PAL`. **`site/star-engine.js` gets no edit in this phase.**
    - **Accent map entry.** Add `#origin { --accent: var(--star-zuben); }` to the map at `:568-590`,
      beside `#index`. ⚠ **This is a required task, not styling polish** (P-4 / Pitfall 6). Omit it and
      `var(--accent)` is unresolved → `rgba(, 0.4)` is an invalid declaration → the browser drops it →
      the label, rule, blob and every accent-derived surface render **transparent, with no console
      error**. The section looks half-built and nothing tells you why. Gate G-4 exists for exactly this.
    - **`.section--full`** near `.section` (`:562`), reusing the hero's cascade from `:164-165`
      **verbatim**: `min-height: max(100vh, 620px)` then `min-height: max(100svh, 620px)`, plus
      `display: flex; align-items: center;` and `.section--full > .section__inner { width: 100%; }`.
      Two declarations, not `@supports`: the second overrides where `svh` is supported and is ignored
      where it is not.
      ⚠ **`min-height` is a FLOOR, not a cage** — content that overruns simply grows. That is the
      property that makes this survive the Phase 3 mobile pass unchanged.
      ⚠ **`svh`, never `dvh`.** `dvh` resizes as the URL bar shows/hides, i.e. a layout reflow fires
      during the exact scroll gesture that triggers reveals, on top of a live canvas rAF loop. Most
      sites can afford `dvh`; a page with a live canvas loop cannot. ⛔ Do **not** use the
      `--vh: innerHeight * 0.01` JS hack — it needs a resize listener, i.e. more scroll-time work on
      the exact device it claims to help. ⛔ Do **not** add `scroll-snap` — `y mandatory` on a page with
      variable-height sections (Work is ~3 viewports) traps the reader mid-section.
      Apply `section--full` to `#origin` only in this task; `#method` gets it in plan 02-03. `#index`,
      `#publications` and `#contact` are UI-SPEC **OI-3**, deliberately deferred to a render — record
      them in the SUMMARY, do not silently drop them.
    - **Blob position:** add `.section__blob--origin` beside the other blob modifiers, positioned so it
      does not stack with `.section__blob--mission`.
  </action>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && test "$(grep -c 'id="origin"' site/index.html)" = "1" && test "$(grep -c 'section section--full' site/index.html)" -ge "1" && test "$(grep -c '// ORIGIN' site/index.html)" = "1" && test "$(grep -c 'section__numeral' site/index.html)" = "3" && test "$(grep -c -- '--star-zuben' site/styles.css)" -ge "2" && test "$(grep -cE '#origin[[:space:]]*\{' site/styles.css)" -ge "1" && grep -A2 -E '#origin[[:space:]]*\{' site/styles.css | grep -q -- '--accent: var(--star-zuben)' && test "$(grep -c '100svh' site/styles.css)" = "2" && test "$(grep -c 'dvh' site/styles.css)" = "0" && test "$(grep -c 'scroll-snap' site/styles.css)" = "0" && test "$(grep -ciE '\bego\b|grano|salis' site/index.html)" = "0" && git diff --quiet site/star-engine.js && test "$(grep -c '\[data-reveal\]' site/styles.css)" = "0" && echo GATES-PASS</automated>
    <human-check>Open the page and scroll from the hero. ORIGIN must appear between Mission and Selected Work, fill at least the viewport height, and be visibly **green** — its label, rule and blob all bio-green, clearly distinct from Mission's blue on one side and Work's gold on the other. Read the three beats end to end: do they sound like Sirio (first person, plain, concrete), and does the third land on "repetition is the bottleneck, so I automate it"? In DevTools console run `getComputedStyle(document.querySelector('#origin')).getPropertyValue('--accent')` — it must return a triplet, not empty (gate G-4).</human-check>
  </verify>

  <acceptance_criteria>
    - Source: `site/index.html` contains exactly one `id="origin"`, positioned after `#index`'s closing `</section>` and before the `01 SELECTED WORK` comment
    - Source: the `#origin` section carries `class="section section--full"`
    - Source: the `#origin` section contains **zero** `section__numeral` elements; `grep -c 'section__numeral' site/index.html` still returns `3` (unchanged — `#work`, `#experience`, `#publications` only)
    - Source: `site/index.html` contains the literal label text `// ORIGIN` exactly once, on an element carrying `section__label--interlude`
    - Source: `#origin` contains exactly three `<p class="narr"` beats, carrying `data-reveal="1"`, `"2"`, `"3"`, each with `data-reveal-kind="assemble"` and each wrapping its text in a single `<span>`
    - Source: `#origin`'s `h2` carries `data-reveal-kind="assemble"` and inherits `.section h2` with no size override
    - Source: `site/index.html` `#index` pills block (`:133-141`) is byte-identical to its pre-task state (D-02 — the index deliberately does not link to interludes)
    - Source: `site/styles.css` `:root` contains `--star-zuben: 126, 240, 174;`
    - Source: `site/styles.css` contains a `#origin` rule whose body includes `--accent: var(--star-zuben)` (gate G-4)
    - Source: `site/styles.css:13`'s comment no longer contains the string `mirror PAL` and instead states that the accent stops must never be added to `PAL`
    - Source: `site/styles.css` contains `.section--full` with **both** `min-height: max(100vh, 620px)` and `min-height: max(100svh, 620px)` in that order
    - Source: `site/styles.css` contains zero occurrences of `dvh` and zero occurrences of `scroll-snap`
    - CLI: `git diff --quiet site/star-engine.js` succeeds — the engine is untouched (L-3, `01-PATTERNS.md:354` "DO NOT REWRITE")
    - Source: `grep -ciE '\bego\b|grano|salis' site/index.html` returns `0` (gate G-8, D-07/D-13)
    - Source: every factual claim in the three beats traces to a cited `profile.md` line, recorded beat-by-beat in the SUMMARY; the only exception is the "late nights" strand, which is cited to `PROJECT.md:46` and flagged as D-09
    - Source: `site/styles.css` still contains **zero** occurrences of the bare bracketed reveal selector (gate G-2)
    - Behavior: `getComputedStyle(document.querySelector('#origin')).getPropertyValue('--accent')` returns a non-empty RGB triplet (gate G-4)
    - Behavior: `#origin` occupies at least the full viewport height at 1280×800 and grows (does not clip) when the viewport is 375×600
  </acceptance_criteria>

  <done>
    A visitor scrolling past the Mission reads a full-height, bio-green ORIGIN section narrating the
    lab years, the repetition realisation and the pivot — every fact traced to `profile.md` except the
    one D-09 beat — with no numeral, no "ego", and the accent resolving to a real triplet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make it readable — the narrative type scale and the slow narrative stagger</name>

  <files>site/styles.css</files>

  <read_first>
    - `site/styles.css:12-40` — the type tokens (`--font-display`, `--font-body`, `--font-mono`) and
      `--rev-stagger` as added by plan 01
    - `site/styles.css:638-660` — `.section__label` (`400 11px` mono, `0.26em`) and `.section h2`
    - `site/styles.css:695-710` — the Mission line's type, for the fluid-`clamp()` precedent
    - `site/styles.css:1005-1010`, `:1055-1060` — the shipped 16/17px card body copy, for contrast
    - `site/styles.css` — the reveal block as built by plan 01 (`--rev-stagger`, `--rev-i`)
    - `.planning/phases/02-the-story/02-UI-SPEC.md` § Typography T-1 / T-2 / T-3 (the full new-role
      table and the line-height contract), § Spacing S-1 / S-2 (the two-tier contract), § C-3 (the
      narrative stagger)
    - `.planning/phases/02-the-story/02-01-SUMMARY.md` — the stagger token as actually built
  </read_first>

  <action>
    The interludes are **read**, not scanned. The live site's 16–17px body copy is correct for *cards*
    and too small for prose at length — Sirio asked explicitly for bigger type across the narrative
    sections. This task adds that scale and slows the reveal pacing to match.

    **Type — add these new roles to `site/styles.css`, near the `#origin` section's block. Do not
    modify any shipped role** (UI-SPEC T-2 — `.section h2` in particular is shared by four sections):

    - `.narr` — **narrative body** (ORIGIN's three beats; plan 02-03's manifesto stanzas reuse it):
      `font: 300 clamp(20px, 1.7vw, 25px)/1.62 var(--font-body); color: var(--text-body);` with
      `max-width: 66ch`. That is ~+47% over card copy at the ceiling — prose read at length wants a
      longer measure and more leading.
    - `.narr--land` — **the narrative landing line** (the beat that carries the argument):
      `font-size: clamp(24px, 2.3vw, 33px); line-height: 1.45;` — tighter leading because these lines
      are short. Colour stays `--text-body`; ⚠ **accent is never on narrative prose** (Cl-3).
    - `.section__label--interlude` — `font-size: 13px` (up from the sector labels' 11px), keeping
      `var(--font-mono)`, `400`, `letter-spacing: 0.26em` and `color: rgba(var(--accent), 0.85)` from
      the base `.section__label` (`:638-643`). ⚠ The interludes are quieter in **hierarchy** (no
      numeral), not in **legibility**.
    - `.origin__beats` — vertical rhythm between the three beats. Use the **micro tier**: gaps come
      from the fixed 4-multiple scale (4, 8, 12, 16, 20, 24, 32, 48), not from free-form values.

    **Weights: {300, 400} only for all new work** (T-1). The live site uses 500 on exactly one element
    — `.section__numeral` (`:596`) — which the interludes do not have (D-02). So the new sections land
    on exactly two weights. ⚠ **The live site never uses 700. Do not introduce it.**

    **Line-height contract (T-3):** headings **1.2** · narrative prose **1.62** · landing lines
    **1.45** · supporting text **1.55** · labels **1**. Measure capped at **~66ch** for narrative prose.

    **Spacing — the two-tier contract (S-1), enforced for all new work:**
    - **Macro tier — fluid, deliberately not 4-multiples.** Section padding and inter-block rhythm use
      `clamp()` / viewport-relative values, matching the shipped `--pad-section: clamp(80px, 12vh,
      150px) clamp(20px, 6vw, 84px)` (`:37`). `12vh` cannot resolve to a multiple of 4 — that is the
      point of it. A fixed 64px gap that is generous at 1440px is claustrophobic at 2560px and enormous
      at 375px; `clamp()` with explicit bounds is a *constraint*, not an absence of one.
    - **Micro tier — fixed 4-multiples, enforced.** Gaps, element padding, label gaps — anything the
      viewport should not move — come from {4, 8, 12, 16, 20, 24, 32, 48}. ⚠ The sketch used `26px`,
      `22px`, `10px`, `6px` freely; **normalise every new fixed value to the 4-multiple scale.**
    - ⚠ `.section h2`'s shipped `margin: 14px 0 0` (`:656`) is **not** a 4-multiple. It is **inherited
      and out of scope — do not "fix" it.** The micro-tier rule binds new work only.

    **The narrative stagger (C-3) — the phase's hardest-won lesson.** Sirio **could not read** the
    sketch's first cut; timings had to be roughly tripled (~7s → ~21s). Treat every number as a floor
    and expect him to want them slower still, not faster.
    - Add `#origin, #method { --rev-stagger: 220ms; }` beside the accent map. The `:root` default of
      `130ms` (plan 01) stays as the **furniture** interval; narrative beats are **read**, not scanned,
      so they get 220ms. Custom properties inherit, so every `[data-rev-state]` element inside the two
      interludes picks it up with no JS change and no per-section rule.
    - ⚠ **Stagger stays a token, never a JS constant.** JS writes only the unitless index (`--rev-i`);
      CSS owns the interval. Re-pacing the whole page must remain a one-line change — which, given the
      lesson above, is the entire point.
  </action>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && test "$(grep -c '\.narr' site/styles.css)" -ge "2" && grep -q 'clamp(20px, 1.7vw, 25px)' site/styles.css && grep -q 'clamp(24px, 2.3vw, 33px)' site/styles.css && grep -q 'section__label--interlude' site/styles.css && grep -qE '#origin,\s*#method\s*\{' site/styles.css && grep -A2 -E '#origin,\s*#method\s*\{' site/styles.css | grep -q -- '--rev-stagger: 220ms' && test "$(grep -cE 'font(-weight)?:\s*(500|600|700)' site/styles.css)" -le "1" && test "$(grep -c '\[data-reveal\]' site/styles.css)" = "0" && echo GATES-PASS</automated>
    <human-check>Read ORIGIN's three beats at 1280px and again at 375px. The prose must be comfortably readable at both — noticeably larger than the Work cards' body copy — and the line length must not exceed ~66 characters at desktop width. Scroll ORIGIN into view and time the beats: each should arrive ~220ms after the previous, slow enough that you finish reading one before the next lands. If it still feels fast, the fix is one number: `--rev-stagger` on `#origin, #method`.</human-check>
  </verify>

  <acceptance_criteria>
    - Source: `site/styles.css` contains a `.narr` rule with `clamp(20px, 1.7vw, 25px)`, `line-height` `1.62`, `font-weight` `300` and `max-width: 66ch`
    - Source: `site/styles.css` contains a `.narr--land` rule with `clamp(24px, 2.3vw, 33px)` and `line-height: 1.45`
    - Source: `site/styles.css` contains `.section__label--interlude` with `font-size: 13px`
    - Source: `site/styles.css` contains `#origin, #method { --rev-stagger: 220ms; }` (or an equivalent grouped rule setting that value on both ids)
    - Source: `site/styles.css` `:root` still contains `--rev-stagger: 130ms` — the furniture default is unchanged
    - Source: `site/styles.css` `.section h2` rule at `:655-660` is byte-identical to its pre-task state (T-2 — shared token)
    - Source: `site/styles.css` `.section__label` rule at `:638-643` is byte-identical to its pre-task state — the interlude size is a modifier, not an edit
    - Source: every font-weight introduced by this task is `300` or `400`; `grep -cE 'font(-weight)?:\s*(500|600|700)' site/styles.css` returns at most `1` (the pre-existing `.section__numeral` at `:596`)
    - Source: every **fixed** (non-`clamp`, non-viewport-relative) spacing value introduced by this task is a member of {4, 8, 12, 16, 20, 24, 32, 48}px
    - Source: `site/main.js` contains no millisecond literal for reveal timing — the stagger is CSS-only
    - Source: `site/styles.css` still contains **zero** occurrences of the bare bracketed reveal selector (gate G-2)
    - Behavior: ORIGIN's three beats reveal with a ~220ms interval between them, measurably slower than `#work`'s plates at 130ms
  </acceptance_criteria>

  <done>
    ORIGIN's prose reads at the larger narrative scale Sirio asked for, capped at ~66ch, on two
    weights and the 4-multiple micro scale; its beats arrive at 220ms intervals while the rest of the
    page keeps the 130ms furniture default; no shipped type token was modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: The Contact close — email, LinkedIn, GitHub, iGEM wiki</name>

  <files>site/index.html, site/styles.css</files>

  <read_first>
    - `site/index.html:296-309` — **the existing `#contact` section. This task EXTENDS it; it does not
      rebuild it** (D-24)
    - `site/index.html:28-35` — the `.nav__link` outbound pattern and the comment at `:28-29` stating
      **why** `noopener` is there
    - `site/index.html:204` — `.plate__link` (the iGEM wiki link already on the page, same pattern)
    - `site/styles.css:1092-1131` — `.section__label--contact`, `.section h2.contact__title`,
      `.contact__email` (including the comment at `:1124-1125` explaining why colour is restated)
    - `../00_Profile/profile.md:11` — the iGEM wiki as *"a public, verifiable record"*
    - `../00_Profile/profile.md:40-44` — contact details
    - `.planning/phases/02-the-story/02-UI-SPEC.md` § `#contact` — the close, § Copywriting Cw-1,
      § Registry Safety (the `rel="noopener"` control)
    - `.planning/phases/02-the-story/02-PATTERNS.md` § `site/index.html` — `#contact` links
  </read_first>

  <action>
    Give a recruiter who has read the story somewhere to go (CONT-04). **Extend the existing
    `#contact` section; do not rebuild it** (D-24) — keep the `Let's build something.` title
    (`index.html:304`) and the `04 — TRANSMISSION · SIRIUS SECTOR` label (`:303`) exactly as they are.

    **Markup:** add a `.contact__links` row as a **new sibling** of the existing
    `<div data-reveal="2">` that wraps the email (`:305-307`), carrying `data-reveal="3"` so it arrives
    after the email. Put three `.contact__link` anchors in it. Do **not** nest a reveal inside the
    email's div (P-1 — reveals must be siblings).

    | Link | `href` | Label |
    |---|---|---|
    | LinkedIn | `https://www.linkedin.com/in/sirio-vittorio-feltrin/` | `LinkedIn&nbsp;&#8599;` |
    | GitHub | `https://github.com/SirSirio` | `GitHub&nbsp;&#8599;` |
    | iGEM wiki | `https://2024.igem.wiki/DTU-Denmark/` | `iGEM wiki&nbsp;&#8599;` |

    - **Email stays exactly as shipped** at `index.html:306` — `mailto:sirio.feltrin@gmail.com`. It is
      the primary CTA and the section title is its verb. ⚠ This is the **profile** address and
      deliberately **not** the account address (`airsiriomax@gmail.com`). Do not change it.
    - **LinkedIn is repeated from the nav on purpose** (D-21) — it is where a recruiter looks *once
      they have decided to act*. This is not a duplication bug.
    - ⛔ **No CV link** (D-22). Not selected. It stays in the persistent nav only. Do not add one.

    **Three conventions the shipped `.nav__link` at `index.html:33-34` encodes — all three are audit
    gates:**
    1. ⚠ **`target="_blank" rel="noopener"` on every outbound link.** `index.html:29` states why:
       **`noopener` is what blocks reverse tabnabbing.** Modern browsers imply it for `target=_blank`,
       but the explicit attribute is the shipped pattern **and the grep target** (gate G-7). This is the
       only live security control this phase touches.
    2. **`&#8599;` (the numeric entity), not a literal ↗ glyph.** The whole file uses numeric entities
       for arrows (`&#8599;` ↗, `&#8600;` ↘ at `:134-137`). Match it.
    3. **`&nbsp;` before the arrow** so the glyph never orphans onto its own line.

    **CSS:** add `.contact__links` (a flex row, gap from the 4-multiple micro scale, `flex-wrap: wrap`
    for narrow viewports) and `.contact__link` near `.contact__email` (`styles.css:1110`). Style the
    links as **secondary** to the email: derive from `rgba(var(--accent), …)` at a lower alpha than
    `.contact__email`'s `0.95`, mono font, `400`, no border-bottom. ⚠ **Restate `color` on the
    `:hover` / `:focus-visible` state** — the comment at `styles.css:1124-1125` records that the global
    `a:hover` rule outranks these on specificity and would otherwise repaint them. Include
    `:focus-visible` alongside `:hover`, matching `.contact__email:hover, .contact__email:focus-visible`
    at `:1126-1127`.

    ⚠ **D-23 — pre-launch human gate, not a code task.** `github.com/SirSirio` must be looked at before
    this ships: a sparse or empty-looking profile **undercuts** rather than supports the "I build
    things" claim. This is reputational, not security. It is raised as a `<human-check>` below and must
    be carried into the phase's end-of-phase verification.
  </action>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && test "$(grep -c 'target="_blank"' site/index.html)" = "$(grep -c 'rel="noopener"' site/index.html)" && test "$(grep -c 'target="_blank"' site/index.html)" = "7" && test "$(grep -c 'https://github.com/SirSirio' site/index.html)" = "1" && test "$(grep -c 'https://2024.igem.wiki/DTU-Denmark/' site/index.html)" = "2" && test "$(grep -c 'linkedin.com/in/sirio-vittorio-feltrin' site/index.html)" = "2" && test "$(grep -c 'mailto:sirio.feltrin@gmail.com' site/index.html)" = "1" && test "$(grep -c 'airsiriomax' site/index.html)" = "0" && test "$(grep -c 'canva.link' site/index.html)" = "1" && grep -q "Let's build something." site/index.html && grep -q '04 — TRANSMISSION · SIRIUS SECTOR' site/index.html && test "$(grep -c 'contact__link"' site/index.html)" = "3" && for u in https://github.com/SirSirio https://2024.igem.wiki/DTU-Denmark/ https://www.linkedin.com/in/sirio-vittorio-feltrin/; do code=$(curl -s -o /dev/null -w '%{http_code}' -L -A 'Mozilla/5.0' "$u"); echo "$u -> $code"; done && echo GATES-PASS</automated>
    <human-check>⚠ **D-23 — this one is Sirio's, and it is a pre-launch gate.** Open `https://github.com/SirSirio` as a stranger would. A sparse or empty-looking profile actively undercuts the "I build things" claim the whole page makes — it is worse than no link. Decide: pin some repos, or drop the GitHub link. Then click LinkedIn, GitHub and iGEM wiki from the live Contact section: each must open in a new tab and land on the right page. Check the row reads as *secondary* to the email — the email must still be the obvious primary CTA.</human-check>
  </verify>

  <acceptance_criteria>
    - Source: `site/index.html` `target="_blank"` count equals `rel="noopener"` count, and both are `7` (4 pre-existing + 3 new) (gate G-7)
    - Source: `#contact` contains exactly three `.contact__link` anchors — LinkedIn, GitHub, iGEM wiki — in a `.contact__links` container carrying `data-reveal="3"`
    - Source: the `.contact__links` container is a **sibling** of the existing `<div data-reveal="2">`, not nested inside it (P-1)
    - Source: `site/index.html` contains `mailto:sirio.feltrin@gmail.com` exactly once, unchanged from its pre-task state at `:306`
    - Source: `site/index.html` contains zero occurrences of `airsiriomax` (the account address must never reach the page)
    - Source: `site/index.html` contains `canva.link` exactly once — in the nav only. **No CV link was added to `#contact`** (D-22)
    - Source: `site/index.html` still contains the literal strings `Let's build something.` and `04 — TRANSMISSION · SIRIUS SECTOR` (D-24 — extend, do not rebuild)
    - Source: each new anchor's label uses the numeric entity `&#8599;` preceded by `&nbsp;` — no literal ↗ glyph
    - CLI: `curl -s -o /dev/null -w '%{http_code}' -L` returns `200` for `https://github.com/SirSirio` and `https://2024.igem.wiki/DTU-Denmark/`. LinkedIn may return `999`/`403` to non-browser agents — that is LinkedIn's bot defence, not a broken link; verify it by hand instead
    - Source: `site/styles.css` `.contact__link:hover` restates `color` explicitly and is paired with `:focus-visible`
    - Behavior: clicking each of the three new links opens a new tab and the opened page cannot reach `window.opener`
    - Human: `github.com/SirSirio` has been reviewed and either strengthened or the link removed (D-23)
  </acceptance_criteria>

  <done>
    The Contact section keeps its shipped title, label and email, and gains three working outbound
    links — LinkedIn, GitHub, iGEM wiki — each with `target="_blank" rel="noopener"`, each styled as
    secondary to the email, with no CV link and no account address anywhere on the page.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages CDN → visitor's browser | The only boundary that exists. Static, client-side-only, no server runtime, no auth, no sessions, no data store, **no input fields**, no secrets in client code (`PROJECT.md:37`). |
| Page → third-party sites (LinkedIn, GitHub, iGEM wiki) | **New this plan.** Three outbound `target="_blank"` links leave the origin. |
| Google Fonts → page | Third-party subresource, shipped in Phase 1 (`index.html:9-12`, `crossorigin`). Untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | the 3 new `target="_blank"` anchors in `#contact` | mitigate | **Reverse tabnabbing** — without `rel="noopener"` the opened page can reach `window.opener` and navigate this tab to a look-alike. Mitigation: `rel="noopener"` on **every** new anchor, matching `.nav__link` (`index.html:33-34`). Enforced by the acceptance criterion that `target="_blank"` count == `rel="noopener"` count == 7 (gate G-7). Modern browsers imply `noopener`, but the explicit attribute is the shipped pattern and the audit gate. |
| T-02-06 | Tampering | `#origin` and `#contact` markup | accept | All content is hand-authored static HTML written by the executor from `profile.md`. **No `innerHTML`, no template interpolation, no user input, no fetched content** — there is no injection sink to reach. ASVS V5 (Input Validation) has no target in this plan. |
| T-02-07 | Information Disclosure | `sirio.feltrin@gmail.com` in plaintext at `index.html:306` | accept | **Deliberately not mitigated.** Locked by D-21/D-24; it is the *profile* address, not the account address. Email harvesting is the accepted cost — a portfolio that hides its email defeats its own purpose. Verified separately: `grep -c 'airsiriomax' site/index.html` = 0 ensures the *account* address never leaks. |
| T-02-08 | Tampering (reputational) | `https://github.com/SirSirio` as a linked destination | transfer | Sirio controls the target. **D-23:** a sparse profile undercuts the "I build things" claim the page makes — reputational rather than security, but it is a genuine pre-launch gate. Transferred to the human check in Task 3 and carried to end-of-phase verification. |
| T-02-09 | Tampering | dependency surface | mitigate | **Zero packages installed.** `02-RESEARCH.md` § Package Legitimacy Audit records zero packages phase-wide; `site/` has no `package.json` and no bundler. Enforced by plan 01's `test ! -f site/package.json` criterion, still true here. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | **Not applicable — this plan runs zero package-manager installs.** If any task is later amended to add a dependency it contradicts D-35 and must be **rejected, not audited**. |

**No `high`-severity threat is left unmitigated.** `security_block_on: high` is satisfied.

**Explicitly not applicable on this stack:** Spoofing (no identity), Repudiation (no actions to
log), Elevation of Privilege (no privilege levels), ASVS V2/V3/V4/V6 (no auth, sessions, access
control or cryptography exist).
</threat_model>

<verification>
Gates from `02-UI-SPEC.md` § Verification Gates reachable in this plan:

- **G-1 (JS disabled):** every word of `#origin` and `#contact` readable, no blank sections.
- **G-2 (`styles.css` greps clean):** `grep -c '\[data-reveal\]' site/styles.css` → `0`;
  `grep -rn 'body\.js' site/` → zero hits.
- **G-3 (reduced motion):** `#origin` fully readable and structurally complete, zero animation.
- **G-4 (accent resolves):** `getComputedStyle(document.querySelector('#origin')).getPropertyValue('--accent')`
  returns a triplet.
- **G-6 (zero dependencies):** no `site/package.json`; one `<script>` tag.
- **G-7 (`rel="noopener"`):** present on all 3 new outbound links; count parity holds at 7/7.
- **G-8 (no banned phrasing):** `grep -ciE '\bego\b|grano|salis' site/index.html` → `0`.
- **G-9 (`github.com/SirSirio` reviewed):** raised as a `<human-check>`; `workflow.human_verify_mode`
  is `end-of-phase`, so it is collected there rather than blocking mid-plan.

Not reachable in this plan: G-5 (`main.js` untouched here), G-10 (METHOD's skills line — plan 02-04).
</verification>

<success_criteria>
- CONT-01 is satisfied: a visitor reads the biotech → design/automation pivot as three beats, in
  Sirio's voice, every fact traced to `profile.md` except the single D-09 beat.
- CONT-04 is satisfied: email + LinkedIn + GitHub + iGEM wiki, all working, all `rel="noopener"`.
- NAV-01 is advanced: `.section--full` exists as a **floor** reusing the hero's shipped cascade, and
  `#origin` uses it. `#index` / `#publications` / `#contact` floors are recorded as OI-3, not dropped.
- MOTION-01 extends to the new content: the beats assemble at 220ms, the rule draws.
- The two source-vs-decision conflicts are resolved the way the decisions demand: no "ego" on the
  page despite `profile.md:244`, and the accent stops diverge from `PAL` with the comment rewritten
  to defend the divergence.
- `site/star-engine.js` is untouched.
</success_criteria>

<output>
Create `.planning/phases/02-the-story/02-02-SUMMARY.md` when done.

Record explicitly:
- **A beat-by-beat content provenance table** — every claim in `#origin` mapped to its `profile.md`
  line, with the D-09 "late nights" strand flagged as `PROJECT.md:46`-only. This is the artefact the
  next reviewer needs; the standing landmine is fabrication and it has already bitten twice.
- The exact `--star-zuben` value shipped, and that Sirio has **not yet seen it on a render** (OI-1).
- **OI-3 still open:** whether `#index`, `#publications` and `#contact` get `section--full`. One class
  each; decide on the rendered page.
- **OI-4 still open:** the `#origin` h2 (`How I got here`) is a react-to proposal, not final.
- D-23's outcome, if Sirio acted on it during this plan.
</output>
