---
phase: 02-the-story
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - site/index.html
  - site/styles.css
  - site/method.js
autonomous: true
requirements: [CONT-03, MOTION-01]

must_haves:
  truths:
    - "Below the manifesto a visitor sees two tracks — DIGITAL and PHYSICAL — that read as different kinds of work at a glance"
    - "The DIGITAL track is a LINE: Discuss -> Plan -> Execute, wires drawing themselves left to right, running straight through and shipping"
    - "The PHYSICAL track is a LOOP: Discuss & Design -> Build & Iterate -> Test -> Analyze Data, with a return arc that draws itself back to the start"
    - "DOCUMENT reads as a rail spanning beneath the loop with its ticks aligned to Test and Analyze Data — not as a step in the chain"
    - "Each track lights on its own scroll position rather than both firing together off-screen"
    - "An understated evidence strip late in the section owns that this same method built the page, and links out to the deck and the GSD repo"
    - "Under prefers-reduced-motion every track is fully lit and every word present — only the pacing is lost"
  artifacts:
    - path: "site/index.html"
      provides: "The two tracks, the return arc, the DOCUMENT rail, the IN PRACTICE evidence strip"
      contains: "data-track=\"physical\""
    - path: "site/method.js"
      provides: "playTrack() and initArc() — per-track choreography and arc measurement"
      contains: "function playTrack("
    - path: "site/styles.css"
      provides: "Track, phase, arc and doc-rail CSS including the sanctioned .track--physical accent override"
      contains: ".track--physical"
  key_links:
    - from: "site/method.js playTrack()"
      to: "site/styles.css [data-ph-state] rules"
      via: "JS-created state attribute at identical specificity — never a body.js specificity race"
      pattern: "data-ph-state|dataset\\.phState"
    - from: "site/method.js initArc()"
      to: "site/styles.css .loop-arc path"
      via: "--len custom property set from getTotalLength() once at init"
      pattern: "getTotalLength"
    - from: "site/index.html evidence strip"
      to: "the GSD deck and the GSD repo"
      via: "target=_blank rel=noopener"
      pattern: "rel=\"noopener\""
---

<objective>
Ship the second act of **METHOD** (CONT-03): Sirio's own distinction — **DIGITAL is a LINE,
PHYSICAL is a LOOP** — and the understated evidence strip that owns the site itself as proof.

**The structural contrast IS the argument.** A line that ships versus a loop that comes back
around is the difference; two lists of text would not be. This is Sirio's insight, supplied in
sketch round 2, and it is what closes D-17's four mandated contents without a single invented claim.

Implements D-17 (the four mandated contents), D-18 (link the deck), D-19 (the same method built
this page), D-20 (closed by sketch 001 variant D), MOTION-01's draw-on leg extended into the
section's own geometry.

Purpose: D-17 requires **AI-for-documentation**, **the GSD Discuss→Plan→Execute framework**,
**AI pressure-tests the design**, and **he decides the direction; AI turns it into reality**.
Variant D maps each onto a structural element rather than a bullet: the rail, the line, the Test
node, and the calculator's landing line plus this evidence strip.

Output: the two tracks, the return arc and the DOCUMENT rail in `site/index.html`; `playTrack()`
and `initArc()` in `site/method.js`; track/phase/arc/rail CSS in `site/styles.css`.
</objective>

<execution_context>
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/workflows/execute-plan.md
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-the-story/02-CONTEXT.md
@.planning/phases/02-the-story/02-RESEARCH.md
@.planning/phases/02-the-story/02-UI-SPEC.md
@.planning/phases/02-the-story/02-PATTERNS.md
@.planning/sketches/001-method-section/README.md
@.planning/phases/02-the-story/02-03-SUMMARY.md
</context>

## Phase Goal

**As a** recruiter deciding whether Sirio's "I build with AI" claim is real, **I want to** see the
two shapes of his work — a line that ships and a loop that answers back — and then find a link that
lets me check it **so that** the claim reads as method rather than as a buzzword.

## Artifacts this phase produces

Every symbol below is **created by Phase 2** and does not exist in the codebase today. The
source-grounding pass must treat these as new, not as drift.

**New files (whole phase):** `site/method.js` (plan 03) · `site/assets/media/ot2-timelapse.mp4` ·
`site/assets/media/ot2-timelapse-poster.jpg` · `site/assets/media/ot2-part-adapter.png` ·
`site/assets/media/ot2-part-pcr-strip-racks.png` · `site/assets/media/ot2-part-buffer-ladder-rack.png` ·
`site/assets/media/ot2-part-rack-holder.png` (plan 05)

**JS functions (whole phase):** `initReveals()` (plan 01, `site/main.js`) · `initMethod()` ·
`playCalc()` (plan 03, `site/method.js`) · **`playTrack()` · `initArc()` (this plan, `site/method.js`)**

**JS constants (whole phase):** `D` — the locked calculator timing object (plan 03) ·
**`T` — the track choreography timings (this plan)**

**CSS custom properties (whole phase):** `--rev-stagger` · `--rev-i` (plan 01) · `--star-zuben`
(plan 02) · `--star-rigel` (plan 03) · **`--len` — the arc path length (this plan)**

**HTML attributes (whole phase):** `data-rev-state` with values `hidden` | `shown` (plan 01) ·
`data-reveal-kind` with values `draw` | `assemble` | `snap` (plan 01) · **`data-track` with values
`digital` | `physical` · `data-phases` · `data-ph-state` with values `armed` | `lit` ·
`data-arc-state` with values `armed` | `lit` · `data-rail-state` with values `armed` | `lit`
(this plan)**

**New element ids (whole phase):** `#origin` (plan 02) · `#method` · `#calc-ghost` · `#calc-out` ·
`#calc-keys` (plan 03) · **`#loop-arc` · `#arc-path` · `#arc-head` · `#doc-rail` (this plan)**

**New CSS classes (whole phase):** `.section--full` · `.section__label--interlude` · `.narr` ·
`.narr--land` · `.origin__beats` · `.contact__links` · `.contact__link` (plan 02) · `.method__act` ·
`.calc-wrap` · `.calc` · `.calc__screen` · `.calc__ghost` · `.calc__out` · `.calc__out--resolved` ·
`.keys` · `.key` · `.key--op` · `.key--press` · `.manifesto` · `.ph__em` (plan 03) ·
**`.tracks` · `.track` · `.track--digital` · `.track--physical` · `.track__head` · `.track__kind` ·
`.track__for` · `.track__title` · `.track__foot` · `.phases` · `.phases--line` · `.phases--loop` ·
`.ph` · `.ph__dot` · `.ph__wire` · `.ph__n` · `.ph__name` · `.ph__desc` · `.ph__note` ·
`.loop-arc` · `.doc-rail` · `.doc-rail__tag` · `.doc-rail__txt` · `.evidence` · `.evidence__label` ·
`.ev-grid` · `.ev` · `.deck-link` (this plan)** · `.media-slot--composite` · `.blueprint` ·
`.blueprint__part` (plan 05)

<tasks>

<task type="auto">
  <name>Task 1: DIGITAL is a LINE — the GSD track, drawing itself and shipping</name>

  <files>site/index.html, site/styles.css, site/method.js</files>

  <read_first>
    - `.planning/sketches/001-method-section/index.html:664-703` — **variant D's DIGITAL track markup,
      LOCKED.** `.tracks`, `.track--digital`, `.track__head`, `.phases--line`, `.ph`, `.track__foot`
    - `.planning/sketches/001-method-section/index.html:315-381` — the track and phase CSS, including
      the ⛔ `body.js` scoping (`:355`, `:369`, `:374`) that must **not** be ported, and the `700`
      weight on `.track__kind` (`:317`) that must be pulled back to `400`
    - `.planning/sketches/001-method-section/index.html:1036-1052` — `playTrack()`, and `:1087-1097` —
      the track IntersectionObserver
    - `.planning/sketches/001-method-section/index.html:422-436` — the `max-width: 860px` vertical
      pipeline media query
    - `.planning/sketches/001-method-section/README.md` § "Variant D copy — provenance, line by line"
      — **which strings are deck-verbatim and which are Claude's framing**
    - `site/method.js` as built in plan 03 — the module header, the `D` object, the play-once guard,
      the `REDUCED` import, `initMethod()`
    - `site/styles.css` — the reveal vocabulary (plan 01), `.calc*` and `.manifesto` (plan 03), the
      accent map, the surface tokens
    - `site/index.html:33-34` — the shipped outbound-link pattern
    - `.planning/phases/02-the-story/02-UI-SPEC.md` § `#method` (the track table), § C-1 (the arming
      contract applies to METHOD's internals too), § C-2, § C-3, § C-4, § C-5, § Typography T-1 / T-3,
      § Spacing S-1, § Color Cl-2 / Cl-3
    - `.planning/phases/02-the-story/02-03-SUMMARY.md` — the module-placement decision and the BEM
      conversion convention
  </read_first>

  <action>
    Add Act 2 to `#method` as a `.method__act--tracks` child of the existing `#method` section — ⚠ **one
    section, not two.** The sketch's second `<section data-sec="d2">` existed only because it was a
    sketch; production keeps one accent-map entry and one floor.

    **Markup — port `sketch:667-703`'s DIGITAL track:**

    - `.tracks` grid → `.track.track--digital` with `data-track="digital"`.
    - `.track__head` containing `<span class="track__kind">DIGITAL</span>` and
      `<span class="track__for">websites, apps, tooling — like this page</span>`.
    - `<h3 class="track__title">Spec-driven development, run by GSD</h3>` — **deck-verbatim**
      (slide 7, fetched and quoted during the sketch session).
    - `.phases.phases--line` with `data-phases`, containing three `.ph` blocks. Each `.ph` holds
      `.ph__dot`, `.ph__wire`, `.ph__n`, `.ph__name`, `.ph__desc`:
      | # | `.ph__name` | `.ph__desc` |
      |---|---|---|
      | 01 | Discuss | Lock decisions before planning. |
      | 02 | Plan | Research first, then atomic tasks. |
      | 03 | Execute | Parallel waves, atomic commits. |
    - `.track__foot` — the sketch's paragraph, with **"straight through and ships"** emphasised via a
      `<b>` (the sketch's own markup; `.track__foot b` styles it to `400` weight in accent, so the tag
      carries no bold weight of its own).
    - The outbound link: `<a class="deck-link" href="https://github.com/open-gsd/gsd-core"
      target="_blank" rel="noopener">GSD is open source&nbsp;&#8599;</a>`. ⚠ **Verified live during the
      sketch session:** `open-gsd/gsd-core`, "Git. Ship. Done.", MIT, v1.7.0, 6.8k stars — the newer
      official repo, **not** an older mirror. ⚠ **Use the numeric entity `&#8599;` preceded by
      `&nbsp;`**, not the sketch's literal `↗` — the live file uses numeric entities throughout
      (`index.html:33-34`, `:134-137`).
    - ⚠ **`rel="noopener"` is mandatory** (gate G-7) — `index.html:29` states why: it is what blocks
      reverse tabnabbing.

    **The `data-ph-state` contract — read this before writing any CSS.**

    ⛔ **The sketch scopes its hiding rules under `body.js` and that pattern is FORBIDDEN.** It only
    avoids the invisible page by winning a CSS specificity race — `body.js .m [data-rise].in` (0,4,1)
    must out-specify `body.js .m [data-rise]` (0,3,1) — and **it lost that race twice in the sketch
    itself, shipping a dark page both times** (sketch README § Bugs, bug 1). A future edit silently
    loses it again. Gate G-2 greps `site/` for `body.js` and requires **zero hits**.

    Instead, `.ph` carries **`data-ph-state`**, a JS-created attribute with exactly two values —
    `armed` and `lit` — at **identical specificity**, mirroring the `data-rev-state` contract from
    plan 01. JS never runs → the attribute never exists → **no rule matches → the section renders
    fully lit and readable**. D-34 is satisfied structurally, not by discipline.

    | Element | `[data-ph-state='armed']` | `[data-ph-state='lit']` |
    |---|---|---|
    | `.ph__wire` | `transform: scaleX(0)` | `transform: scaleX(1)` + `transition: transform 800ms cubic-bezier(0.3, 0.7, 0.2, 1)` |
    | `.ph__name` | `opacity: 0.38` | `opacity: 1` |
    | `.ph__desc` | `opacity: 0; transform: translateY(8px)` | `opacity: 1; transform: none` + a `150ms` delay |
    | `.ph__dot` | unlit outline | filled `rgb(var(--accent))` + `box-shadow: 0 0 26px rgba(var(--accent), 0.95)` |

    ⚠ **Both sides must be written as `[data-ph-state='armed'] .x` and `[data-ph-state='lit'] .x`** —
    two attribute-value selectors at (0,2,0), not one bare `.x` and one `.x--lit`. The symmetry is what
    removes the race. ⚠ **The dimming is a JS-driven state too** (`sketch:369` learned this): a bare
    `.ph__name { opacity: .38 }` would leave a no-JS visitor with a permanently half-dead section —
    same lesson as D-34, one step further.

    ⚠ **Do NOT port the sketch's `@media (prefers-reduced-motion)` blocks** (`sketch:358`, `:372`,
    `:377`). D-36 forbids a second query; `styles.css:1156` already exists and `playTrack()`'s reduced
    branch sets the resolved state directly. ⚠ Also drop the `body.reduced` overrides — sketch toolbar
    plumbing.

    **CSS — port the sketch's geometry, re-theme colour and type:**

    - `.tracks` — `display: grid`, macro-tier fluid gap. `.phases--line` — `grid-template-columns:
      repeat(3, 1fr)`. `.ph` — `position: relative`, right padding on the micro 4-multiple scale (the
      sketch's `22px` → `20px` or `24px`).
    - `.ph__wire` — `position: absolute; top: 7px; left: 15px; right: 0; height: 1px;` gradient from
      `rgba(var(--accent), 0.6)` to `rgba(var(--accent), 0.2)`, `transform-origin: left center`.
      `.phases--line .ph:last-child .ph__wire { display: none; }` — **the line ends; that is the point.**
    - `.ph__dot` — `15px` square, `border-radius: 50%`, `background: var(--bg-0)`, `1px solid
      rgba(var(--accent), 0.5)`, `z-index: 2`.
    - `.ph__n` — `400 11px` mono, `0.2em` tracking, `rgba(255,255,255,0.3)`, margins on the 4-multiple
      scale (the sketch's `22px 0 8px` → normalise).
    - `.ph__name` — `400 clamp(24px, 2.7vw, 38px)` display, `-0.02em`, `line-height: 1.2` (UI-SPEC T-3
      — Sirio asked for the GSD phases to be bigger; this is the size bump).
    - `.ph__desc` — `300 16px/1.55` body, `rgba(228,232,244,0.6)`, `max-width: 26ch`.
    - `.track__kind` — `12px` mono, `0.24em` tracking, `rgb(var(--accent))`, ⚠ **weight `400`, NOT the
      sketch's `700`** (`sketch:317`). The live site never uses `700`. **Let the accent colour and the
      `0.24em` tracking carry the emphasis** — that is how every other label on the page does it
      (UI-SPEC T-1).
    - `.track__for` — `300 15px` body, `rgba(255,255,255,0.42)`.
    - `.track__title` — `300 clamp(24px, 2.4vw, 34px)` display, `-0.02em`, `line-height: 1.2`. ⚠ It is
      an `h3` and must sit clearly below the shared `h2` ceiling of `86px`.
    - `.track__foot` — `300 15px/1.6` body, `rgba(228,232,244,0.5)`, `max-width: 70ch`.
      `.track__foot b { color: rgba(var(--accent), 0.95); font-weight: 400; }`
    - `.deck-link` — the outbound-link style, reusable by the evidence strip in Task 3.
    - **Responsive:** port `sketch:422-432`'s `@media (max-width: 860px)` — `.phases--line` collapses
      to `1fr`, and `.ph__wire` rotates to a vertical `1px` line with `transform-origin: top center`
      and `scaleY` instead of `scaleX`. ⚠ **The vertical form's armed/lit rules use the same
      `data-ph-state` contract**, never `body.js`. ⚠ **OI-5:** the sketch was never validated below
      ≈485 CSS px (headless Chrome clamps there) — the `max-width: 760px`/`860px` query is *rendered*
      but **not validated at 375/390px**. Phase 3's mobile pass owns that check; record it.
    - ⚠ **Micro tier enforced:** the sketch used `26px`, `22px`, `10px`, `6px` freely. Normalise every
      new **fixed** value to {4, 8, 12, 16, 20, 24, 32, 48}px. Macro rhythm stays fluid `clamp()`.

    **`site/method.js` — `playTrack(el)` and the track observer, extending the plan-03 module:**

    - Add a **`T` timing object at the top beside `D`**, holding the sketch's track values:
      `phStep: 700` (ms between phases lighting), `phStart: 200` (ms before the first),
      `arcAfter: 700`, `railAfter: 700`. Same rule as `D`: **one object, at the top, numbers-only
      re-pacing.** ⚠ C-3's lesson is phase-wide — Sirio could not read the first cut and everything was
      roughly tripled. These are floors.
    - `playTrack(el)`:
      - ⚠ **Reduced branch first:** if `REDUCED`, set `data-ph-state="lit"` on every `.ph` in the track
        (and, in Task 2, `data-arc-state="lit"` / `data-rail-state="lit"`), then `return`. **Fully lit,
        every word present — nothing lost** (UI-SPEC C-5).
      - Otherwise stagger `data-ph-state="lit"` across the track's `.ph` elements at
        `T.phStart + i * T.phStep`.
    - Arm in the same synchronous pass that observes: set `data-ph-state="armed"` on every `.ph` inside
      `initMethod()` **only when not `REDUCED`**, immediately before observing. The code that hides is
      the code that shows.
    - **The track observer** (`sketch:1087-1097`): a second `IntersectionObserver` at
      `{ threshold: 0.25 }` on `[data-track]` elements. ⚠ **Sirio asked for this explicitly** — *"if you
      animate them on scroll, that would be good"* — so each track lights on **its own** scroll position
      rather than both firing together off-screen. Guard it with a per-track `Set` so each plays once.
    - ⚠ **Under `REDUCED`, do NOT attach the track observer** — call `playTrack()` on every track at
      init and return. The sketch learned this at `:1111-1117`: the reduced path has no sequence to
      play, so a missed or late IO callback would leave the section dimmed and unlit forever.
    - ⚠ **No `getBoundingClientRect()` in the callback** (gate G-5). ⚠ **No rAF loop** (D-37).
      ⚠ **`textContent`, never `innerHTML`** (L-5).
  </action>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && node --check site/method.js && test "$(grep -c 'data-track="digital"' site/index.html)" = "1" && test "$(grep -c 'phases--line' site/index.html)" = "1" && test "$(grep -c 'Spec-driven development, run by GSD' site/index.html)" = "1" && test "$(grep -c 'https://github.com/open-gsd/gsd-core' site/index.html)" = "1" && test "$(grep -c 'target="_blank"' site/index.html)" = "$(grep -c 'rel="noopener"' site/index.html)" && test "$(grep -c 'target="_blank"' site/index.html)" = "8" && test "$(grep -c 'data-sec=' site/index.html)" = "0" && test "$(grep -c 'id="method"' site/index.html)" = "1" && test "$(grep -c 'track__kind' site/styles.css)" -ge "1" && ! grep -A3 'track__kind' site/styles.css | grep -q '700' && test "$(grep -c "data-ph-state='armed'" site/styles.css)" -ge "3" && test "$(grep -c "data-ph-state='lit'" site/styles.css)" -ge "3" && test "$(grep -rc 'body\.js' site/ 2>/dev/null | grep -vc ':0')" = "0" && test "$(grep -c 'prefers-reduced-motion' site/styles.css)" = "1" && test "$(grep -c 'innerHTML' site/method.js)" = "0" && test "$(grep -c 'getBoundingClientRect' site/method.js)" = "0" && test "$(grep -c 'requestAnimationFrame' site/method.js)" = "0" && test "$(grep -c 'transition: all' site/styles.css)" = "0" && curl -s -o /dev/null -w 'gsd-repo=%{http_code}\n' -L https://github.com/open-gsd/gsd-core && echo GATES-PASS</automated>
    <human-check>Scroll the DIGITAL track into view on its own. The three dots must light one after another, each wire drawing left-to-right between them, and the line must **end** after Execute — no wire trailing off the last node. Confirm DIGITAL reads as cyan and that the label's emphasis comes from colour and tracking, not from bold. Scroll away and back: it must not replay or stall. Toggle `prefers-reduced-motion: reduce` and reload: the whole track must be fully lit with every description at full opacity, immediately, with no scrolling required.</human-check>
  </verify>

  <acceptance_criteria>
    - CLI: `node --check site/method.js` exits 0
    - Source: `site/index.html` contains exactly one `data-track="digital"` and exactly one `phases--line`
    - Source: `#method` remains **one** section — `grep -c 'id="method"' site/index.html` returns `1` and `grep -c 'data-sec=' site/index.html` returns `0`
    - Source: `.phases--line` contains exactly three `.ph` blocks, numbered `01`/`02`/`03`, named `Discuss`/`Plan`/`Execute`
    - Source: `site/index.html` contains `https://github.com/open-gsd/gsd-core` exactly once, with `target="_blank" rel="noopener"`, labelled with `&nbsp;&#8599;` (no literal ↗ glyph)
    - Source: `target="_blank"` count equals `rel="noopener"` count and both are `8` (7 after plan 02 + 1 new) (gate G-7)
    - Source: `site/styles.css` contains `[data-ph-state='armed']` and `[data-ph-state='lit']` selectors, at least 3 of each, and **no** `body.js` scoping
    - Source: `grep -rn 'body\.js' site/` returns zero hits (gate G-2)
    - Source: `site/styles.css` `.track__kind` rule has `font-weight` `400` — no `700` appears in its declaration block (UI-SPEC T-1)
    - Source: `site/styles.css` contains exactly **one** `prefers-reduced-motion` at-rule — the sketch's extra queries were not ported (D-36)
    - Source: `site/styles.css` contains `.phases--line .ph:last-child .ph__wire { display: none; }` or equivalent — the line ends
    - Source: `site/method.js` contains a `T` timing object declared at the top of the module beside `D`
    - Source: `grep -c 'innerHTML' site/method.js` = `0`; `grep -c 'getBoundingClientRect' site/method.js` = `0`; `grep -c 'requestAnimationFrame' site/method.js` = `0`
    - Source: `grep -c 'transition: all' site/styles.css` returns `0`
    - Source: every **fixed** spacing value introduced by this task is a member of {4, 8, 12, 16, 20, 24, 32, 48}px
    - CLI: `curl -s -o /dev/null -w '%{http_code}' -L https://github.com/open-gsd/gsd-core` returns `200`
    - Behavior: with JS disabled, every `.ph__name` and `.ph__desc` in the DIGITAL track renders at full opacity and every wire is drawn
    - Behavior: with `prefers-reduced-motion: reduce`, `method.js` attaches **no** track observer and every `.ph` carries `data-ph-state="lit"` at load
    - Behavior: at ≤860px the phases stack to one column and the wires run vertically, with no horizontal overflow
  </acceptance_criteria>

  <done>
    The DIGITAL track lights on its own scroll position — dots in sequence, wires drawing between them,
    the line ending after Execute — themed from the live tokens at weight 400, with the GSD repo link
    carrying `rel="noopener"`, no `body.js` anywhere, and a fully-lit reduced-motion end state.
  </done>
</task>

<task type="auto">
  <name>Task 2: PHYSICAL is a LOOP — the return arc and the DOCUMENT rail</name>

  <files>site/index.html, site/styles.css, site/method.js</files>

  <read_first>
    - `.planning/sketches/001-method-section/index.html:705-760` — **variant D's PHYSICAL track markup,
      LOCKED**: `.phases--loop`, the four `.ph` blocks, the `.loop-arc` SVG (`:743-746`), the
      `.doc-rail` (`:749-755`)
    - `.planning/sketches/001-method-section/index.html:334` — `.track--physical { --accent:
      var(--star-zuben); }` — the sanctioned accent override
    - `.planning/sketches/001-method-section/index.html:383-420` — the arc and doc-rail CSS, including
      the tick alignment at `:411-416` and the ⛔ `body.js` scoping at `:417`
    - `.planning/sketches/001-method-section/index.html:1054-1060` — `initArc()`
    - `.planning/sketches/001-method-section/README.md` § "Variant D copy — provenance, line by line"
      — ⚠ **especially the ⚠ DRAFTED BY CLAUDE block**
    - `site/styles.css:542-560` — the architectural comment stating *"`--accent` is section-driven,
      never plate-driven"*, and `:568-590` — the accent map with `--star-zuben` from plan 02
    - `site/method.js` as built in plan 03 + Task 1 — `D`, `T`, `initMethod()`, `playTrack()`
    - `.planning/phases/02-the-story/02-UI-SPEC.md` § `#method` (the loop track, the DOCUMENT rail,
      the Sirio corrections), § Color Cl-2 (the sanctioned override and why it is not the bug
      `styles.css:557` warns against), § Open Items **OI-2**
    - `.planning/phases/02-the-story/02-PATTERNS.md` § `site/index.html` — `#method` (the two sketch
      lines that must not ship as-is), and § `site/main.js` — METHOD sequence (`initArc()` is safe)
  </read_first>

  <action>
    Add the PHYSICAL track beside DIGITAL. **The loop is the argument**: the digital track runs straight
    through and ships; the physical track arcs back on itself because the real thing answers back.

    **Markup — port `sketch:706-760`:**

    - `.track.track--physical` with `data-track="physical"`.
    - `.track__head` → `<span class="track__kind">PHYSICAL</span>` +
      `<span class="track__for">the dispenser, the bench, the thesis hardware</span>`.
    - `<h3 class="track__title">Working with AI is one continuous loop</h3>` — **deck-verbatim**.
    - `.phases.phases--loop` with `data-phases`, containing **four** `.ph` blocks:

      | # | `.ph__name` | `.ph__desc` | `.ph__note` |
      |---|---|---|---|
      | 01 | Discuss & Design | Talk through the next move; AI proposes it — then argues each design decision and models the physics. | ⛔ **SEE OI-2 BELOW** |
      | 02 | Build & Iterate | Custom tools and artifacts get built along the way — often to *make* the decisions, not just to carry them out. | — |
      | 03 | Test | The design meets the real thing, and the real thing decides. Reality is the pressure test. | — |
      | 04 | Analyze Data | AI processes the results and runs the stats. | ↳ I give the direction — it writes the custom Python. It does not run itself. |

    - ⛔ **OI-2 — DO NOT SHIP THE SKETCH'S `.ph__note` ON PHASE 01.** `sketch:719` reads *"↳ skills
      carry my design rules in, so it reasons from my constraints"*. **That line is Claude-invented and
      unconfirmed.** Sirio asked only that the design part *"should mention the use of skills"* — **he
      never said what his skills do or contain.** Fabrication is a standing project landmine and this is
      exactly its shape (Cw-4; it has already bitten twice). **Action: CUT the note.** UI-SPEC OI-2
      names cutting as an acceptable fallback — *"the loop reads without it"* — and cutting is the only
      option that cannot ship a guess. Then raise it in the human check below so Sirio can supply his
      own sentence, which a later edit can add back as a one-line change. **Do not paraphrase it, do
      not soften it, do not ship a "safer" version of the guess.**
    - ⚠ **Phase 04's `.ph__note` is Sirio's deliberate narrowing and MUST ship.** The desc (*"AI
      processes the results and runs the stats"*) is the deck's blunter claim, which **overstates the
      autonomy**; the note narrows it. **Keep the note; do not widen the desc back.**
    - ⚠ **The loop's copy is deliberately GENERIC, per Sirio's explicit correction.** Round 3 pulled the
      deck's concrete nouns back out: *"Calculators, solvers and the campaign app"* → the Build &
      Iterate line above (*"just say that custom tools and artifacts are built also to make
      decisions"*); *"Gravimetric campaigns on the real machine"* → the Test line above (*"gravimetric
      testing is specific, this part is meant to be generic"*). **Do not re-introduce project-specific
      nouns** — no "campaign app", no "gravimetric", no "rotor solver". The loop must read as *how he
      works*, not as a thesis changelog.
    - In Build & Iterate, the emphasised word *make* uses `<em class="ph__em">` — the class defined in
      plan 03. ⚠ **Never an inline `style=`** (the sketch's `sketch:725` form).
    - **The return arc** — port `sketch:743-746` verbatim: `<svg class="loop-arc" id="loop-arc"
      viewBox="0 0 1000 62" preserveAspectRatio="none" aria-hidden="true">` with `#arc-path` and
      `#arc-head`. It loops back from phase 04 to phase 01.
    - **The DOCUMENT rail** — port `sketch:749-755`: `#doc-rail` with `.doc-rail__tag` (`DOCUMENT`) and
      `.doc-rail__txt`. ⚠ **The rail is a RAIL, not a step** — deck slide 7 says *"Document — captured
      continuously"*, so it is not a node in the chain at all. It spans **beneath** the loop with ticks
      aligned to phase **03 (Test)** and **04 (Analyze Data)**. **The alignment IS the argument:**
      documentation runs *while* the test runs. This is D-17's AI-for-documentation requirement, made
      structural.
    - `.track__foot` — *"Nothing here ships and stops — the real thing answers back, and the loop comes
      round again."*

    **CSS:**

    - `.phases--loop` — `grid-template-columns: repeat(4, 1fr)`. Unlike `.phases--line`, the **last
      wire stays** — the loop does not end.
    - ⚠ **`.track--physical { --accent: var(--star-zuben); }` — the ONE sanctioned deviation from
      `styles.css:557`** (*"`--accent` is section-driven, never plate-driven"*). Name it in the SUMMARY
      as sanctioned. **Why it is not the bug that comment warns against:** that warning exists because
      the V4 design gave a Work *plate* an Aldebaran accent inside an Antares section **arbitrarily** —
      decoration masquerading as meaning. Here the accent **is** the meaning: it encodes kind-of-work,
      and it is the mechanism by which the line/loop contrast reads at a glance. It is a **scoped,
      semantic, bounded** custom-property override on one subtree. The section head
      (`.section__label`, `.section__rule`, `.section__blob`) still derives from `#method`'s rigel and
      is unaffected. **Bonus resonance, not accident:** PHYSICAL green **is** ORIGIN green — physical
      work is the lab root, and the page says so in colour without a word.
    - `.loop-arc` — `display: block; width: 100%; height: 62px; margin-top: 4px; overflow: visible;`
      `.loop-arc path` — `fill: none; stroke: rgba(var(--accent), 0.45); stroke-width: 1;
      stroke-dasharray: var(--len); stroke-dashoffset: var(--len);`
      `[data-arc-state='lit'] path` (or `.loop-arc[data-arc-state='lit'] path`) →
      `stroke-dashoffset: 0; transition: stroke-dashoffset 1.5s ease;`
      ⚠ **The armed state must key on `[data-arc-state='armed']`, not on a bare `.loop-arc path`
      rule** — otherwise a no-JS visitor gets an invisible arc forever (D-34, one step further). Two
      values, one JS-created attribute, identical specificity.
      ⚠ **`stroke-dashoffset` is NOT a compositor property** — it repaints every frame. That is
      acceptable here because the arc is one thin path over a small area, drawn once. **Do not
      generalise this mechanism to anything larger**; the page's other lines use `transform: scaleX()`
      precisely because it is free (research P-1 verdict).
    - `.doc-rail` — dashed `1px rgba(var(--accent), 0.32)`, `border-radius: 8px`, `background:
      rgba(var(--accent), 0.045)`, padding on the 4-multiple scale (the sketch's `16px 20px` is already
      compliant). Ticks via `::before` / `::after` at `left: calc(50% + 7px)` and `left: calc(75% + 7px)`
      — the 4-column grid puts the Test and Analyze dots exactly there. `[data-rail-state='armed']` →
      `opacity: 0; transform: translateY(8px)` (the sketch's `10px` → normalised);
      `[data-rail-state='lit']` → `opacity: 1; transform: none;` with an enumerated transition.
    - `.doc-rail__tag` — ⚠ **weight `400`, not the sketch's `700`** (`sketch:404`); accent colour and
      `0.24em` tracking carry the emphasis (T-1).
    - **Responsive** (`sketch:434-436`): below `860px`, `.loop-arc { display: none; }` and the rail's
      ticks hide — the arc's `viewBox` geometry assumes the 4-column row. ⚠ **OI-5:** not validated
      below ≈485 CSS px; Phase 3 owns the real-device check.

    **`site/method.js`:**

    - **`initArc()`** — port `sketch:1055-1060`: for each `.loop-arc path`, read
      `Math.ceil(p.getTotalLength())` and set it as the `--len` custom property. ⚠ **`getTotalLength()`
      is a layout read, and it is SAFE here** — it runs **once, at init, outside any loop, scroll
      handler or IO callback.** Gate G-5 forbids reads in `loop()`, the pointer handler and the IO
      callback; **it does not apply to `initArc()`.** State that in the SUMMARY so the gate is not
      misapplied to it later.
    - Call `initArc()` from `initMethod()`, **before** arming or observing, on both the reduced and
      non-reduced paths — the arc needs its length in either case (the reduced path sets
      `data-arc-state="lit"`, which still needs `--len` to have resolved the dasharray).
    - Extend `playTrack(el)` to set `data-arc-state="lit"` on the track's `.loop-arc` at
      `T.phStart + phases.length * T.phStep`, and `data-rail-state="lit"` on its `.doc-rail`
      `T.railAfter` after that. Both must be present in the `REDUCED` branch too — set `lit`
      immediately alongside the phases.
    - ⚠ **Do not port the sketch's `@media (prefers-reduced-motion)` at `:391` / `:420`.** D-36 forbids
      a second query; `playTrack()`'s reduced branch already sets `lit`, which resolves
      `stroke-dashoffset` to `0`. ⚠ This matters specifically because **`styles.css:1156` flattens
      *durations* but does not reset *values*** — an armed `stroke-dashoffset: var(--len)` is **not**
      rescued by it. **JS owns the resolved end state.**
  </action>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && node --check site/method.js && test "$(grep -c 'data-track="physical"' site/index.html)" = "1" && test "$(grep -c 'phases--loop' site/index.html)" = "1" && test "$(grep -c 'Working with AI is one continuous loop' site/index.html)" = "1" && test "$(grep -c 'id="loop-arc"' site/index.html)" = "1" && test "$(grep -c 'id="doc-rail"' site/index.html)" = "1" && test "$(grep -c 'skills carry my design rules' site/index.html)" = "0" && test "$(grep -c 'It does not run itself' site/index.html)" = "1" && test "$(grep -ciE 'gravimetric|campaign app|rotor solver' site/index.html)" = "0" && test "$(grep -c 'style="' site/index.html)" = "0" && grep -q 'track--physical' site/styles.css && grep -A2 'track--physical' site/styles.css | grep -q -- '--accent: var(--star-zuben)' && test "$(grep -c "data-arc-state='armed'" site/styles.css)" -ge "1" && test "$(grep -c "data-rail-state='armed'" site/styles.css)" -ge "1" && test "$(grep -c 'getTotalLength' site/method.js)" = "1" && test "$(grep -c 'prefers-reduced-motion' site/styles.css)" = "1" && test "$(grep -rc 'body\.js' site/ 2>/dev/null | grep -vc ':0')" = "0" && test "$(grep -c 'getBoundingClientRect' site/method.js)" = "0" && ! grep -A3 'doc-rail__tag' site/styles.css | grep -q '700' && echo GATES-PASS</automated>
    <human-check>⚠ **OI-2 — this one blocks a line and needs Sirio's words.** The Discuss & Design note *"skills carry my design rules in, so it reasons from my constraints"* has been **cut** because Claude invented it — Sirio only ever said the design part "should mention the use of skills", never what his skills contain. Sirio: either give the sentence in your own words (it goes back as a one-line edit) or confirm the loop reads fine without it. **Do not let anyone re-add a guess.**

Then scroll the PHYSICAL track into view. It must be **green** — visibly a different kind of work from DIGITAL's cyan — and the four dots must light in sequence, then the return arc must draw itself from Analyze Data back round to Discuss & Design, then the DOCUMENT rail must arrive with its two ticks landing under Test and Analyze Data. **Those ticks are the argument** — check they align with the dots, not near them. Toggle `prefers-reduced-motion: reduce` and reload: the arc must be fully drawn (not invisible) and the rail present — this is the case `styles.css:1156` cannot rescue, so it is the one most likely to be wrong.</human-check>
  </verify>

  <acceptance_criteria>
    - CLI: `node --check site/method.js` exits 0
    - Source: `site/index.html` contains exactly one `data-track="physical"`, one `phases--loop`, one `id="loop-arc"`, one `id="doc-rail"`
    - Source: `.phases--loop` contains exactly four `.ph` blocks named `Discuss & Design`, `Build & Iterate`, `Test`, `Analyze Data`
    - Source: `grep -c 'skills carry my design rules' site/index.html` returns `0` — **OI-2's invented line is not on the page in any form** (cut, not paraphrased)
    - Source: `grep -c 'It does not run itself' site/index.html` returns `1` — Sirio's narrowing note on Analyze Data ships
    - Source: `grep -ciE 'gravimetric|campaign app|rotor solver' site/index.html` returns `0` — the loop copy stayed generic per Sirio's round-3 correction
    - Source: `grep -c 'style="' site/index.html` returns `0` — the `make` emphasis uses `<em class="ph__em">`
    - Source: `site/styles.css` contains `.track--physical { --accent: var(--star-zuben); }`
    - Source: `site/styles.css` contains `[data-arc-state='armed']` and `[data-arc-state='lit']`, and `[data-rail-state='armed']` and `[data-rail-state='lit']` — each pair at identical specificity, with **no** bare armed rule that applies without JS
    - Source: `site/styles.css` `.doc-rail` ticks are at `left: calc(50% + 7px)` and `left: calc(75% + 7px)`
    - Source: `site/styles.css` `.doc-rail__tag` and `.track__kind` both use `font-weight` `400`; no `700` in either declaration block
    - Source: `site/styles.css` contains exactly **one** `prefers-reduced-motion` at-rule (D-36)
    - Source: `site/method.js` contains `getTotalLength` exactly once, inside `initArc()`, and `initArc()` is called from `initMethod()` outside any loop, scroll handler or observer callback
    - Source: `grep -c 'getBoundingClientRect' site/method.js` returns `0` (gate G-5 — `getTotalLength()` in `initArc()` is explicitly **not** a G-5 violation)
    - Source: `grep -rn 'body\.js' site/` returns zero hits (gate G-2)
    - Behavior: with `prefers-reduced-motion: reduce`, the loop arc renders **fully drawn** (`stroke-dashoffset: 0`) and the doc-rail is fully opaque at load, with no scroll required — JS set the resolved end state because `styles.css:1156` flattens durations but cannot reset an armed `stroke-dashoffset`
    - Behavior: with JS disabled, every phase name, description and note in the PHYSICAL track renders at full opacity and the rail is visible
    - Behavior: the rail's two ticks align with the phase-03 and phase-04 dots at ≥861px
    - Behavior: at ≤860px the loop arc is hidden and the phases stack, with no horizontal overflow
    - Human: OI-2 resolved — Sirio either supplied his own skills sentence or confirmed the loop reads without it
  </acceptance_criteria>

  <done>
    The PHYSICAL track lights green on its own scroll position, the return arc draws itself back to
    Discuss & Design, and the DOCUMENT rail lands with its ticks aligned to Test and Analyze Data; the
    invented skills line is gone; Sirio's narrowing note ships; reduced motion resolves the arc and
    rail rather than leaving them armed.
  </done>
</task>

<task type="auto">
  <name>Task 3: IN PRACTICE — the understated evidence strip and the deck link</name>

  <files>site/index.html, site/styles.css</files>

  <read_first>
    - `.planning/sketches/001-method-section/index.html:763-770` — **variant D's evidence strip, LOCKED**
    - `.planning/sketches/001-method-section/README.md` § "Content Provenance" — the deck link's
      verification status
    - `site/index.html:33-34` — the shipped outbound-link pattern
    - `site/styles.css` — `.deck-link` as defined in Task 1
    - `.planning/phases/02-the-story/02-UI-SPEC.md` § `#method` (Evidence strip), § Copywriting Cw-1
      (the METHOD outbound links table)
    - `.planning/phases/02-the-story/02-CONTEXT.md` D-10, D-17, D-18, D-19
  </read_first>

  <action>
    Close the section with the evidence strip — the part that owns the claim without shouting it.

    ⚠ **D-10 is the whole design constraint here.** Sirio's exact words: *"In between the explicit and
    the philosophy first option."* Lead with the philosophy (the manifesto, the tracks — all of which
    came first), then own the site as evidence **plainly but late and understated**. **Do not headline
    "this site was built with AI"**, and do not bury it as a footnote either. This strip is the "late
    and understated" half. Its placement at the very end of `#method` is load-bearing.

    **Markup — port `sketch:763-770` as the last child of `.method__act--tracks`:**

    - `.evidence` with `data-reveal="6"` (continuing the section's reveal indices past the manifesto's
      1–5) and the default fade-up kind — it is a quiet arrival, deliberately not a `snap` or a `draw`.
    - `.evidence__label` → `IN PRACTICE`.
    - `.ev-grid` with two `.ev` items, verbatim:
      - *"The same method built my thesis tooling — and this page."* — ⚠ this is **D-19's available
        true claim**, and it is stronger and more specific than "I used AI." It is checkable: the deck
        link below proves the thesis-tooling half, and the page itself proves the other.
      - *"AI shortens the gap between an idea and a finished thing. It does not choose the idea."* —
        this is D-17's **"he decides the direction; AI turns it into reality"** requirement. Sirio
        emphasised *"I DO!"* — the direction is his, always.
    - The deck link (**D-18**): `<a class="deck-link"
      href="https://sirsirio.github.io/thesis-tools/decks/lab-meeting-2026-06/" target="_blank"
      rel="noopener">see the method&nbsp;&#8599;</a>`. ⚠ **A quiet link, not a call to action.** It
      makes the claim checkable and shows the method applied to real thesis engineering, not just a
      portfolio.
    - ⚠ **`rel="noopener"` is mandatory** (gate G-7). ⚠ **Numeric entity `&#8599;` preceded by
      `&nbsp;`**, not the sketch's literal `↗`.

    ⚠ **The deck's content was VERIFIED during the sketch session** — `02-RESEARCH.md` Open Question 1
    flagged that the deck had never been opened and that D-17's quotes were therefore at risk of being
    fabrication; the sketch session fetched `index.html#/7` and confirmed *"Working with AI is one
    continuous loop"*, *"Spec-driven development, run by GSD"*, *"Document — captured continuously"*
    and the Discuss/Plan/Execute sub-lines **verbatim**. That question is **closed**. This task must
    still confirm the **URL still 200s**, because it is a live outbound link on a shipped page.

    **CSS — near the track styles:**

    - `.evidence` — a quiet block, separated from the tracks by macro-tier fluid rhythm.
    - `.evidence__label` — `IN PRACTICE`, mono, `400`, accent-tracked in the shipped label idiom
      (cf. `.section__label` at `styles.css:638-643`). ⚠ **Weight `400`; the live site never uses 700.**
    - `.ev-grid` — a two-column grid at desktop collapsing to one column at `860px`; gap on the
      4-multiple micro scale.
    - `.ev` — `300 15px`–`16px`/1.55 body copy in `--text-body`. ⚠ **Supporting text is deliberately
      NOT scaled up** — only the narrative prose is (UI-SPEC T-3). Do not reuse `.narr` here.
    - `.deck-link` — reuse the class from Task 1; do not define a second link style.
    - ⚠ **Accent is never on the `.ev` prose** (Cl-3) — that is `--text-body`. Accent belongs to the
      `↗` glyph and its hover state only.
    - ⚠ **`:focus-visible` alongside `:hover`** on `.deck-link`, and restate `color` on both — the
      comment at `styles.css:1124-1125` records that the global `a:hover` rule outranks these on
      specificity and would otherwise repaint them.
  </action>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && test "$(grep -c 'IN PRACTICE' site/index.html)" = "1" && test "$(grep -c 'sirsirio.github.io/thesis-tools/decks/lab-meeting-2026-06' site/index.html)" = "1" && test "$(grep -c 'class="ev"' site/index.html)" = "2" && test "$(grep -c 'The same method built my thesis tooling' site/index.html)" = "1" && test "$(grep -c 'It does not choose the idea' site/index.html)" = "1" && test "$(grep -c 'deck-link' site/index.html)" = "2" && test "$(grep -c 'target="_blank"' site/index.html)" = "$(grep -c 'rel="noopener"' site/index.html)" && test "$(grep -c 'target="_blank"' site/index.html)" = "9" && test "$(grep -ciE '\bego\b|grano|salis' site/index.html)" = "0" && test "$(grep -c '↗' site/index.html)" = "0" && test "$(grep -c 'deck-link:focus-visible\|deck-link:hover' site/styles.css)" -ge "1" && curl -s -o /dev/null -w 'deck=%{http_code}\n' -L https://sirsirio.github.io/thesis-tools/decks/lab-meeting-2026-06/ && test "$(grep -c '<script' site/index.html)" = "1" && test ! -f site/package.json && echo GATES-PASS</automated>
    <human-check>Read the whole METHOD section top to bottom as a stranger would. Does the IN PRACTICE strip land **plainly but late and understated** (D-10) — owning that this page is itself the evidence, without shouting it? It must not read as a boast and it must not be invisible. Click "see the method ↗" and "GSD is open source ↗": both must open in a new tab and land correctly. Finally: is the whole section — manifesto, calculator, two tracks, evidence — still one coherent argument that follows from ORIGIN's ending, or has it become a topic of its own (D-16)?</human-check>
  </verify>

  <acceptance_criteria>
    - Source: `site/index.html` contains `IN PRACTICE` exactly once, inside `#method`, as the last block of the section
    - Source: `.ev-grid` contains exactly two `.ev` items carrying the two approved sentences verbatim
    - Source: `site/index.html` contains `https://sirsirio.github.io/thesis-tools/decks/lab-meeting-2026-06/` exactly once, with `target="_blank" rel="noopener"`, labelled `see the method&nbsp;&#8599;`
    - Source: `target="_blank"` count equals `rel="noopener"` count and both are `9` (8 after Task 1 + 1 new) (gate G-7)
    - Source: `grep -c '↗' site/index.html` returns `0` — every arrow is the numeric entity `&#8599;`
    - Source: `site/styles.css` `.deck-link` has a `:hover` / `:focus-visible` rule that restates `color`
    - Source: `.ev` type is 15–16px — **not** `.narr`'s `clamp(20px, 1.7vw, 25px)` (T-3: supporting text is not scaled up)
    - Source: no font-weight above `400` was introduced
    - Source: `grep -ciE '\bego\b|grano|salis' site/index.html` returns `0` (gate G-8)
    - CLI: `curl -s -o /dev/null -w '%{http_code}' -L https://sirsirio.github.io/thesis-tools/decks/lab-meeting-2026-06/` returns `200` (D-18 — a live outbound link on a shipped page)
    - CLI: `grep -c '<script' site/index.html` returns `1`; `test ! -f site/package.json` succeeds (gate G-6)
    - Behavior: both METHOD outbound links open in a new tab and the opened page cannot reach `window.opener`
    - Behavior: with JS disabled, the evidence strip and both links are fully readable and clickable
  </acceptance_criteria>

  <done>
    METHOD closes on an understated IN PRACTICE strip carrying D-19's checkable claim and D-17's
    direction claim, with the deck and GSD repo links both live, both `rel="noopener"`, both using
    numeric-entity arrows — and all four of D-17's mandated contents now present as structure rather
    than bullets.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages CDN → visitor's browser | The only boundary that exists. Static, client-side-only, no server runtime, no auth, no sessions, no data store, **no input fields**, no secrets in client code (`PROJECT.md:37`). |
| Page → third-party sites (github.com/open-gsd, sirsirio.github.io) | **New this plan.** Two outbound `target="_blank"` links leave the origin. |
| Google Fonts → page | Third-party subresource shipped in Phase 1. Untouched — this plan adds no `<script src>`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | the 2 new `target="_blank"` anchors (GSD repo, GSD deck) | mitigate | **Reverse tabnabbing.** `rel="noopener"` on both, matching `.nav__link` (`index.html:33-34`). Enforced by the count-parity criterion (`target="_blank"` == `rel="noopener"` == 9). The sketch already carries `noopener` on both (`sketch:702`, `:769`) — preserve it, do not lose it in the port. |
| T-02-16 | Tampering | `https://github.com/open-gsd/gsd-core` as a linked destination | accept | Third-party repo Sirio does not control. Verified live during the sketch session as the **newer official repo** (`open-gsd/gsd-core`, MIT, v1.7.0, 6.8k stars, npm `@opengsd/gsd-core`) — **not an older mirror**, which is the realistic confusion risk here. Re-verified by the `curl` 200 check. It is a link, not an executed dependency: no code from it enters the page. |
| T-02-17 | Tampering | `site/method.js` — `playTrack()` / `initArc()` DOM writes | mitigate | **DOM XSS via `innerHTML`.** Both functions write only attribute values (`data-ph-state`, `data-arc-state`, `data-rail-state`) and a numeric custom property (`--len`). No markup construction. Enforced by `grep -c 'innerHTML' site/method.js` = 0 (L-5). ⚠ Variant A of the sketch builds an HTML string (`sketch:859-860`); it is **not** the locked variant. |
| T-02-18 | Denial of Service (self-inflicted) | every visitor, JS-disabled or module-load failure | mitigate | ⛔ The sketch's `body.js` scoping **shipped a dark page twice** by losing a specificity race. Replaced by `data-ph-state` / `data-arc-state` / `data-rail-state` — each two values of one JS-created attribute at identical specificity. Verified by `grep -rn 'body\.js' site/` = 0 and by the JS-disabled criteria (every phase name, desc and note at full opacity; the rail visible). Severity would be **high** if missed. |
| T-02-19 | Denial of Service (self-inflicted) | mobile paint budget | accept | `stroke-dashoffset` on the loop arc is **not** a compositor property and repaints every frame — a deliberate, bounded exception. Accepted because it is one thin path over a ~62px-tall area, drawn once per page load, and hidden entirely below 860px. ⚠ The plan explicitly forbids generalising the mechanism; every other line on the page uses `transform: scaleX()` because it is free (research P-1 verdict). Phase 3's mobile pass owns the on-device confirmation. |
| T-02-20 | Repudiation / misrepresentation | METHOD's copy about Sirio's own working method | mitigate | ⚠ **The real risk in this plan is not a security bug — it is shipping a claim Sirio never made.** OI-2's *"skills carry my design rules in"* is Claude-invented; D-17's remaining quotes were verified against the deck during the sketch session (closing `02-RESEARCH.md` Open Question 1). Mitigation: the invented line is **cut, not paraphrased**, enforced by `grep -c 'skills carry my design rules' site/index.html` = 0; the deck-verbatim strings are cited line by line in the sketch README; Sirio's narrowing note on Analyze Data ships and the desc is not widened back. Fabrication is a standing project landmine that has already bitten twice. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | **Not applicable — this plan runs zero package-manager installs.** `02-RESEARCH.md` § Package Legitimacy Audit records zero packages phase-wide. If any task is later amended to add a dependency it contradicts D-35 and must be **rejected, not audited**. |

**No `high`-severity threat is left unmitigated.** `security_block_on: high` is satisfied.

**Explicitly not applicable on this stack:** Spoofing, Elevation of Privilege, ASVS V2/V3/V4/V6.
⚠ **ASVS V5 (Input Validation) has no target — this plan adds zero input fields.** CONT-03 is a
narrative section; the **AI Concierge (ASKAI-\*) is Phase 6** and owns the input surface. Do not
conflate them.
</threat_model>

<verification>
Gates from `02-UI-SPEC.md` § Verification Gates reachable in this plan:

- **G-1 (JS disabled):** every word of both tracks and the evidence strip readable; nothing dimmed,
  no invisible arc.
- **G-2 (greps clean):** `grep -c '\[data-reveal\]' site/styles.css` → `0`; `grep -rn 'body\.js' site/`
  → **zero hits** (this is the gate the sketch failed twice).
- **G-3 (reduced motion):** every track fully lit, the arc fully drawn, the rail present. ⚠ This is the
  case `styles.css:1156` **cannot** rescue — it flattens durations but does not reset an armed
  `stroke-dashoffset` or `scaleX(0)`. JS owns the resolved end state.
- **G-4 (accent resolves):** `#method` returns a triplet; `.track--physical` resolves to `--star-zuben`.
- **G-5 (no loop reads):** `grep -c 'getBoundingClientRect' site/method.js` → `0`. ⚠ `getTotalLength()`
  in `initArc()` is **not** a violation — one read, at init, outside every hot path.
- **G-6 (zero dependencies):** no `site/package.json`; one `<script>` tag.
- **G-7 (`rel="noopener"`):** count parity holds at 9/9.
- **G-8 (no banned phrasing):** `grep -ciE '\bego\b|grano|salis' site/index.html` → `0`.
- **G-10 (OI-2):** the skills line is **cut**; raised to Sirio in Task 2's human check for his words or
  his confirmation. `workflow.human_verify_mode` is `end-of-phase`, so it is collected there.

Not reachable in this plan: G-9 (the `github.com/SirSirio` review — plan 02).
</verification>

<success_criteria>
- CONT-03 is complete. All four of D-17's mandated contents ship as **structure, not bullets**:
  AI-for-documentation (the rail), GSD Discuss→Plan→Execute (the line), AI pressure-tests the design
  (the Test node), and he decides the direction (the calculator's landing line + the evidence strip).
- The line/loop contrast reads at a glance, carried by the sanctioned `.track--physical` accent
  override — the one defended deviation from `styles.css:557`, where the accent **is** the meaning.
- MOTION-01's draw-on leg extends into the section's own geometry: wires `scaleX`, the arc
  `stroke-dashoffset`.
- **Nothing invented reaches the page.** OI-2's line is cut rather than guessed at; every other string
  is deck-verbatim, profile-grounded, or Sirio's own correction.
- Sirio's narrowing survives: Analyze Data's note ships and its desc is not widened back to the deck's
  blunter autonomy claim.
- D-36/D-37 hold: one reduced-motion query, one rAF loop, `loop()` and `onScroll()` untouched.
- Zero dependencies, one script tag, zero `body.js`.
</success_criteria>

<output>
Create `.planning/phases/02-the-story/02-04-SUMMARY.md` when done.

Record explicitly:
- **OI-2's disposition** — that the skills line was **cut, not shipped**, and that it goes back only
  in Sirio's own words. This must survive into Phase 2.1's context; the deep-dive phase will be
  tempted to re-add it.
- **The `.track--physical` accent override named as SANCTIONED**, with Cl-2's reasoning, so a future
  reader does not "fix" it against `styles.css:557`.
- **That `getTotalLength()` in `initArc()` is not a G-5 violation** and why — one read, at init,
  outside every hot path. The gate must not be misapplied to it.
- **OI-5 carried to Phase 3:** the tracks were never validated below ≈485 CSS px. The mobile pass must
  check them on a real device at 375/390px.
- Whether the reduced-motion arc actually resolved — this is the case the CSS block cannot rescue and
  the most likely thing to have been got wrong.
- The measured wall-clock duration of each track's sequence, and whether Sirio wanted them slower.
</output>
