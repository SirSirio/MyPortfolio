---
phase: 01-the-design-build
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - site/index.html
  - site/styles.css
  - site/assets/media/README.md
autonomous: true
requirements: [FOUND-04, MEDIA-01]

must_haves:
  truths:
    - "A visitor scrolls one continuous page from the hero through Mission, Selected Work, Experience, Publications and Contact"
    - "The verbatim mission one-liner introduces the page, with the gradient span on 'hardware, software and automation'"
    - "Selected Work shows exactly 3 plates in importance order: 001 Portable Precision Liquid Dispenser (2026), 002 Automated Gel Electrophoresis (2025), 003 EndoSense (2024)"
    - "Experience shows exactly 4 reverse-chronological rows, including the AGC Biologics Student Assistant role the design omits"
    - "Publications shows exactly ONE row (EMBO), labelled 'Contributing author' with no position number"
    - "Every media slot renders as a labelled striped 16/9 placeholder, and pasting one <img> or <video> into it swaps to the asset with no CSS or JS change (MEDIA-01)"
    - "The docked nav from 01-02 stays reachable across every section and its anchors land correctly under the 70px offset"
    - "Contact exposes sirio.feltrin@gmail.com and external links open in a new tab"
  artifacts:
    - path: "site/index.html"
      provides: "Mission, Selected Work (3 plates), Experience (4 rows), Publications (1 row), Contact + footer — curated from profile.md"
      contains: "Contributing author"
      min_lines: 180
    - path: "site/styles.css"
      provides: "One .section class + per-section --accent, plate/exp/pub/contact styling, and the :has()-driven swappable media slot"
      contains: ":has(> img"
    - path: "site/assets/media/README.md"
      provides: "MEDIA-01 documented drop-in folder: naming convention, formats, and the one-line reference to paste"
      contains: "media-slot"
  key_links:
    - from: "site/styles.css .media-slot"
      to: "site/assets/media/"
      via: "CSS :has() toggles placeholder chrome off when an img/video child exists — zero JS"
      pattern: ":not\\(:has\\(> img, > video\\)\\)"
    - from: "site/index.html #work plate 003"
      to: "https://2024.igem.wiki/DTU-Denmark/"
      via: "external link mandated by profile.md:11 for every EndoSense listing"
      pattern: "2024\\.igem\\.wiki/DTU-Denmark"
    - from: "site/index.html #publications"
      to: "https://doi.org/10.1038/s44318-025-00567-1"
      via: "EMBO DOI on the single publication row"
      pattern: "10\\.1038/s44318-025-00567-1"
    - from: "site/index.html sections"
      to: "site/styles.css --accent"
      via: "one .section class; each section sets --accent to a PAL stop and every child reads rgba(var(--accent), a)"
      pattern: "--accent"
---

<objective>
Fill the page: the curated content sections — Mission, Selected Work, Experience, Publications, Contact — ported from the approved V4 design, **recontented from `../00_Profile/profile.md`**, plus the `:has()`-driven swappable media slot that closes MEDIA-01.

Two things make this plan different from a straight port. First, **most of the design's placeholder content is factually wrong** — it invents a "Research Assistant 2022–2024" role that does not exist, mis-titles the iGEM role, mislabels the EMBO publication, carries the literal lorem string `Descriptio od endosese`, and **invents a second publication that `profile.md` does not support at all**. Every row must be traced to `profile.md` or deleted. Second, the design's media slots are hard-coded placeholders that toggle nothing; MEDIA-01 needs a real swap mechanism.

Purpose: FOUND-04 (the single immersive scroll home, completed) and MEDIA-01 (swappable slots). Closes ROADMAP success criteria 3 and 6.
Output: The complete Phase 1 page — deliberately tight, a few curated entries per section, media slots empty but drop-in ready.
</objective>

<execution_context>
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/workflows/execute-plan.md
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-the-design-build/01-RESEARCH.md
@.planning/phases/01-the-design-build/01-PATTERNS.md
@.planning/phases/01-the-design-build/01-02-SUMMARY.md

# Design source of truth
@.planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html

# Content source of truth — the ONLY content source (CLAUDE.md "Sources of Truth")
@../00_Profile/profile.md

# Live source being modified (as left by Plan 01-02)
@site/index.html
@site/styles.css
</context>

<constraints>
**Paths are `site/`.** The folder was renamed from `app/` in commit 1b4b908. Zero `app/` references.

**`../00_Profile/profile.md` is the only content source.** `_Archive/` is git-ignored and off-limits. There is no CONTEXT.md for this phase by design (deleted in commit bdbc909). If something is not in `profile.md`, it does not go on the page — **do not invent, infer, or pad.**

**Ship small, then expand** (STATE.md, locked): *"it is ok if for some sections I have only a couple of entries max."* Tight beats exhaustive. Do not add entries beyond the curation specified below.

**Do not port** the deferred V4 machinery (Phase 2/4): `[data-proj]` overlays, `openProj`, `closeProj`, `startBurst`, `applyTrans`, `drawWarp`, `[data-warpov]`, `data-open`, `cursor:pointer` on plates. Per RESEARCH Open Question 1, **Phase 1 ships persistent nav + single-scroll home only.** The V4 design does answer FOUND-04's "multi-page" question — with `position:fixed` overlay divs revealed by a warp transition, not separate `.html` files — and that answer is recorded here so **Phase 4 (PROJ-01) does not re-litigate it**. Building detail pages now contradicts both "ship small" and the Phase 4 boundary.

**`style-hover` is not real HTML** (~14 occurrences across V4: media boxes, plate CTAs, sector pills, publication rows, the contact email). Browsers ignore it silently, while the paired `transition:` in the normal `style` attribute survives — so a verbatim port looks right in a screenshot and is completely dead on hover. Translate every one into a real CSS `:hover` rule.

**⚠ `data-reveal` — the invisible-page landmine.** Port the `data-reveal` attributes with the markup (`V4:56,58,73,77,78,80,92,111,115,116,145,149,150,167,168,169`), but leave `updateReveals()` for Phase 2 (MOTION-01). This is safe **because `updateReveals()` is what sets `opacity:0` in the first place** (`V4:579-585`) — without it the elements simply render normally. **Therefore: do NOT add any CSS rule that hides or fades `[data-reveal]`.** Doing so ships an invisible page. `site/styles.css` must contain zero `[data-reveal]` selectors.

**No inline styles.** FOUND-03's token system landed in 01-02; every value here goes through `--accent` and the existing tokens.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Mission + the section system — the one-liner introduces the page</name>

  <files>site/index.html, site/styles.css</files>

  <read_first>
    - `.planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html:52-78` — the Mission section (`:53-66`) and the Work section's head (`:69-78`), which together define the repeating section furniture. Note `:57` (**the verbatim one-liner and its gradient span**), `:59-62` (the sector pills), `:63` (**the dead copy to delete**), `:70-71` (numeral + blob), `:74-77` (label + rule + h2).
    - `.planning/phases/01-the-design-build/01-PATTERNS.md` — the `site/styles.css` section: the `.section` furniture CSS block, the accent table (id → sector label → `--accent` → PAL stop → numeral side → stroke alpha), the mono-label typography rule, and the `style-hover` translation table.
    - `.planning/phases/01-the-design-build/01-RESEARCH.md` — "Pattern 3: The star-spectrum section system" and "Design Tokens (FOUND-03)".
    - `site/styles.css` and `site/index.html` as left by Plan 01-02 — **reuse the tokens already on `:root`** (`--star-sirius`, `--star-antares`, `--star-aldebaran`, `--star-vega`, `--font-display`, `--font-mono`, `--pad-section`, `--max-w`, `--nav-h`, `--text-hi`, `--text-body`). Do not redefine them.
    - `.planning/STATE.md` — the locked one-liner decision.
  </read_first>

  <action>
    Build the reusable section system once, then use it for Mission. Every V4 section is structurally identical (`:69-104` ≡ `:107-138` ≡ `:141-161`) and **only the accent triplet changes** — so author one `.section` class and set `--accent` per section. This is what makes plate reordering free and the file portable.

    **1 — Section furniture in `site/styles.css`** (values from `V4:69-78`, consolidated per PATTERNS):
      - `.section` — `position:relative; padding:var(--pad-section); scroll-margin-top:var(--nav-h)`. The `70px` offset is already on every design section (`V4:53,69,107,141,164`) and matches the docked nav height from 01-02.
      - `.section__numeral` — the giant outlined numeral: `position:absolute; top:-2%; font:500 clamp(170px,27vw,400px)/0.8 var(--font-display); letter-spacing:-0.04em; color:transparent; -webkit-text-stroke:1px rgba(var(--accent),0.22); pointer-events:none; user-select:none`, carrying `aria-hidden="true"` and `data-para="-0.07"`. Numeral side alternates: `#work` right, `#experience` left, `#publications` right (`V4:70,108,142`).
      - `.section__blob` — `position:absolute; width:50vw; height:50vw; border-radius:50%; background:radial-gradient(circle, rgba(var(--accent),0.10), rgba(var(--accent),0) 62%); pointer-events:none`, carrying `data-para="0.13"`. `applyPara()` from 01-02 drives both parallax elements; its ±400px cull is the perf guard.
      - `.section__label` — `font:400 11px var(--font-mono); letter-spacing:0.26em; color:rgba(var(--accent),0.85); white-space:nowrap`.
      - `.section__rule` — `flex:1; height:1px; background:linear-gradient(90deg, rgba(var(--accent),0.4), rgba(var(--accent),0))`.
      - `.section__inner` — `position:relative; max-width:var(--max-w); margin:0 auto`.
      - `h2` — `margin:14px 0 0; font:300 clamp(40px,6vw,86px) var(--font-display); letter-spacing:-0.035em; color:var(--text-hi)`.

    **2 — The accent map.** Set `--accent` on each section element to the PAL stop it owns (`star-engine.js:4-10`). These are the five sections and their sector labels:
      `#index` → `var(--star-sirius)` (PAL 0.00), label `DEEP FIELD — OBSERVATION LOG · 2026`;
      `#work` → `var(--star-antares)` (0.18), label `01 — ANTARES SECTOR`;
      `#experience` → `var(--star-aldebaran)` (0.46), label `02 — ALDEBARAN SECTOR`;
      `#publications` → `var(--star-vega)` (0.72), label `03 — VEGA SECTOR`;
      `#contact` → `var(--star-sirius)` (1.00), label `04 — TRANSMISSION · SIRIUS SECTOR`.
      **⚠ Accent is section-driven, not plate-driven.** `V4:92-100` gives Work plate 002 an Aldebaran accent (`255,122,89`) *inside the Antares section* — that is a bug in the design file. Do **not** reproduce it faithfully. Using `var(--accent)` throughout resolves it and is why Task 2 can reorder plates for free.
      **⚠ `--gold-glow` (255,196,110) is the hero star and is NOT `--star-antares` (244,182,89).** Two different golds; never use `--gold-glow` in a section.

    **3 — Mission section (`#index`), inside `[data-field]` right after the hero.** Port `V4:53-66`:
      - The sector label `DEEP FIELD — OBSERVATION LOG · 2026` and the blob (`data-para="0.16"`, right-offset per `V4:54`).
      - **The one-liner — verbatim, locked, and already exactly right in the design (`V4:57`). Copy it character-for-character; do not reword, retitle, or "improve" it:**
        *Biotechnology MSc at DTU Copenhagen. I design and build the* **hardware, software and automation** *that free scientists from repetitive lab work.*
        The bolded fragment is a `.grad` span: `background-image:linear-gradient(90deg,#96c4ff,#f4b659 40%,#ff7a59 68%,#ab8cff); background-clip:text; -webkit-background-clip:text; color:transparent` — the PAL spectrum again. Paragraph type per `V4:57`: `font:300 clamp(28px,4.3vw,60px)/1.18 var(--font-display); letter-spacing:-0.02em; max-width:1050px; text-wrap:pretty`.
        This satisfies ROADMAP success criterion 4's second clause — the one-liner *introduces the page*. (Its first clause, the nav's CV/LinkedIn, shipped in 01-02.)
      - The four sector pills (`V4:59-62`) → `#work`, `#experience`, `#publications`, `#contact`. Base per `V4:59`: `font:400 11px var(--font-mono); letter-spacing:.2em; border:1px solid rgba(var(--accent),.35); border-radius:999px; padding:10px 20px; transition:border-color .3s ease, box-shadow .3s ease`. Each pill sets its own `--accent` to the section it points at, so the pill row previews the spectrum. Translate the `style-hover` into a real rule: `.pill:hover{ border-color:rgba(var(--accent),.8); box-shadow:0 0 18px rgba(var(--accent),.25) }`.
      - **Delete `V4:63`** — *"— SECTOR JUMPS ENGAGE A SHORT WARP BURST"*. The warp burst is Phase 2/4; shipping the copy would promise a behaviour this page does not have.
      - Port the `data-reveal` attributes as authored. **Add no CSS for them** (see constraints — `updateReveals()` is what sets `opacity:0`, and it is Phase 2).
  </action>

  <acceptance_criteria>
    - `site/index.html` contains the one-liner verbatim: `Biotechnology MSc at DTU Copenhagen. I design and build the` and `that free scientists from repetitive lab work.` with a `.grad` span wrapping exactly `hardware, software and automation`.
    - `site/index.html` contains sections with ids `index`, and anchors to `#work`, `#experience`, `#publications`, `#contact`.
    - `site/index.html` contains zero occurrences of `SECTOR JUMPS` (dead copy deleted).
    - `site/styles.css` contains `.section`, `--accent`, and `scroll-margin-top` bound to `var(--nav-h)`.
    - `site/styles.css` contains a `.pill:hover` rule; `site/index.html` contains zero `style-hover`.
    - `site/styles.css` contains **zero** `[data-reveal]` selectors (a hiding rule would ship an invisible page).
    - `site/index.html` contains `data-reveal` (attributes ported for Phase 2).
    - `site/styles.css` does not use `--gold-glow` inside any `.section`/`#index`/`#work`/`#experience`/`#publications`/`#contact` rule.
  </acceptance_criteria>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && fail=0; grep -q "Biotechnology MSc at DTU Copenhagen. I design and build the" site/index.html || { echo "FAIL: locked one-liner not verbatim"; fail=1; }; grep -q "that free scientists from repetitive lab work." site/index.html || { echo "FAIL: one-liner truncated"; fail=1; }; grep -q "hardware, software and automation" site/index.html || { echo "FAIL: gradient span missing"; fail=1; }; grep -q 'id="index"' site/index.html || { echo "FAIL: Mission section missing"; fail=1; }; grep -q "SECTOR JUMPS" site/index.html && { echo "FAIL: dead warp copy ported"; fail=1; }; grep -q "style-hover" site/index.html && { echo "FAIL: style-hover is not real HTML"; fail=1; }; grep -q -- "--accent" site/styles.css || { echo "FAIL: accent system missing"; fail=1; }; grep -q "\[data-reveal\]" site/styles.css && { echo "FAIL: CSS hides [data-reveal] - Phase 1 would ship an INVISIBLE page"; fail=1; }; grep -q "data-reveal" site/index.html || { echo "FAIL: data-reveal attributes not ported"; fail=1; }; grep -q ".pill:hover" site/styles.css || { echo "FAIL: pill hover not translated to real CSS"; fail=1; }; exit $fail</automated>
  </verify>

  <done>Scrolling past the hero reaches the Mission section: the verbatim one-liner with its spectrum gradient, four working sector pills that light on hover, and the section furniture (numeral, blob, label, rule) that every later section reuses via `--accent`.</done>
</task>

<task type="auto">
  <name>Task 2: Selected Work — 3 curated plates + the swappable media slot (MEDIA-01)</name>

  <files>site/index.html, site/styles.css, site/assets/media/README.md</files>

  <read_first>
    - `.planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html:69-104` — the Work section and its two plates. `:82` and `:94` are the media slots (**`aspect-ratio:16/9`** — read it directly and confirm). `:84-89` is the per-plate structure. `:92` is the even-plate alternation. `:87` is the lorem string to kill. `:88,100` are the warp CTAs to drop.
    - `../00_Profile/profile.md` — **the only content source.** Read: `:57` (MSc thesis — the dispenser), `:87-103` (the DALSA / OT-2 extended entry), `:193-204` (iGEM EndoSense), `:11` (**the iGEM wiki URL is mandatory whenever EndoSense is listed**), `:8` (expand acronyms at first mention), `:9` (add descriptors to non-obvious tools).
    - `.planning/phases/01-the-design-build/01-RESEARCH.md` — "Curated Content" (the wrong-placeholder table and the 3-plate recommendation with per-row `profile.md` traces) and the **"Media Slot Pattern (MEDIA-01)"** section, which carries the full `:has()` CSS and the reasoning against `:empty`.
    - `.planning/phases/01-the-design-build/01-PATTERNS.md` — "No Analog Found" (the media-slot mechanism is new work; the design supplies only the shape).
    - `site/styles.css` as left by Task 1 — reuse `.section` and `--accent`.
  </read_first>

  <action>
    **1 — Selected Work (`#work`, `--accent: var(--star-antares)`), 3 plates in IMPORTANCE order (D-03).** This deliberately **inverts the design's chronological order** (which leads with EndoSense). ROADMAP success criterion 3 says "most important items first", and CLAUDE.md says engineering/design is the primary identity with biotech a *hint* — leading with the wet-lab biosensor would invert the intended identity. Accent is section-driven, so reordering costs nothing.

      - **PLATE 001 · 2026 — Portable Precision Liquid Dispenser.** Source `profile.md:57`. The flagship: self-designed MSc thesis hitting design + hardware + automation + AI in one artefact — Fusion360 CAD, Arduino, electronics/motors, 3D printing, and a custom AI-assisted testing app using Design of Experiments. A modular, field-deployable device for accurate liquid dispensing in point-of-care diagnostics, pandemic monitoring and field research. **The year is 2026, not the design's 2025** (decision A8 — the thesis deadline is Aug 2026 and the MSc runs to Sep 2026). Media label: `[ device render / gif ]`.
      - **PLATE 002 · 2025 — Automated Gel Electrophoresis.** Source `profile.md:87-103`. Grade 12/12. A ~785-line custom Opentrons OT-2 Python protocol plus 5 Fusion360-designed 3D-printed components, developed with the full Double Diamond process including user interviews. **Lead with the quantifiable win: up to 144 samples per run, ~10 min per gel** — the most concrete automation result in the profile. Per `profile.md:8-9`, expand acronyms and add tool descriptors at first mention: "DALSA (DTU Arena for Life Science Automation)" and "Opentrons OT-2 liquid-handling robot". Media label: `[ project video / image ]`.
      - **PLATE 003 · 2024 — EndoSense.** Source `profile.md:193-204`. The **biotech hint**: a cell-free biosensor for detecting endocrine-disrupting compounds in drinking water; iGEM Gold Medal. **It must carry `https://2024.igem.wiki/DTU-Denmark/`** — `profile.md:11` mandates this URL whenever EndoSense is listed, because it is a public verifiable record. `target="_blank" rel="noopener"`. Media label: `[ project image ]`.
      - **Kill `Descriptio od endosese` (`V4:87`)** — obvious lorem. Every plate description is written from `profile.md`; nothing else.
      - Plate structure per `V4:84-89`: mono `PLATE 00N · YYYY` (`font:400 12px var(--font-mono); letter-spacing:.2em; color:rgba(var(--accent),.85)`) → `h3` (`font:400 clamp(36px,4.4vw,60px)/0.98 var(--font-display); letter-spacing:-.03em`) → `p` (`max-width:420px; font-size:17px; line-height:1.62; font-weight:300; color:var(--text-body)`) → the media slot. Alternation per `V4:92`: even plates get `flex-wrap:wrap-reverse; flex-direction:row-reverse` (a `.plate--flip` modifier).
      - **Drop the warp CTA** (`V4:88,100` — `OPEN PLATE — WARP JUMP →`), `data-open`, and `cursor:pointer`. There is nothing to open in Phase 1 and the design's `PLATES ARE CLICKABLE — WARP JUMP TO PROJECT PAGE` line (`V4:78`) must go with it. **Exception:** plate 003 keeps a real outbound link to the iGEM wiki, styled as the CTA pill — that is a working link, not a promise of a page this phase does not ship. Translate the CTA `style-hover` into `.plate__link:hover{ border-color:rgba(var(--accent),.9); box-shadow:0 0 20px rgba(var(--accent),.3) }`.

    **2 — The swappable media slot (MEDIA-01) in `site/styles.css`.** The design supplies the slot *shape* only; it hard-codes a placeholder and toggles nothing. The mechanism is new work and is **pure CSS — no JS, no config array, no build.**
      - `.media-slot` — `width:100%; aspect-ratio:16 / 9; border-radius:18px; overflow:hidden; margin:0; display:flex; align-items:center; justify-content:center`, carrying `data-para="0.05"` per `V4:82`.
      - **The ratio is `16/9`.** Read `V4:82` and `V4:94` and confirm: both say `aspect-ratio:16/9`. **STATE.md and the superseded 01-03 plan say "16:10" — that is drift D-2; `16/10` appears nowhere in either design file. The design wins.** Note the correction in the SUMMARY so STATE.md can be fixed.
      - **Empty state** — `.media-slot:not(:has(> img, > video))` gets the striped placeholder per `V4:82`: `background:repeating-linear-gradient(135deg, rgba(var(--accent),.09) 0 11px, rgba(var(--accent),.02) 11px 22px); border:1px dashed rgba(var(--accent),.32); box-shadow:0 30px 90px rgba(var(--accent),.10); transition:box-shadow .5s ease, border-color .5s ease`. Its `::after` renders `content:attr(data-label)` in `font-family:var(--font-mono); font-size:12px; letter-spacing:.14em; text-transform:uppercase; color:rgba(var(--accent),.65)`.
      - **Filled state** — `.media-slot > img, .media-slot > video { width:100%; height:100%; object-fit:cover; display:block }`. The placeholder chrome disappears automatically because the `:not(:has(...))` guard stops matching.
      - **Use `:has()`, never `:empty`.** `:empty` is a trap here: it matches only elements with *no child nodes at all*, and **whitespace counts as a text node** — so normally-formatted markup with the closing tag on its own line is not `:empty` and the placeholder would silently vanish. `:has()` is immune to whitespace and has been Baseline widely available across browsers since December 2023.
      - Translate the slot `style-hover` (`V4:82`) into `.media-slot:hover{ box-shadow:0 40px 110px rgba(var(--accent),.24); border-color:rgba(var(--accent),.7) }`.

    **3 — `site/assets/media/README.md`** — the documented folder half of MEDIA-01. State: the folder is `site/assets/media/`; filenames are lowercase-hyphenated and match the project slug; stills are `.jpg`/`.webp`, clips are `.mp4` (H.264) and every video needs a `poster` still; **filling a slot is a drop-in plus one line** — paste a single `<img src="./assets/media/<name>.jpg" alt="...">` (or a `<video ... muted loop playsinline autoplay>`) inside the `<figure class="media-slot">` and the placeholder disappears with no CSS or JS change. Show the exact one-line form for both. State the locked rule prominently: **no stock or AI-generated imagery, ever.** Note that encoding/sizing is formally Phase 4 (MEDIA-02) — Phase 1 only guarantees the slot accepts the file.
  </action>

  <acceptance_criteria>
    - `site/index.html` contains exactly **3** occurrences of `PLATE 00`, in source order `PLATE 001 · 2026`, `PLATE 002 · 2025`, `PLATE 003 · 2024`.
    - `site/index.html` contains `2024.igem.wiki/DTU-Denmark` (mandated by `profile.md:11`) with `rel="noopener"`.
    - `site/index.html` contains zero occurrences of `Descriptio`, `WARP JUMP`, `data-open`, `PLATES ARE CLICKABLE`, `Liquid Handler` (the design's truncated title).
    - `site/index.html` contains `144` and `Fusion360` (the quantified DALSA win and the CAD tool).
    - `site/styles.css` contains `aspect-ratio: 16 / 9` and zero occurrences of `16/10` or `16 / 10`.
    - `site/styles.css` contains `:not(:has(> img, > video))` and `content: attr(data-label)`; it contains zero `.media-slot:empty` selectors.
    - `site/index.html` contains 3 `class="media-slot"` figures, each with a `data-label` attribute.
    - `site/assets/media/README.md` exists and contains `media-slot`, `assets/media/`, and the no-stock/AI rule.
    - Pasting `<img src="./assets/media/x.jpg" alt="x">` into any slot removes the striped placeholder with **no** CSS/JS edit (verify by temporary insertion, then revert).
  </acceptance_criteria>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && fail=0; [ "$(grep -c 'PLATE 00' site/index.html)" = "3" ] || { echo "FAIL: need exactly 3 plates (ship small)"; fail=1; }; grep -q "PLATE 001 · 2026" site/index.html || { echo "FAIL: plate 001 must be the dispenser at 2026 (A8, not the design's 2025)"; fail=1; }; grep -q "PLATE 003 · 2024" site/index.html || { echo "FAIL: plate 003 EndoSense missing/misordered"; fail=1; }; grep -q "2024.igem.wiki/DTU-Denmark" site/index.html || { echo "FAIL: mandatory iGEM wiki URL missing (profile.md:11)"; fail=1; }; for b in Descriptio "WARP JUMP" data-open "PLATES ARE CLICKABLE" "Liquid Handler"; do grep -q "$b" site/index.html && { echo "FAIL: design placeholder/deferred machinery ported: $b"; fail=1; }; done; grep -Eq "aspect-ratio:\s*16\s*/\s*9" site/styles.css || { echo "FAIL: media slot must be 16/9 per design (drift D-2: 16:10 is wrong)"; fail=1; }; grep -Eq "16\s*/\s*10" site/styles.css && { echo "FAIL: 16/10 appears nowhere in the design"; fail=1; }; grep -q ":not(:has(> img, > video))" site/styles.css || { echo "FAIL: MEDIA-01 :has() swap mechanism missing"; fail=1; }; grep -q "media-slot:empty" site/styles.css && { echo "FAIL: :empty is a whitespace trap - use :has()"; fail=1; }; grep -q "attr(data-label)" site/styles.css || { echo "FAIL: slot label missing"; fail=1; }; [ "$(grep -c 'class="media-slot"' site/index.html)" = "3" ] || { echo "FAIL: expected 3 media slots"; fail=1; }; [ -f site/assets/media/README.md ] || { echo "FAIL: MEDIA-01 documented folder missing"; fail=1; }; grep -q "media-slot" site/assets/media/README.md || { echo "FAIL: README does not document the one-line reference"; fail=1; }; exit $fail</automated>
  </verify>

  <done>Selected Work shows 3 plates in importance order with descriptions traced to `profile.md`, alternating layout, and 3 labelled striped 16/9 placeholders. Dropping a file into `site/assets/media/` and pasting one `<img>`/`<video>` line swaps a placeholder for the asset with no other change (MEDIA-01).</done>
</task>

<task type="auto">
  <name>Task 3: Experience (4 rows) + Publications (exactly 1 row) + Contact — every row traced to profile.md</name>

  <files>site/index.html, site/styles.css</files>

  <read_first>
    - `.planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html:106-178` — Experience (`:107-138`, spine + dot + 3-column baseline rows), Publications (`:141-161`, **two rows, and the border trick at `:151` vs `:155`**), Contact (`:164-171`), footer (`:173-178`).
    - `../00_Profile/profile.md` — **the only content source.** Read: `:155` (AGC Biologics Student Assistant — **the current job the design omits entirely**), `:52` (MSc), `:16` (**the mandated iGEM role phrasing**), `:193-195` (iGEM EndoSense), `:339` (**iGEM Gold Medal** in the Awards table), `:208-215` (Research Intern, CIBIO UniTrento), `:328` (**the full publication citation**), `:329` and `:28` (**"Contributing author", and the rule: do not list position number**), `:42` (email).
    - `.planning/phases/01-the-design-build/01-RESEARCH.md` — the "❌ Design placeholders that are WRONG" table and the Experience/Publications curation, including the **one-row border consequence**.
    - `.planning/phases/01-the-design-build/01-PATTERNS.md` — the `.exp` / `.pub` CSS blocks and the `style-hover` translation table.
    - `site/styles.css` as left by Tasks 1–2.
  </read_first>

  <action>
    **1 — Experience (`#experience`, `--accent: var(--star-aldebaran)`), 4 rows, reverse-chronological** (the design ships 3, two of which are wrong). CSS per PATTERNS: `.exp` (`position:relative; margin-top:clamp(44px,5.5vw,70px); padding-left:46px`), `.exp__spine` (`position:absolute; left:8px; top:10px; bottom:10px; width:1px` with the four-stop accent gradient from `V4:117`), `.exp__row` (`position:relative; display:flex; flex-wrap:wrap; align-items:baseline; gap:8px 40px; padding:clamp(20px,2.6vw,30px) 0`), `.exp__dot` (`position:absolute; left:-42px; top:calc(clamp(20px,2.6vw,30px) + .55em); width:9px; height:9px; border-radius:50%; background:rgb(var(--accent)); box-shadow:0 0 14px rgba(var(--accent),.9)`). Period span `flex:0 0 160px; font:400 14px var(--font-mono); letter-spacing:.06em`; `h4` `margin:0; flex:1 1 240px; font:400 clamp(24px,2.8vw,34px) var(--font-display); letter-spacing:-.02em; color:#fff`; right column `font-size:16px; font-weight:300; color:rgba(232,228,232,.6)`.

      The four rows, each traced:
      | Period | Role (`h4`) | Right column | Source |
      |---|---|---|---|
      | `2024 — Present` | `Student Assistant — PM Team` | `AGC Biologics, Copenhagen` | `profile.md:155` |
      | `2024 — 2026` | `MSc Biotechnology` | `DTU Copenhagen` | `profile.md:52` |
      | `2024` | `iGEM Wet Lab Lead — EndoSense Biosensor` | `iGEM Gold Medal` | `profile.md:16`, `:193`, `:339` |
      | `2023` | `Research Intern — CIBIO, UniTrento` | `EMBO publication` | `profile.md:208` |

      **Three corrections the design gets wrong — do not port them faithfully:**
      - **AGC Biologics is missing from the design entirely.** It is Sirio's *current job*; omitting it would be conspicuous to a recruiter. It leads the list.
      - `Hardware Lead — iGEM` (`V4:127`) is the **wrong role title**. `profile.md:16` mandates the exact phrasing **`iGEM Wet Lab Lead — EndoSense Biosensor`**. Use it verbatim. The medal is **Gold** (decision A7: the Awards table at `:339` and the CV guidelines at `:31` both say Gold Medal; the "type TBC" at `:195` is a stale contradiction outvoted two-to-one).
      - `Research Assistant / 2022 — 2024 / Lab automation` (`V4:132-134`) is **fabricated — no such role exists in `profile.md`.** Replace it with the real one: Research Intern at CIBIO, UniTrento, Feb–Sep 2023 (MD simulation + Western blot, not lab automation). Keep it: it is the *provenance* of the Publications entry, and dropping it leaves the EMBO row unexplained.

    **2 — Publications (`#publications`, `--accent: var(--star-vega)`), exactly ONE row.** `profile.md` supports exactly one publication.
      - **Delete the design's second row** (`V4:156-157`, `Part collection contribution — iGEM Registry · 2024`). **It has zero support in `profile.md`. Do not invent a second publication to fill the space.**
      - The single row, from `profile.md:328`: title ***Mapping cryptic phosphorylation sites in the human proteome***; role label **`Contributing author`**; venue `The EMBO Journal · 2025 · 44:6704–6731`; link `https://doi.org/10.1038/s44318-025-00567-1`.
      - **The design gets both fields wrong** (`V4:152-153`): the title `Cell-free diagnostics` and the label `— co-author` are both incorrect. Use the real title and **`Contributing author`** per `profile.md:28`, which also mandates ***do not list position number*** — so **no "10th of 20"**, even though `profile.md:329` records it internally.
      - **⚠ The one-row border consequence.** The design builds a shared rule set by putting `border-top` on row 1 (`:151`) and `border-top + border-bottom` on row 2 (`:155`). Collapsing to one row means **that row needs BOTH borders**, or the list renders with a hanging open edge. `.pub` = `display:flex; flex-wrap:wrap; justify-content:space-between; align-items:baseline; gap:8px 32px; padding:clamp(22px,2.8vw,32px) 14px; border-top:1px solid rgba(255,255,255,.1); border-bottom:1px solid rgba(255,255,255,.1); transition:background .4s ease`.
      - The `↗` in `V4:153` implies a link — make the row an `<a>` with `target="_blank" rel="noopener"` to the DOI. Translate the `style-hover` into `.pub:hover{ background:rgba(255,255,255,.03) }`.

    **3 — Contact (`#contact`, `--accent: var(--star-sirius)`) + footer.** Port `V4:164-171` and `V4:173-178` as-is: the sector label, `Let's build something.` (`font:300 clamp(46px,7vw,96px) var(--font-display)`), and the email `sirio.feltrin@gmail.com` — **this address is correct and locked** (the design at `V4:169` and `profile.md:42` agree; it is *not* the account address `airsiriomax@gmail.com`). Translate its `style-hover` into `.contact__email:hover{ text-shadow:0 0 22px rgba(var(--accent),.85); border-color:rgba(var(--accent),.9) }`. Footer: `SF — SIRIO VITTORIO FELTRIN` / `COPENHAGEN · 2026`, `font:400 11px var(--font-mono); letter-spacing:.18em`.

    **4 — Close the loop.** With all five sections present, the four Mission pills and the docked nav from 01-02 now have real targets. Verify every anchor lands with its heading clear of the 70px nav.
  </action>

  <acceptance_criteria>
    - `site/index.html` contains exactly **4** `class="exp__row"` occurrences and exactly **1** `class="pub"` occurrence.
    - `site/index.html` contains `AGC Biologics`, `Student Assistant — PM Team`, `iGEM Wet Lab Lead — EndoSense Biosensor`, `iGEM Gold Medal`, `Research Intern — CIBIO, UniTrento`, `MSc Biotechnology`.
    - `site/index.html` contains `Mapping cryptic phosphorylation sites in the human proteome`, `Contributing author`, `The EMBO Journal`, `10.1038/s44318-025-00567-1`.
    - `site/index.html` contains **zero** occurrences of: `Hardware Lead`, `Research Assistant`, `co-author`, `Cell-free diagnostics`, `Part collection contribution`, `iGEM Registry`, `10th of 20`, `2022 — 2024`.
    - `site/styles.css` `.pub` rule carries **both** `border-top` and `border-bottom` (one row otherwise hangs open).
    - `site/index.html` contains `sirio.feltrin@gmail.com` and zero occurrences of `airsiriomax`.
    - `site/index.html` contains sections with ids `work`, `experience`, `publications`, `contact`; every `target="_blank"` is paired with `rel="noopener"` (equal, non-zero counts).
    - `site/index.html` contains zero `style-hover`; `site/styles.css` contains `.pub:hover` and `.contact__email:hover`.
    - `site/index.html` contains zero occurrences of `data-proj`, `data-warpov`, `openProj`, `drawWarp`, `applyTrans` (Phase 2/4 machinery).
  </acceptance_criteria>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && fail=0; [ "$(grep -c 'class="exp__row"' site/index.html)" = "4" ] || { echo "FAIL: need exactly 4 experience rows"; fail=1; }; [ "$(grep -c 'class="pub"' site/index.html)" = "1" ] || { echo "FAIL: profile.md supports exactly ONE publication - do not invent a second"; fail=1; }; for s in "AGC Biologics" "Student Assistant — PM Team" "iGEM Wet Lab Lead — EndoSense Biosensor" "iGEM Gold Medal" "Research Intern — CIBIO, UniTrento" "Mapping cryptic phosphorylation sites in the human proteome" "Contributing author" "The EMBO Journal" "10.1038/s44318-025-00567-1" "sirio.feltrin@gmail.com"; do grep -q "$s" site/index.html || { echo "FAIL: missing required content: $s"; fail=1; }; done; for b in "Hardware Lead" "Research Assistant" "co-author" "Cell-free diagnostics" "Part collection contribution" "iGEM Registry" "10th of 20" "2022 — 2024" "airsiriomax" data-proj data-warpov openProj drawWarp applyTrans style-hover; do grep -q "$b" site/index.html && { echo "FAIL: fabricated content or deferred machinery present: $b"; fail=1; }; done; grep -A6 '\.pub\s*{' site/styles.css | grep -q "border-top" && grep -A6 '\.pub\s*{' site/styles.css | grep -q "border-bottom" || { echo "FAIL: single pub row needs BOTH borders or the list hangs open"; fail=1; }; for i in work experience publications contact; do grep -q "id=\"$i\"" site/index.html || { echo "FAIL: section #$i missing"; fail=1; }; done; tb=$(grep -o 'target="_blank"' site/index.html | wc -l); no=$(grep -o 'rel="noopener"' site/index.html | wc -l); [ "$tb" = "$no" ] && [ "$tb" != "0" ] || { echo "FAIL: target=_blank ($tb) != rel=noopener ($no)"; fail=1; }; exit $fail</automated>
  </verify>

  <done>Experience shows 4 reverse-chronological rows led by the current AGC Biologics job; Publications shows exactly one correctly-cited EMBO row linking to the DOI; Contact exposes the locked email. Every claim on the page traces to a line in `profile.md`, and no fabricated row survives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser ← GitHub Pages CDN | Static, read-only, public by intent. Still zero user input in this phase. |
| page → external content links | iGEM wiki and the EMBO DOI open in a new tab via `target="_blank"`. |
| page → `mailto:` | The contact address is exposed in markup by intent. |
| repo → `site/assets/media/` | A new first-party asset folder; MEDIA-01 defines what may be dropped in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | iGEM wiki + EMBO DOI anchors (`target="_blank"`) | mitigate | `rel="noopener"` on both, as the design already does on every external link. Task 3 asserts `target="_blank"` count equals `rel="noopener"` count. Mitigates reverse tabnabbing. |
| T-01-08 | Information disclosure | `mailto:sirio.feltrin@gmail.com` harvesting | accept | The address is intentionally public — a locked decision (STATE.md, confirmed by the user 2026-07-16). A contact form is explicitly v2 (`CONT-V2-02`). |
| T-01-09 | Tampering | `site/assets/media/` drop-in surface | mitigate | Slots accept **only first-party files committed to the repo**; the documented one-line reference is a relative `./assets/media/...` path. No remote or hot-linked `src` — that would add a third-party tampering boundary for free. The locked "no stock or AI-generated imagery, ever" rule is stated in the folder README. |
| T-01-10 | Tampering | Content injection / XSS | mitigate | All content is hand-authored static HTML with **no `innerHTML` and no user input** anywhere in Phase 1. The only DOM-writing code is the engine's typewriter, which uses `textContent` (`star-engine.js:103-104`) — this plan does not touch the engine. V5 (input validation) first becomes live in Phase 6 (ASKAI-04). |
| T-01-11 | Tampering | `[data-reveal]` styling | mitigate | Not a security threat but a **shipping-integrity gate** enforced identically: adding a CSS rule that hides `[data-reveal]` ships a blank page to every visitor. `site/styles.css` must contain zero `[data-reveal]` selectors (Task 1). |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No packages are installed by this plan.** RESEARCH's Package Legitimacy Audit table is empty by design — no `package.json`, no bundler, no dependency; the media slot is deliberately pure CSS (`:has()`) rather than a JS media loader precisely to avoid adding one. If any task would introduce a package-manager install, **stop and re-run the Package Legitimacy Gate protocol**. |

**Net:** ASVS L1 V2/V3/V4/V5/V6 remain structurally N/A for a read-only static page with no user input. V14 (configuration) is owned by 01-01's pipeline and is untouched here. The security obligation of this plan is to *preserve* `rel="noopener"` and keep the asset surface first-party.
</threat_model>

<artifacts_this_phase_produces>
Every symbol created by this plan. It consumes the tokens and shell from 01-02 (`--star-*`, `--font-*`, `--pad-section`, `--max-w`, `--nav-h`, `--text-hi`, `--text-body`, `[data-field]`, `applyPara()`) and must not redefine them.

**New file paths:** `site/assets/media/` (folder) · `site/assets/media/README.md` (the MEDIA-01 documented drop-in contract)

**CSS custom properties:** `--accent` (**new** — set per section to a PAL stop; every child reads `rgba(var(--accent), a)`. This is the whole reason the file is portable and plate reordering is free.)

**CSS classes:** `.section` · `.section__numeral` · `.section__blob` · `.section__label` · `.section__rule` · `.section__inner` · `.section__head` · `.grad` (the PAL-spectrum text gradient on the one-liner) · `.pill` · `.plate` · `.plate--flip` (even-plate `row-reverse` alternation) · `.plate__meta` · `.plate__body` · `.plate__link` · `.media-slot` (**MEDIA-01**) · `.exp` · `.exp__spine` · `.exp__row` · `.exp__dot` · `.pub` · `.contact` · `.contact__email` · `.footer`

**Hover rules translated from the design's non-functional `style-hover` attribute:** `.pill:hover` · `.media-slot:hover` · `.plate__link:hover` · `.pub:hover` · `.contact__email:hover`

**Data attributes:** `data-label` (**new** — the media slot's placeholder caption, rendered via `content:attr(data-label)`) · `data-reveal` (ported for Phase 2 MOTION-01; **no CSS may consume it in Phase 1**) · `data-para` (consumed by `applyPara()` from 01-02) · `data-jump`

**Section ids (completing the single-scroll home — FOUND-04):** `#index` (Mission) · `#work` (Selected Work) · `#experience` · `#publications` · `#contact`

**Section → accent map (PAL stops, `star-engine.js:4-10`):** `#index` → `--star-sirius` (0.00) · `#work` → `--star-antares` (0.18) · `#experience` → `--star-aldebaran` (0.46) · `#publications` → `--star-vega` (0.72) · `#contact` → `--star-sirius` (1.00)

**Recorded for Phase 4 (PROJ-01), so it is not re-litigated:** the V4 design *does* answer FOUND-04's "multi-page architecture" — with `position:fixed` `[data-proj]` overlay divs revealed by a warp transition (`openProj`/`applyTrans`/`drawWarp`, ~200 lines at `V4:181+` and `:394+`), not separate `.html` files. Phase 1 deliberately ships **persistent nav + single-scroll home only**; the mechanism decision is Phase 4's to make with this precedent in hand.
</artifacts_this_phase_produces>

<verification>
- `python -m http.server -d site 8080`: one continuous scroll from the hero through Mission → Work → Experience → Publications → Contact, each section carrying its own accent from the star spectrum.
- **The page is visible.** (If `site/styles.css` gained a `[data-reveal]` rule, every section would be at `opacity:0` — the single highest-consequence mistake available in this plan.)
- Every Mission pill and the docked nav land on their section with the heading clear of the 70px bar.
- Hover states are alive: pills glow, media slots lift, the publication row tints, the email flares. (If they are dead, `style-hover` was ported verbatim.)
- **Content audit against `profile.md`** — no claim on the page lacks a source line. Specifically: 3 plates, 4 experience rows, **1** publication; no `Research Assistant`; no `co-author`; no `10th of 20`; iGEM medal is Gold; the dispenser is 2026.
- MEDIA-01 live check: paste `<img src="./assets/media/test.jpg" alt="test">` into a slot — the stripes and label disappear and the image fills the 16/9 box with **no CSS or JS edit**. Revert.
- `grep -rn "app/" site/` returns nothing.
- Push and confirm the Actions run is green and the full page is live at `https://sirsirio.github.io/MyPortfolio/`.
- <human-check>Sirio reviews the rendered page end-to-end at the phase's end-of-phase verification gate (`workflow.human_verify_mode: end-of-phase`): section order and curation ("most important first", tight not exhaustive), the plate copy, and that nothing on the page overstates his record.</human-check>
</verification>

<success_criteria>
- [ ] A visitor scrolls one continuous page: hero → Mission → Selected Work → Experience → Publications → Contact (FOUND-04; ROADMAP criterion 1).
- [ ] The verbatim locked one-liner introduces the page with its spectrum gradient span (ROADMAP criterion 4, second clause).
- [ ] Selected Work = **3** plates in importance order (dispenser 2026 → gel electrophoresis 2025 → EndoSense 2024), each traced to `profile.md`, with the mandatory iGEM wiki link (ROADMAP criterion 3).
- [ ] Experience = **4** reverse-chronological rows led by the current AGC Biologics role the design omitted; the fabricated "Research Assistant 2022–2024" is gone; the iGEM role uses the mandated phrasing and Gold medal.
- [ ] Publications = **exactly 1** row, correctly titled and labelled `Contributing author` with no position number, linking to the DOI, and styled with both borders.
- [ ] Every media slot is a labelled striped **16/9** placeholder that swaps to a real asset via drop-in + one line, no re-engineering, documented in `site/assets/media/README.md` (MEDIA-01; ROADMAP criterion 6).
- [ ] Zero fabricated content, zero `style-hover`, zero `[data-reveal]` CSS, zero warp/overlay machinery, zero `app/` paths.
- [ ] The full page is live at `https://sirsirio.github.io/MyPortfolio/` via the 01-01 pipeline (ROADMAP criterion 5).
</success_criteria>

<output>
Create `.planning/phases/01-the-design-build/01-03-SUMMARY.md` when done.

**Record these corrections to the written record** (RESEARCH drift register) so STATE.md can be reconciled at phase close:
- **D-2** — the media slot is **16/9** (per `V4:82,94`), not the "16:10" recorded in STATE.md. The *decision* STATE.md captures (one box serving image OR video; page structure does not depend on assets) is unaffected — only the ratio literal was wrong.
- **A7** — the iGEM medal is **Gold** (`profile.md:339` + `:31` outrank the "type TBC" at `profile.md:195`); suggest correcting `profile.md:195`.
- **A8** — the liquid dispenser is **2026**, not the design's 2025.
- The pending todo `2026-06-17-hero-typewriter-rotating-role-animation` is **satisfied by the approved design** (8 terms with per-term gradients, article logic, and a reduced-motion fallback — all already in the engine, shipped by 01-02) and can be closed.
</output>
</content>
