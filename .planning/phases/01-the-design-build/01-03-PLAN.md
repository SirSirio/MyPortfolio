---
phase: 01-the-design-build
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/index.html
  - app/styles.css
autonomous: false
requirements: [FOUND-04, NAV-02, HERO-03]

must_haves:
  truths:
    - "Below the hero, a visitor scrolls through distinct sections: Mission, Selected Work (01), Experience (02), Publications (03), Contact"
    - "The Mission section states the identity one-liner and the discipline chips Design · Engineering · Automation · Biotech"
    - "Selected Work shows curated real project cards, each with a striped 16:10 media placeholder and a Space Mono caption (no stock/AI imagery)"
    - "Experience shows a curated real timeline; Publications shows the real EMBO paper and the iGEM contribution"
    - "Each section owns its star color (Work=Antares gold, Experience=Aldebaran orange, Publications=Vega violet, Contact=Sirius blue) and carries its mono section number"
    - "The Contact section shows the working email link sirio.feltrin@gmail.com and the COPENHAGEN · 2026 footer"
    - "The Mission sector-jump chips and the hero scroll-hint smooth-scroll to their target sections"
  artifacts:
    - path: "app/index.html"
      provides: "Mission + Work + Experience + Publications + Contact + footer sections with curated real content and section anchors (#index, #work, #experience, #publications, #contact)"
      contains: "id=\"work\""
    - path: "app/styles.css"
      provides: "Section, glass-card, project-card, striped-placeholder, timeline, publication-row styling"
      contains: "repeating-linear-gradient"
  key_links:
    - from: "app/index.html Mission sector chips"
      to: "#work / #experience / #publications / #contact"
      via: "anchor hrefs resolving to section ids"
      pattern: "href=\"#work\""
    - from: "app/index.html Contact"
      to: "mailto:sirio.feltrin@gmail.com"
      via: "email link"
      pattern: "mailto:sirio\\.feltrin@gmail\\.com"
---

<objective>
Fill the long scroll below the hero with the real, curated portfolio: a Mission one-liner + discipline chips, Selected Work (01) project cards, Experience (02) timeline, Publications (03) list, and a Contact (04) close — each section owning its star color and mono number, ported faithfully from the V4 layout, with content curated from Sirio's master profile (most important items first, page kept tight, not exhaustive). This completes the phase goal: the full Deep Field × Hyperlight page, live, with real content.

Purpose: Delivers FOUND-04 (single immersive scroll home with distinct sections), completes NAV-02 (section anchors now resolve to real sections with smooth scroll), and finishes HERO-03's identity statement via the Mission one-liner. Image slots stay as labeled striped placeholders (user supplies media later).
Output: The complete home page below the hero, deployable, with curated real content and working in-page navigation.
</objective>

<execution_context>
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/workflows/execute-plan.md
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-the-design-build/01-CONTEXT.md
@.planning/phases/01-the-design-build/SKELETON.md
@.planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html
@../00_Profile/profile.md
</context>

## Artifacts this phase produces

Symbols this plan creates (exclude from drift verification):

- Section elements in `app/index.html`: `#index` (Mission), `#work`, `#experience`, `#publications`, `#contact`, and a footer
- CSS classes in `app/styles.css`: `.section`, `.section__eyebrow`, `.section__title`, `.chips`, `.project-card`, `.project-card__media` (striped placeholder), `.project-card__body`, `.tags`, `.timeline`, `.timeline__row`, `.pub-row`, `.contact`, `.footer`
- Reuses (does NOT redefine) the design tokens and pill/nav/hero classes created in Plan 01-02

<tasks>

<task type="auto">
  <name>Task 1: Mission + Selected Work (01) with curated real projects</name>
  <files>app/index.html, app/styles.css</files>
  <read_first>
    - app/index.html (hero + nav from Plan 01-02; append sections after the hero, before closing wrappers; keep the fixed deep-field canvas and spine as the shared background)
    - app/styles.css (reuse the tokens --star-antares etc. and glass values defined in Plan 01-02; add section/card classes)
    - .planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html (source of truth for the Mission section markup, the Work section number/heading/sector label, and the project-card + striped-placeholder structure — port the markup, drop data-open/data-reveal/data-para)
    - ../00_Profile/profile.md (source of truth for curated content: MSc thesis portable liquid dispenser; DALSA Opentrons OT-2 automated gel electrophoresis; iGEM EndoSense)
    - .planning/phases/01-the-design-build/01-CONTEXT.md (curation rule: most important first, tight; every card keeps one empty 16:10 striped media slot with a Space Mono caption; no stock/AI imagery)
  </read_first>
  <action>
    Append the Mission section `<section id="index" class="section">` after the hero: an eyebrow mono line (e.g. `PORTFOLIO · COPENHAGEN · 2026`), the verbatim identity one-liner from brief §1 — "Biotechnology MSc at DTU Copenhagen. I design and build the hardware, software and automation that free scientists from repetitive lab work." (this completes HERO-03) — with the phrase "hardware, software and automation" carrying the multi-stop spectrum gradient text as in V4, and a `.chips` row reading `Design · Engineering · Automation · Biotech`. Include the sector-jump chip links to `#work`, `#experience`, `#publications`, `#contact` (mono pills, each tinted to its section color) — these are the in-page nav that completes NAV-02. Do NOT include the V4 "WARP BURST" `data-jump` behavior (deferred); the chips are plain smooth-scroll anchors.

    Append the Work section `<section id="work" class="section">` (Antares gold, `--star-antares`): the sector label `01 — ANTARES SECTOR`, the giant faint outlined `01` numeral, heading "Selected Work". Then THREE curated `.project-card` articles (most important first, alternating image side per the V4 rhythm — card 1 image-left, card 2 image-right, card 3 image-left). Each card = a 16:10 `.project-card__media` striped placeholder (`repeating-linear-gradient(135deg, rgba(<section-color>,0.09) 0 11px, rgba(<section-color>,0.02) 11px 22px)` + `1px dashed rgba(<section-color>,0.32)`) with a Space Mono caption, plus a body with a mono `PLATE 00X · YEAR`, an `<h3>` title, a one-line description, and mono tag pills. Use this curated real content (from profile.md; replace the V4 placeholder text like "Descriptio od endosese"):
    - PLATE 001 · 2026 — "Portable Liquid Dispenser" — "MSc thesis: a portable, field-deployable precision liquid-dispensing instrument for point-of-care diagnostics — self-designed CAD, Arduino electronics, 3D-printed hardware, and an AI-assisted testing app." Tags: Product Design, Hardware, Firmware. Media caption: `[ device render / demo ]`.
    - PLATE 002 · 2025 — "Automated Gel Electrophoresis" — "Design-thinking-led lab automation on the Opentrons OT-2: a ~785-line Python protocol plus 5 custom 3D-printed components automating gel electrophoresis end-to-end — up to 144 samples per run." Tags: Automation, Python, Hardware. Media caption: `[ ot-2 / cad ]`.
    - PLATE 003 · 2024 — "EndoSense" — "iGEM Wet Lab Lead (Gold medal): a cell-free biosensor detecting endocrine-disrupting compounds in drinking water using allosteric transcription factors and an RNA-aptamer readout." Tags: Biology, Cell-free, Hardware. Media caption: `[ biosensor / assay ]`.
    Keep the media slots EMPTY (labeled placeholders only). Do NOT add project deep-dive links or `data-open` — deep-dive pages are Phase 5; the cards are display-only in this phase.

    Add `.section`, `.section__eyebrow`, `.section__title`, `.chips`, `.project-card`, `.project-card__media`, `.project-card__body`, `.tags` styling to `app/styles.css`, reusing the tokens. Follow the V4 Work-card structure (borderless card with a striped media plate) and add mobile stacking (cards become single-column below ~760px).
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const h=f.readFileSync('app/index.html','utf8');const c=f.readFileSync('app/styles.css','utf8');['id=\"index\"','id=\"work\"','Portable Liquid Dispenser','Automated Gel Electrophoresis','EndoSense','Design · Engineering · Automation · Biotech','free scientists from repetitive lab work'].forEach(k=>{if(!h.includes(k))throw new Error('missing '+k)});if(/Descriptio od endosese/.test(h))throw new Error('V4 placeholder text still present');if(/data-open=/.test(h))throw new Error('deep-dive data-open must not be present (Phase 5)');if(!/repeating-linear-gradient/.test(c+h))throw new Error('striped placeholder missing');['#work','#experience','#publications','#contact'].forEach(a=>{if(!h.includes('href=\"'+a+'\"'))throw new Error('sector jump anchor missing '+a)});console.log('mission+work OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/index.html` contains `id="index"` and `id="work"` sections
    - The verbatim one-liner "...free scientists from repetitive lab work" and the chips "Design · Engineering · Automation · Biotech" are present
    - Three curated cards are present: "Portable Liquid Dispenser", "Automated Gel Electrophoresis", "EndoSense"
    - The V4 placeholder string "Descriptio od endosese" is absent
    - No `data-open=` attribute exists (project deep-dive links deferred to Phase 5)
    - Each of `#work`, `#experience`, `#publications`, `#contact` has a matching sector-jump `href` in the Mission section
    - A `repeating-linear-gradient` striped placeholder rule exists
  </acceptance_criteria>
  <done>The Mission section states the identity one-liner + chips + working sector-jump anchors, and Selected Work shows three curated real project cards with empty labeled striped media slots and no deferred deep-dive wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Experience (02) + Publications (03) + Contact (04) + footer with curated real content</name>
  <files>app/index.html, app/styles.css</files>
  <read_first>
    - app/index.html (append after the Work section from Task 1)
    - app/styles.css (reuse tokens; add timeline / pub-row / contact / footer classes)
    - .planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html (source of truth for the Experience timeline rows, Publications list rows, Contact centered block, and footer — port markup, drop data-reveal/data-para)
    - ../00_Profile/profile.md (curated content: AGC Biologics student assistant; MSc Biotechnology DTU; iGEM Wet Lab Lead Gold; UniTrento research intern / EMBO paper; Publications = EMBO 2025 + iGEM Registry)
    - Sirio Portfolio - Fable Brief.md (§1 Contact block: "Let's build something." + email + COPENHAGEN · 2026)
  </read_first>
  <action>
    Append the Experience section `<section id="experience" class="section">` (Aldebaran orange, `--star-aldebaran`): sector label `02 — ALDEBARAN SECTOR`, faint `02` numeral, heading "Experience", and a curated timeline (`.timeline` with a left accent line and dot markers as in V4). Curated real rows (most important / most recent first, kept tight — 4 rows), each = mono date · role (Space Grotesk) · org (muted):
    - `2024 — Present` · "Student Assistant — Design, Automation & Reporting" · "AGC Biologics, Copenhagen"
    - `2024 — 2026` · "MSc Biotechnology" · "DTU Copenhagen"
    - `2024` · "iGEM Wet Lab Lead — EndoSense" · "Gold medal"
    - `2023` · "Research Intern — SMAD2 phosphorylation" · "University of Trento, CIBIO"

    Append the Publications section `<section id="publications" class="section">` (Vega violet, `--star-vega`): sector label `03 — VEGA SECTOR`, faint `03`, heading "Publications", and a list of real rows (title left; mono source · year right, with the `↗` glyph as in V4). Curated real rows:
    - "Mapping cryptic phosphorylation sites in the human proteome <span>— contributing author</span>" · `EMBO Journal · 2025 ↗`
    - "EndoSense — cell-free EDC biosensor (part collection)" · `iGEM Registry · 2024 ↗`
    Keep these as display rows in this phase (the `↗` is the design's visual cue). Do NOT wire verified external hyperlinks yet — verified external project/publication links are LINK-03 (Phase 5). No `target=_blank` external anchors are added here.

    Append the Contact section `<section id="contact" class="section contact">` (Sirius blue, `--star-sirius`): sector label `04 — TRANSMISSION · SIRIUS SECTOR`, the centered heading "Let's build something.", and the working email link `<a href="mailto:sirio.feltrin@gmail.com">sirio.feltrin@gmail.com</a>` styled in Space Mono. Then a `<footer class="footer">` with `SF — SIRIO VITTORIO FELTRIN` and `COPENHAGEN · 2026`.

    Add `.timeline`, `.timeline__row`, `.pub-row`, `.contact`, `.footer` styling to `app/styles.css` reusing tokens, with mobile stacking (timeline date/role/org stack; pub rows wrap).
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const h=f.readFileSync('app/index.html','utf8');['id=\"experience\"','id=\"publications\"','id=\"contact\"','AGC Biologics','MSc Biotechnology','iGEM Wet Lab Lead','cryptic phosphorylation','iGEM Registry','Let\\'s build something.','mailto:sirio.feltrin@gmail.com','COPENHAGEN · 2026'].forEach(k=>{if(!h.includes(k))throw new Error('missing '+k)});if(/Cell-free diagnostics/.test(h))throw new Error('V4 wrong publication placeholder still present');if(/data-reveal=|data-para=|data-warpov/.test(h))throw new Error('deferred motion/warp markup must not be present');console.log('exp+pub+contact OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/index.html` contains `id="experience"`, `id="publications"`, and `id="contact"` sections
    - Experience rows include "AGC Biologics", "MSc Biotechnology", "iGEM Wet Lab Lead", and "Research Intern — SMAD2 phosphorylation"
    - Publications include "Mapping cryptic phosphorylation sites in the human proteome" (EMBO · 2025) and "iGEM Registry · 2024"; the wrong V4 placeholder "Cell-free diagnostics" is absent
    - Contact contains the heading "Let's build something." and a working `mailto:sirio.feltrin@gmail.com` link
    - The footer contains `COPENHAGEN · 2026`
    - No `data-reveal=`, `data-para=`, or `data-warpov` markup is present (deferred to Phases 4/5)
  </acceptance_criteria>
  <done>Experience, Publications, and Contact carry curated real content; the email link works; the footer closes the page; and no deferred reveal/parallax/warp markup leaked in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the full curated page + responsive scroll (human-verify)</name>
  <files>(none — visual verification of the deployed page)</files>
  <action>
    Human-verify checkpoint: Claude has completed the full content scroll and pushed (the Plan 01-01 pipeline redeploys automatically). The developer confirms the section order, curated content accuracy, striped placeholders, smooth-scroll anchors, working email, and mobile stacking via the steps in how-to-verify. No further Claude code changes are needed for this task; it gates on the developer's approval.
  </action>
  <what-built>
    The full Deep Field × Hyperlight home page is complete: below the gold-star hero, a single immersive scroll runs Mission -> Selected Work (3 curated cards) -> Experience (curated timeline) -> Publications (real EMBO + iGEM) -> Contact, each section in its star color, with striped media placeholders and a working email. Pushing to the repo redeployed via the Plan 01-01 pipeline.
  </what-built>
  <how-to-verify>
    1. Open the live URL (or `python -m http.server -d app 8080`).
    2. Scroll the full page top to bottom: confirm distinct sections Mission, Selected Work (gold), Experience (orange), Publications (violet), Contact (blue), and the footer.
    3. Read the content: confirm the three Work cards, the four Experience rows, and the two Publications are the real curated items (not "Descriptio od endosese" / "Cell-free diagnostics").
    4. Confirm every project media slot is an empty striped placeholder with a Space Mono caption (no stock/AI images).
    5. In the Mission section click each sector chip (01 Work, 02 Experience, 03 Publications, 04 Contact) and the hero SCROLL hint — confirm each smooth-scrolls to the right section.
    6. Click the Contact email — confirm it opens a mail composer to sirio.feltrin@gmail.com.
    7. Resize to mobile width: confirm cards/timeline/pub-rows stack into a single column, type stays large and legible, nothing overflows horizontally.
    8. Confirm the left spectrum spine and the deep-field background persist behind all sections.
  </how-to-verify>
  <verify><automated>MISSING — full-page layout, section colors, and responsive stacking are visual properties confirmed by the developer (content-string and anchor greps run in Tasks 1-2)</automated></verify>
  <acceptance_criteria>
    - Developer confirms all five sections + footer render in order with the correct star colors
    - Developer confirms curated content accuracy and empty labeled striped media slots
    - Developer confirms sector chips + scroll hint smooth-scroll, the email link works, and mobile stacks cleanly
  </acceptance_criteria>
  <resume-signal>Type "approved", or describe issues (wrong content, broken anchor, layout overflow on mobile, missing section).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page -> mail client | `mailto:` link hands the address to the OS mail handler (no network input accepted by the site) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Email address exposed in `mailto:` (scrapeable) | accept | The email is intentionally public (it is in the brief and CV); a portfolio contact address is meant to be reachable |
| T-03-02 | Tampering | (Deferred) external publication/project links | accept | No external `target=_blank` links are added in this phase; verified external links are LINK-03 in Phase 5 where `rel="noopener"` will be enforced |
| T-03-SC | Tampering | package installs (npm/pip/cargo) | accept | No package installs in this plan (static HTML/CSS only) — legitimacy gate not applicable |
</threat_model>

<verification>
- Structural greps confirm all five sections, curated content strings, working mailto, and absence of V4 placeholder/deferred markup (task automated checks).
- Developer confirms the full scroll, curated content accuracy, smooth-scroll anchors, striped placeholders, and mobile stacking on the deployed site.
</verification>

<success_criteria>
- FOUND-04: A single immersive scroll home with distinct sections (Mission, Work, Experience, Publications, Contact) plus persistent nav is in place.
- NAV-02: Section anchors resolve to real sections and smooth-scroll (sector chips + scroll hint).
- HERO-03: The Mission one-liner states the identity in one line.
- Sections are filled with real, curated portfolio content (most important first, tight), with labeled striped media placeholders and a working contact email — completing the Phase 1 goal.
</success_criteria>

<output>
Create `.planning/phases/01-the-design-build/01-03-SUMMARY.md` when done. Note which projects/experience/publications were curated so Phase 4 (Story) and Phase 5 (Showcase) can extend them.
</output>
