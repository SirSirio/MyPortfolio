---
phase: 01-the-design-build
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/index.html
  - app/styles.css
  - app/main.js
  - app/star-engine.js
autonomous: false
requirements: [FOUND-03, HERO-01, HERO-02, HERO-03, HERO-04, NAV-02, LINK-01, LINK-02]

must_haves:
  truths:
    - "On load the hero shows a living gold star (glow 255,196,110 / core 255,236,200) breathing on a ~20s cycle"
    - "The star is pinned to the optical centre of the 'o' in the wordmark Sirio (the two i's are dotless with glowing orbs)"
    - "A typewriter under the name cycles the roles Engineer / Designer / Developer / Biotechnologist / Product Maker / Lab Automator / Data Analyst / AI enthusiast with a blinking caret"
    - "The i-dot orbs run one eased orbit each time the star ignites (brightness crosses ~0.45)"
    - "A fixed deep-field starfield behind the page parallaxes as the visitor scrolls"
    - "A persistent top nav shows SF monogram + name, a LinkedIn glass pill, and a View CV primary pill; both links work"
    - "When prefers-reduced-motion is set, the star freezes at a bright frame and the typewriter shows a single static role"
  artifacts:
    - path: "app/index.html"
      provides: "Hero + nav markup with the star DOM contract (data-star, data-planet, dotless-i spans, transparent-o span, data-art/data-typer, hero canvas, fixed deep-field canvas, spectrum spine)"
      contains: "data-typer"
    - path: "app/styles.css"
      provides: "Design tokens (FOUND-03) + hero/nav/pill/spine styling"
      contains: "--star-antares"
    - path: "app/main.js"
      provides: "Boots HeroStar (gold) + deep-field parallax loop + reduced-motion + smooth-scroll"
      contains: "new HeroStar"
    - path: "app/star-engine.js"
      provides: "HeroStar using the gold palette"
      contains: "255, 196, 110"
  key_links:
    - from: "app/main.js"
      to: "app/star-engine.js"
      via: "import HeroStar + engine helpers"
      pattern: "import\\s*\\{[^}]*HeroStar[^}]*\\}\\s*from\\s*'\\./star-engine\\.js'"
    - from: "app/main.js"
      to: "app/index.html hero"
      via: "new HeroStar(heroEl) driving the [data-star] layer"
      pattern: "new HeroStar"
    - from: "app/index.html nav"
      to: "https://canva.link/5a5yj5bdg78axhv"
      via: "View CV pill href"
      pattern: "canva\\.link/5a5yj5bdg78axhv"
---

<objective>
Deliver the signature wow slice: replace the minimal skeleton page with the real Deep Field hero — the living GOLD Sirio Star wordmark (breathing star as the 'o', dotless-i orbs, typewriter), a fixed deep-field parallax starfield background, the spectrum spine, and the persistent nav with working View CV + LinkedIn actions. After this plan a visitor opening the URL immediately gets the "wow — this person can build something impressive" moment.

Purpose: This is the core identity piece (HERO-01..04) and the design-token foundation (FOUND-03) every later section reuses. The hero is ported from the approved `Sirio Star.dc.html` motion into a plain static page and dropped into the V4 hero slot in place of the mockup.
Output: A deployable page showing only the hero (content sections arrive in Plan 01-03), with the living gold star running on load and reacting to scroll.
</objective>

<execution_context>
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/workflows/execute-plan.md
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-the-design-build/01-CONTEXT.md
@.planning/phases/01-the-design-build/SKELETON.md
@.planning/phases/01-the-design-build/design/Sirio Star.dc.html
@.planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html
@.planning/phases/01-the-design-build/design/star-engine.js
</context>

## Artifacts this phase produces

Symbols this plan creates (exclude from drift verification):

- CSS custom properties in `app/styles.css`: `--bg-0`, `--star-sirius`, `--star-antares`, `--star-aldebaran`, `--star-vega`, `--gold-glow`, `--gold-core`, `--text-hi`, `--text-body`, `--glass-bg`, `--glass-border`, `--glass-blur`, `--font-display`, `--font-body`, `--font-mono`, plus a type-scale/spacing set
- CSS classes: `.nav`, `.nav__brand`, `.nav__monogram`, `.pill`, `.pill--primary`, `.pill--glass`, `.hero`, `.hero__canvas`, `.wordmark`, `.hero__typer`, `.deep-field`, `.spine`, `.scroll-hint`, `.tag`
- DOM contract attributes (consumed by main.js): `[data-star]`, `[data-planet="near"]`, `[data-planet="far"]`, `[data-art]`, `[data-typer]`, the wordmark `<span>` set (two `ı` U+0131, one transparent `o`), `.hero > canvas`, `canvas[data-deep]`
- JS in `app/main.js`: a `boot()` entry, a `DeepField` renderer (or `drawDeep` function) + rAF loop
- Gold palette applied to `HeroStar` in `app/star-engine.js`

<tasks>

<task type="auto">
  <name>Task 1: Design tokens + hero/nav markup with the star DOM contract</name>
  <files>app/index.html, app/styles.css</files>
  <read_first>
    - app/index.html (current minimal skeleton from Plan 01-01 — replace its body)
    - app/styles.css (current minimal reset from Plan 01-01 — extend it)
    - .planning/phases/01-the-design-build/design/Sirio Star.dc.html (source of truth for the hero markup: dotless-i wordmark, transparent-o, [data-star] nested spans, planet divs, typewriter <p>, COPENHAGEN tag)
    - .planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html (source of truth for the fixed deep-field canvas, spectrum spine, nav pills, page background gradient, hero section framing)
    - .planning/phases/01-the-design-build/design/star-engine.js (read HeroStar._startType and _measure to see EXACTLY which selectors the JS expects: [data-star], [data-planet="near"|"far"], h1>span text 'ı' and 'o', [data-art], [data-typer])
    - Sirio Portfolio - Fable Brief.md (§1 verbatim identity: name, monogram SF, COPENHAGEN · 2026 tag, email, LinkedIn, CV link; §2 visual language; §3 layout notes)
  </read_first>
  <action>
    Establish the design-token system (FOUND-03) at the top of `app/styles.css` on `:root`. Define the four star colors as `--star-sirius:150,196,255`, `--star-antares:244,182,89`, `--star-aldebaran:255,122,89`, `--star-vega:171,140,255` (bare R,G,B triplets so they can be used inside `rgba(var(--x),a)`), the gold-hero pair `--gold-glow:255,196,110` and `--gold-core:255,236,200`, background base `--bg-0:#04050c`, heading `--text-hi:#eef2ff`, body `--text-body:rgba(228,232,244,0.76)`, glass surface `--glass-bg:rgba(255,255,255,0.04)`, `--glass-border:rgba(255,255,255,0.1)`, `--glass-blur:22px`, the three fonts `--font-display:'Space Grotesk'`, `--font-body:'Hanken Grotesk'`, `--font-mono:'Space Mono'`, plus a small type-scale and spacing set. Set the page background to the V4 vertical gradient `linear-gradient(180deg,#04050c 0%,#0c0a09 24%,#110b0a 46%,#100a14 68%,#080a16 86%,#05060d 100%)` and `html{scroll-behavior:smooth}`.

    Rewrite `app/index.html` body to the Deep Field shell (port from the V4 file, converting DesignCraft syntax to plain static HTML — there is no `support.js`, so drop `<x-dc>`, `<helmet>`, `data-screen-label`, and convert every `style-hover="..."` attribute into a real CSS `:hover` rule in styles.css):
    - A fixed full-viewport `<canvas data-deep>` at `z-index:0` behind everything.
    - The 5px `.spine` element with the spectrum gradient `linear-gradient(180deg,#96c4ff 0%,#f4b659 18%,#ff7a59 44%,#ab8cff 70%,#96c4ff 100%)` running the full page height at the left edge.
    - A persistent top `.nav` (NAV-02, HERO-04, LINK-01, LINK-02): left = `.nav__monogram` box reading `SF` + name `Sirio Feltrin`; right = a `.pill--glass` linking to `https://www.linkedin.com/in/sirio-vittorio-feltrin/` (text "LinkedIn") and a `.pill--primary` linking to `https://canva.link/5a5yj5bdg78axhv` (text "View CV"). Both external links use `target="_blank"` and `rel="noopener"`.
    - The hero `<section class="hero" id="top">` (min-height:100vh) containing, in this order: its own `<canvas class="hero__canvas">` (absolute, inset:0) for the star's starfield/rings; the two orb divs `<div data-planet="far">` and `<div data-planet="near">`; the `<div data-star>` layer with the nested glow/glass/dashed-ring/core spans copied from the Star file (keep the `--halo-op`, `--glass-op`, `--ring-op`, `--core`, `--core-blur`, `--glow` CSS-var references intact); the eyebrow "Hello, I am"; the `<h1 class="wordmark">` rendering `S<span>ı</span>r<span>ı</span><span>o</span>` where the two i-spans contain the dotless character U+0131 (`&#305;`) and the final `o` span is `color:transparent` (the live star shows through); the typewriter `<p>` containing `I am a` then an inline wrapper with `<span data-art></span>` (holds the article " a"/" an"), `<span data-typer></span>` (holds the role text, has the gradient background-clip text style), and the blinking caret span; and the bottom-left `.tag` reading `COPENHAGEN · 2026`. Add a `.scroll-hint` anchor at the bottom linking to `#index` (its target section is created in Plan 01-03; the anchor is inert-safe until then).

    Add hero/nav styling to `app/styles.css`: `.wordmark` uses `--font-display` weight 300, `font-size:clamp(58px,12.5vw,184px)`, `line-height:0.98`, `letter-spacing:-0.04em`, color `--text-hi`; `.pill--primary` = gradient of the star color with dark text `#04121a` and a colored glow shadow; `.pill--glass` = frosted glass (`--glass-bg`, `--glass-border`, backdrop blur) with light text; nav layout matches the V4 top bar. Keep the `@keyframes blink`, `spin`, `spinRev`, `orbit` used by the star spans.

    Do NOT add the V4 warp-jump overlay, the project deep-dive pages, `data-open`/`data-close`/`data-jump` wiring, `data-reveal`, or `data-para` — those are deferred (project pages -> Phase 5; scroll-reveal choreography -> Phase 4 per CONTEXT). The content sections (Mission/Work/Experience/Publications/Contact/footer) are added in Plan 01-03.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const h=f.readFileSync('app/index.html','utf8');const c=f.readFileSync('app/styles.css','utf8');const need=['data-star','data-planet=\"near\"','data-planet=\"far\"','data-typer','data-art','data-deep','canva.link/5a5yj5bdg78axhv','linkedin.com/in/sirio-vittorio-feltrin','&#305;'];need.forEach(k=>{if(!h.includes(k))throw new Error('index.html missing '+k)});if((h.match(/&#305;/g)||[]).length<2)throw new Error('need two dotless-i spans');if(/data-warpov|data-open=|data-reveal=/.test(h))throw new Error('deferred V4 warp/project/reveal markup must not be present');['--star-antares','--gold-glow','--font-display'].forEach(k=>{if(!c.includes(k))throw new Error('styles.css missing token '+k)});if(!/rel=\"noopener\"/.test(h))throw new Error('external links need rel=noopener');console.log('markup OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/index.html` contains `[data-star]`, `[data-planet="near"]`, `[data-planet="far"]`, `[data-typer]`, `[data-art]`, and `canvas[data-deep]`
    - The wordmark contains at least two `&#305;` (dotless i) spans and one transparent `o` span
    - The View CV pill href is exactly `https://canva.link/5a5yj5bdg78axhv`; the LinkedIn pill href is `https://www.linkedin.com/in/sirio-vittorio-feltrin/`; both have `target="_blank"` and `rel="noopener"`
    - `app/styles.css` `:root` defines `--star-sirius/-antares/-aldebaran/-vega`, `--gold-glow`, `--gold-core`, and the three font vars
    - No `data-warpov`, `data-open=`, or `data-reveal=` attributes appear in index.html (deferred features absent)
    - No `style-hover=` attributes remain (converted to CSS `:hover`)
  </acceptance_criteria>
  <done>The page renders the Deep Field hero shell with the exact star DOM contract, the spectrum spine, and a persistent nav whose CV/LinkedIn links resolve; design tokens are defined; deferred V4 warp/project/reveal markup is absent.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Boot the gold HeroStar + deep-field parallax + reduced-motion + smooth-scroll</name>
  <files>app/main.js, app/star-engine.js</files>
  <behavior>
    - On DOMContentLoaded, `new HeroStar(heroSection)` is created and its palette is gold; the star breathes and drives the CSS vars on the hero.
    - A fixed deep-field canvas renders 3 parallax star layers that shift with window.scrollY.
    - When `REDUCED` (prefers-reduced-motion) is true, no rAF loop runs; the star renders one static bright frame and the typewriter shows one static role.
    - Nav/section anchor clicks scroll smoothly (native `scroll-behavior:smooth` is sufficient).
  </behavior>
  <read_first>
    - app/star-engine.js (the HeroStar class: constructor sets this.pal to blue-white [150,196,255]/[255,251,242]; frame(now) drives the star; _startType runs the typewriter using [data-typer]/[data-art])
    - .planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html (port ONLY these parts of the DC script: `_boot` layer setup `this.layers` with the three `{f,a,stars}` layers and `mk(n,rM,rX)`, and `drawDeep(now)` — the fixed 3-layer parallax starfield tinted via `eng.tintAt`. Do NOT port openProj/closeProj/startBurst/applyTrans/drawWarp/applyPara/updateReveals — those are deferred.)
    - app/index.html (confirm the hero section selector and that [data-typer]/[data-art]/[data-star]/[data-planet] exist)
    - .planning/phases/01-the-design-build/01-CONTEXT.md (gold variant is locked; motion honors prefers-reduced-motion; canvases pause off-screen)
  </read_first>
  <action>
    Apply the GOLD palette to `HeroStar` in `app/star-engine.js`: change the constructor default `this.pal` to `{ glow: [255, 196, 110], core: [255, 236, 200] }`, OR add support for `opts.palette` and pass gold from main.js. The final rendered hero MUST use glow rgb(255,196,110) and core rgb(255,236,200) (the "Gold / Antares" tweak the user approved), not the blue-white default. Keep all other HeroStar behavior unchanged.

    Rewrite `app/main.js` as an ES module that:
    - Imports from `./star-engine.js`: `HeroStar`, `REDUCED`, `DPR`, `tintAt`, `rgba`, `clamp` (whichever the deep-field render needs).
    - On `DOMContentLoaded`, selects the hero section (`.hero` / `#top`) and constructs `const star = new HeroStar(heroEl, { periodSec: 20 })` (Slow cycle ~20s per CONTEXT); ensure the gold palette is active. Also select `canvas[data-deep]`.
    - Builds the deep-field state exactly as the V4 `_boot` did: three layers `{f:0.14,a:0.26,stars:mk(280,0.3,0.9)}`, `{f:0.36,a:0.40,stars:mk(140,0.5,1.3)}`, `{f:0.66,a:0.58,stars:mk(64,0.8,1.9)}` where each star has `{x,y:Math.random()*3,r,ph,hue}`.
    - Implements `drawDeep(now)` = the V4 parallax starfield: size the fixed canvas to viewport×DPR, clear, and for each layer/star compute `sy=(s.y*vh - scrollY*L.f) % (vh*3)` and draw twinkling dots filled with `rgba(tintAt(s.hue), L.a*tw)`.
    - Runs a single rAF loop calling `star.frame(now)` (only while the hero is on-screen — HeroStar.frame already early-returns when the hero is scrolled above the viewport, satisfying the off-screen pause) and `drawDeep(now)`.
    - Reduced-motion branch: if `REDUCED`, do NOT start the rAF loop; call `star.frame(performance.now())` once and `drawDeep(performance.now())` once for a static frame (HeroStar internally freezes brightness at 0.72 and shows a static typewriter role when reduced).
    - Add a `resize` listener that repaints once when reduced, and lets the rAF loop handle sizing otherwise.
    Native `scroll-behavior:smooth` (set in Task 1 CSS) covers NAV-02 smooth scrolling; no JS scroll handler required.
  </action>
  <verify>
    <automated>node --check app/main.js && node --check app/star-engine.js && node -e "const f=require('fs');const m=f.readFileSync('app/main.js','utf8');const e=f.readFileSync('app/star-engine.js','utf8');if(!/new HeroStar/.test(m))throw new Error('HeroStar not instantiated');if(!/import\s*\{[^}]*HeroStar[^}]*\}\s*from\s*'\.\/star-engine\.js'/.test(m))throw new Error('static import of HeroStar missing');if(!/data-deep|deep/.test(m))throw new Error('deep-field render missing');if(!/255,\s*196,\s*110/.test(e+m))throw new Error('gold glow 255,196,110 not applied');if(!/255,\s*236,\s*200/.test(e+m))throw new Error('gold core 255,236,200 not applied');if(/drawWarp|openProj|applyTrans/.test(m))throw new Error('deferred warp/project logic must not be ported');console.log('engine wiring OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` passes on `app/main.js` and `app/star-engine.js`
    - `app/main.js` statically imports `HeroStar` from `'./star-engine.js'` and calls `new HeroStar(...)`
    - The gold values `255,196,110` (glow) and `255,236,200` (core) appear in star-engine.js or main.js
    - `app/main.js` contains the deep-field 3-layer render referencing `data-deep`
    - `app/main.js` contains a reduced-motion branch that avoids starting the rAF loop
    - No `drawWarp`, `openProj`, or `applyTrans` (deferred warp/project logic) in main.js
  </acceptance_criteria>
  <done>The hero boots a breathing GOLD star pinned to the 'o', the typewriter cycles roles, i-orbs orbit on ignition, the deep-field parallaxes on scroll, and prefers-reduced-motion yields a static frame.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the living gold star hero on the deployed site (human-verify)</name>
  <files>(none — visual verification of the deployed page)</files>
  <action>
    Human-verify checkpoint: Claude has wired the living gold Sirio Star hero and pushed (the Plan 01-01 Actions pipeline redeploys automatically). The developer visually confirms the star color/motion, wordmark anchoring, typewriter, deep-field parallax, working nav links, and reduced-motion behavior using the numbered steps in how-to-verify. No further Claude code changes are needed for this task; it gates on the developer's approval.
  </action>
  <what-built>
    The living GOLD Sirio Star hero is now wired: a breathing star (~20s) pinned to the optical centre of the 'o' in "Sirio", dotless-i orbs that orbit on each ignition, a role typewriter, a fixed deep-field parallax starfield, the left spectrum spine, and a persistent nav with working View CV + LinkedIn pills. Pushing to the repo has redeployed via the Plan 01-01 Actions pipeline.
  </what-built>
  <how-to-verify>
    1. Open the live github.io URL (or run `python -m http.server -d app 8080` and open http://localhost:8080). NOTE: ES modules require a server — do not open index.html via file://.
    2. Confirm the star is GOLD (warm amber glow), not blue-white, and visibly breathes brighter/dimmer over ~20 seconds.
    3. Confirm the star sits exactly where the 'o' of "Sirio" would be, and the two i's have small glowing orbs on their dots.
    4. Watch one bright cycle: the orbs should run a single orbit around the star and return home.
    5. Confirm the "I am a ___" typewriter types and deletes through the roles (Engineer, Designer, Developer, Biotechnologist, ...).
    6. Scroll the page a little: the background starfield should parallax (layers drift at different speeds).
    7. Click "View CV" (opens the Canva link in a new tab) and "LinkedIn" (opens the profile).
    8. Toggle OS "reduce motion" and reload: the star should hold a static bright frame and the typewriter show a single role.
    9. Resize to a narrow (mobile) width: the hero should stay legible and the star still anchored to the 'o'.
  </how-to-verify>
  <verify><automated>MISSING — hero motion/color/anchoring is a visual property confirmed by the developer in how-to-verify (static greps for the gold values and DOM contract run in Task 2)</automated></verify>
  <acceptance_criteria>
    - Developer confirms the star is gold, breathes, and is anchored to the 'o'
    - Developer confirms the typewriter cycles roles and the i-orbs orbit on ignition
    - Developer confirms View CV + LinkedIn open correctly and reduced-motion yields a static frame
  </acceptance_criteria>
  <resume-signal>Type "approved", or describe any issue (star wrong color, star not on the 'o', typewriter stuck, links broken, janky on mobile).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Google Fonts CDN | third-party font CSS/woff2 loaded at runtime |
| page -> external link targets (Canva, LinkedIn) | `target="_blank"` navigations to third-party origins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Reverse tabnabbing via `target="_blank"` (View CV, LinkedIn pills) | mitigate | Every `target="_blank"` link carries `rel="noopener"` (verified in Task 1 automated check) |
| T-02-02 | Information Disclosure | Google Fonts CDN sees visitor IP/referrer | accept | Standard for a portfolio; low sensitivity, no PII collected. Design mandates the Google Fonts trio; self-hosting deferred |
| T-02-03 | Denial of Service | rAF canvas loop pegging the CPU on weak/mobile devices | mitigate | HeroStar.frame early-returns when the hero is off-screen; prefers-reduced-motion path skips the loop entirely (full perf pass is Phase 5) |
| T-02-SC | Tampering | package installs (npm/pip/cargo) | accept | No package installs in this plan (plain static ES modules, no bundler) — legitimacy gate not applicable |
</threat_model>

<verification>
- `node --check` passes on both JS files; markup/token/gold greps pass (task automated checks).
- Developer confirms the gold breathing star, orbiting i-orbs, typewriter, deep-field parallax, working CV/LinkedIn pills, and reduced-motion static frame on the deployed site.
- HERO-01 note: the approved design implements the "wow centerpiece" as an animated Canvas-2D star (the later-approved Claude Design supersedes the earlier "WebGL/Three.js" wording in REQUIREMENTS FOUND/HERO). The animated canvas star satisfies HERO-01's intent (a 3D-feeling animated hero centerpiece).
</verification>

<success_criteria>
- HERO-01: An animated cosmic star centerpiece runs on load.
- HERO-02: The hero reacts to scroll (deep-field parallax shifts with scrollY; the star pauses when scrolled off-screen).
- HERO-03: The identity reads in one line — the "Sirio" wordmark + the "I am a [role]" typewriter.
- HERO-04 / LINK-01 / LINK-02: The nav exposes working View CV (Canva) and LinkedIn actions.
- FOUND-03: A dark-themed design-token system (star colors, bg gradient, text, glass, fonts, scale) is applied.
- NAV-02: A persistent top nav plus native smooth-scroll anchors are in place.
</success_criteria>

<output>
Create `.planning/phases/01-the-design-build/01-02-SUMMARY.md` when done. Record the star DOM contract (data-attributes) and CSS token names so Plan 01-03 reuses them for the content sections.
</output>
