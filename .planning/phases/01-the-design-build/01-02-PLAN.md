---
phase: 01-the-design-build
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - site/index.html
  - site/styles.css
  - site/main.js
  - site/star-engine.js
  - site/README.md
autonomous: false
requirements: [FOUND-03, HERO-01, HERO-02, HERO-03, HERO-04, NAV-02, LINK-01, LINK-02]

must_haves:
  truths:
    - "On load the hero shows a living GOLD star (glow 255,196,110 / core 255,236,200) breathing on a 20s cycle — not blue-white"
    - "The star is pinned to the optical centre of the transparent 'o' in the wordmark Sırıo, and scales with the h1"
    - "The two dotless-i orbs each run one eased orbit per breath, once brightness crosses 0.45"
    - "A typewriter under the name cycles 8 roles with per-term gradients and a blinking caret"
    - "A fixed deep-field starfield parallaxes behind the whole page and is visible THROUGH the hero — no density seam at the hero's bottom edge (D-04)"
    - "The star and rings drift slightly toward the cursor and settle back — suppressed on touch and under prefers-reduced-motion (D-05)"
    - "The hero top bar docks into a fixed 70px glass nav once the visitor scrolls past the hero, exposing working CV and LinkedIn links (D-01)"
    - "Under prefers-reduced-motion the star renders one static, fully-lit gold frame and the typewriter shows a single static role"
  artifacts:
    - path: "site/index.html"
      provides: "Page shell + deep-field canvas + hero DOM satisfying every engine selector (canvas, data-star, 2x data-planet, dotless-i spans, transparent-o span, data-art, data-typer) + docking nav"
      contains: "data-typer"
      min_lines: 60
    - path: "site/styles.css"
      provides: "FOUND-03 design tokens on :root + hero/star-layer/nav styling + b-driven breathing overlay"
      contains: "--gold-glow"
      min_lines: 180
    - path: "site/main.js"
      provides: "GOLD palette assignment, HeroStar wiring at periodSec 20, single rAF loop, deep field, pointer parallax, nav docking"
      contains: "new HeroStar"
      min_lines: 80
    - path: "site/star-engine.js"
      provides: "Additive opt-in opts.transparent flag enabling clearRect instead of the opaque background fill (D-04)"
      contains: "opts.transparent"
  key_links:
    - from: "site/main.js"
      to: "site/star-engine.js"
      via: "static ES module import of HeroStar + REDUCED + DPR + tintAt + rgba"
      pattern: "import\\s*\\{[^}]*HeroStar[^}]*\\}\\s*from\\s*'\\./star-engine\\.js'"
    - from: "site/main.js"
      to: "the GOLD palette (D-02)"
      via: "post-construction star.pal assignment — the engine hard-codes blue-white at :43 and exposes no opts.pal"
      pattern: "star\\.pal\\s*=\\s*\\{\\s*glow:\\s*\\[255,\\s*196,\\s*110\\]"
    - from: "site/index.html hero"
      to: "site/main.js"
      via: "[data-hero] element queried by HeroStar; canvas + data-star + data-planet consumed per frame"
      pattern: "data-hero"
    - from: "site/index.html nav"
      to: "https://canva.link/5a5yj5bdg78axhv"
      via: "CV anchor href with target=_blank rel=noopener"
      pattern: "canva\\.link/5a5yj5bdg78axhv"
    - from: "site/index.html nav"
      to: "https://www.linkedin.com/in/sirio-vittorio-feltrin/"
      via: "LinkedIn anchor href with target=_blank rel=noopener"
      pattern: "linkedin\\.com/in/sirio-vittorio-feltrin"
---

<objective>
Turn the walking skeleton into the wow moment: the real, living **GOLD Sirio Star** hero from the approved `Sirio Star.dc.html`, composed onto the V4 deep-field cosmos, with the FOUND-03 design-token system every later section reuses and a hero bar that docks into a persistent nav carrying working CV + LinkedIn links.

This phase is **a port, not an invention.** Every visual constant already exists on disk in `.planning/phases/01-the-design-build/design/`. `site/star-engine.js` is byte-identical to the design's engine (`diff` exit 0, verified) and already contains a complete `HeroStar` class with breathing, orbits, rings, typewriter, reduced-motion, DPR capping and off-screen culling. **Do not rewrite it.** The work is: supply the DOM it expects, set the gold palette, drive one rAF loop, and compose it with the deep field.

Purpose: HERO-01..04 + NAV-02 + LINK-01/02 + FOUND-03 — the core identity piece and the token foundation Plan 01-03 builds every section on.
Output: A deployable page whose hero is the approved gold star on a continuous cosmos. Content sections arrive in Plan 01-03.
</objective>

<execution_context>
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/workflows/execute-plan.md
@D:/06. Job & Applications/05_Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-the-design-build/01-RESEARCH.md
@.planning/phases/01-the-design-build/01-PATTERNS.md
@.planning/phases/01-the-design-build/01-01-SUMMARY.md

# Design sources of truth (per CLAUDE.md "Sources of Truth")
@.planning/phases/01-the-design-build/design/Sirio Star.dc.html
@.planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html

# Live source being modified (shipped by 01-01, live at https://sirsirio.github.io/MyPortfolio/)
@site/index.html
@site/styles.css
@site/main.js
@site/star-engine.js
</context>

<constraints>
**Paths:** the site source root is `site/`. It was renamed from `app/` in commit 1b4b908. **Zero `app/` references may be written.** `01-01-SUMMARY.md` documents `app/…` paths — that is a historical record; do not inherit it.

**`_Archive/` is off-limits** — git-ignored, superseded, never a design/content/context source. There is deliberately no CONTEXT.md for this phase (the old one was deleted in commit bdbc909); the sources of truth are the design snapshots, `../00_Profile/profile.md`, and ROADMAP/REQUIREMENTS.

**Do not port** (Phase 2/4 scope, present in the V4 file): `openProj`, `closeProj`, `startBurst`, `applyTrans`, `drawWarp`, `wstars`/`spawn`, `[data-proj]`, `[data-warpov]`, `data-open`. Per RESEARCH Open Question 1, Phase 1 ships **persistent nav + single-scroll home only**. The V4 design's answer to FOUND-04's "multi-page" (fixed overlays + warp transition) exists and is recorded — Phase 4 (PROJ-01) should not re-litigate it, and Phase 1 must not build it.

**Do not strip framework scaffolding into the port:** `<x-dc>`, `<helmet>`, `./support.js`, `data-screen-label`, `<script type="text/x-dc">`, `data-props`, `DCLogic`, `componentDidMount`, `renderVals()` are all Claude Design authoring artifacts and must not appear in `site/`.

**`style-hover` is not real HTML.** It appears ~14 times across the design and browsers ignore it silently. The paired `transition:` sits in the normal `style` attribute and **survives** — so a verbatim port looks right in a screenshot and is completely dead on hover. Every `style-hover` in ported markup must become a real CSS `:hover` rule.

**Zero inline styles in the port.** FOUND-03 asks for a token system; a 1:1 inline-style copy passes visual review and fails the requirement.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: The living GOLD cosmos — tokens, hero DOM, gold star, deep field, composition (D-04)</name>

  <files>site/index.html, site/styles.css, site/main.js, site/star-engine.js</files>

  <read_first>
    - `site/star-engine.js` **in full (296 lines)** — this is the contract. Note especially: `:42` `opts.periodSec || 15`; `:43` the hard-coded blue-white `this.pal`; `:44-48` the selectors it queries; `:49-52` `this.stars` (130-star field, an **instance property**); `:63` `if (!this.canvas) return`; `:83-85` the `[data-art]`/`[data-typer]` lookup and its silent early return; `:120-148` `_measure`; `:160-163` the breathing `b`; `:168-177` the 9 CSS vars it writes on the hero element; `:179-183` the star-layer pin; `:185-187` **the opaque background fill — the D-04 blocker**; `:189-192` the radial glow wash; `:194-204` the hero starfield; `:239-294` the i-dot orbital mechanics.
    - `.planning/phases/01-the-design-build/design/Sirio Star.dc.html:14-71` — the hero source. `:18-21` keyframes `spin`/`spinRev`/`blink`; `:26` the hero canvas; `:28-29` the two planets; `:31-47` the 6-span star layer in z-order; `:57-67` intro + wordmark + typewriter; `:59` **the wordmark character sequence**. Also `:203` (`cycleSpeed` "Slow" ⇒ 20s) and `:204-211` (the palette map — Gold is `glow:[255,196,110]`, `core:[255,236,200]`).
    - `.planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html:14-50` — the page shell. `:16-23` reset/keyframes; `:26` the root wrapper gradient + `overflow-x:clip`; `:27` the fixed `[data-deep]` canvas; `:30` `[data-field]`; `:33` the hero section box; `:41-43` **the mockup to delete**; `:44-49` `[data-scrollhint]`. Also `:331-336` (deep-field layer construction) and `:360-379` (`drawDeep`).
    - `.planning/phases/01-the-design-build/01-PATTERNS.md` — "THE SEAM: the hero markup contract" table (11 rows, each a silent failure) and the `site/main.js` section.
    - `.planning/phases/01-the-design-build/01-RESEARCH.md` — "The GOLD Star: Exact Values", the E-1..E-8 gap table, "Design Tokens (FOUND-03)", and Pitfall 3.
    - `site/index.html`, `site/styles.css`, `site/main.js` — current shipped state (walking skeleton; the `.hero`/`h1` rules at `styles.css:21-36` are replaced, the reset at `:1-6` and the reduced-motion block at `:38-47` survive).
  </read_first>

  <action>
    Build the page shell, the hero DOM, and the engine wiring so the approved gold star renders on a continuous deep-field cosmos.

    **1 — Design tokens (FOUND-03), in `site/styles.css` on `:root`.** Author bare `R,G,B` triplets so they compose inside `rgba(var(--x), a)`. Required: `--star-sirius:150,196,255`, `--star-antares:244,182,89`, `--star-aldebaran:255,122,89`, `--star-vega:171,140,255` (these are the `PAL` stops at `star-engine.js:4-10`); `--gold-glow:255,196,110` and `--gold-core:255,236,200` (the hero star); `--bg-0:#04050c`; `--text-hi:#eef2ff`; `--text-body:rgba(228,232,244,0.76)`; `--glass-bg:rgba(255,255,255,0.04)`; `--glass-border:rgba(255,255,255,0.1)`; `--font-display:'Space Grotesk',sans-serif`; `--font-body:'Hanken Grotesk',sans-serif`; `--font-mono:'Space Mono',monospace`; `--pad-section:clamp(80px,12vh,150px) clamp(20px,6vw,84px)`; `--max-w:1180px`; `--nav-h:70px`.
    **`--gold-glow` (255,196,110, the star) is NOT `--star-antares` (244,182,89, the Work section). Two different golds. Do not conflate them.**

    **2 — Page shell.** Keep `styles.css:1-6` (`*{box-sizing:border-box}`) and the existing reduced-motion block at `:38-47` (it is stricter than the design's `V4:23` — keep the shipped version). Replace the skeleton `.hero`/`.hero h1` rules. Add `html{scroll-behavior:smooth}`, the `V4:18-21` reset/`::selection`/link colours, and the `V4:26` root wrapper: `position:relative; width:100%; overflow-x:clip;` plus the page gradient `linear-gradient(180deg,#04050c 0%,#0c0a09 24%,#110b0a 46%,#100a14 68%,#080a16 86%,#05060d 100%)`. Use **`overflow-x:clip`, never `overflow:hidden`** — `Sirio Star.dc.html:16` sets `html,body{overflow:hidden}` because it was authored as a standalone full-viewport screen; copying that would kill the scrolling page. Port keyframes `spin`, `spinRev`, `blink` (`Sirio Star.dc.html:18-21`) and `scrollDot` (`V4:22`). Do not set `height:100%` on `html,body`.

    **3 — Hero DOM in `site/index.html`.** Structure per `Sirio Star.dc.html:25-68`, dropped into the V4 hero slot (`V4:33`) in place of the mockup — **delete `V4:41-43`** (`Hello, I am`, `<h1>Sirio</h1>`, and the literal `[ STATIC MOCKUP … ]` marker). Keep `V4:44-49` `[data-scrollhint]` → `#index`. Emit, inside `<section id="top" class="hero" data-hero>` (which **must be `position:relative`** — the engine computes hero-relative coords at `:145-146` and `:242`):
      - a `<canvas>` (`position:absolute; inset:0; z-index:0; pointer-events:none`) — **omit it and `frame()` bails at `:63` and the entire hero draw never runs**;
      - a `<div data-parallax>` wrapper at `position:absolute; inset:0; pointer-events:none` containing `<div data-planet="far">`, `<div data-planet="near">` and `<div data-star>` (this wrapper is Task 2's pointer-parallax handle; because it is `inset:0` on the hero it preserves the engine's hero-relative coordinate space exactly);
      - the `[data-star]` layer's **6 sub-spans verbatim in z-order** per `Sirio Star.dc.html:31-47`: halo `2.7em` (`--halo-op`, radial `--glow`) → flare A `2.0em` (`spinRev 56s`, `calc(var(--flare-op)*0.5)`) → flare B `1.45em` (`spin 46s`, white) → flare C `0.96em` (`spinRev 32s`, `--glow`) → glass disc `0.92em` (`--glass-op`, `backdrop-filter:blur(3px)`) → dashed ring `0.78em` (`spin 17s`, `--ring-op`) → core `0.5em` (`background:var(--core)`, `box-shadow:0 0 var(--core-blur) rgba(var(--glow),0.85)`). **Every star sub-element must be sized in `em`, never px** — the engine sets `starLayer.style.fontSize` from the h1's computed size (`:180`) and the whole star scales from that. Keep the SVG flare paths and the dashed-ring `<circle>` exactly as authored.
      - the intro `<div>` `Hello, I am` and the wordmark `<h1>`. **The wordmark is load-bearing and fails silently:** it is `S` + span `&#305;` + `r` + span `&#305;` + span `o`, where `&#305;` is **U+0131 LATIN SMALL LETTER DOTLESS I** and the `o` span is `color:transparent; display:inline-block`. The engine finds the orb homes with `textContent.trim() === 'ı'` at `:48` and requires **exactly 2** (`:239`); a normal `i` skips the entire planet block with no error. It finds the star's anchor with `=== 'o'` at `:124`; the transparent `o` **is** the star and must stay a real text node. Comment both facts loudly in the markup so nobody "fixes" the gap that reads as `Sır`.
      - the typewriter `<p>`: `<span>I am a</span>` then a wrapper holding `<span data-art></span>`, `<span data-typer></span>`, `<span class="caret"></span>`. **`data-art` and `data-typer` are in NEITHER design file** — the `.dc.html` addresses these spans positionally (`children[0]`/`[1]`, `:130-131`) but the engine requires explicit attributes (`:83-84`) and `_startType` early-returns at `:85` with no error if `[data-typer]` is missing. Omit them and the typewriter silently never runs.
      - Keep the `COPENHAGEN · 2026` corner mark (`Sirio Star.dc.html:70`).

    **4 — Deep field.** Add the fixed `<canvas data-deep>` per `V4:27` (`position:fixed; inset:0; width:100vw; height:100vh; z-index:0; pointer-events:none`) as the first child of the root wrapper, and wrap all sections in `<div data-field>` at `position:relative; z-index:1` (`V4:30`). In `site/main.js`, port the layer construction verbatim from `V4:331-336` — three layers `{f:0.14,a:0.26,stars:mk(280,0.3,0.9)}`, `{f:0.36,a:0.40,stars:mk(140,0.5,1.3)}`, `{f:0.66,a:0.58,stars:mk(64,0.8,1.9)}` (484 stars total) — and `drawDeep(now)` verbatim from `V4:360-379`. **Drop V4's `this.dens()` density multiplier** (a Claude Design prop that does not exist here). `drawDeep` must use **`clearRect`, not `fillRect`** (`V4:367`) — that is what lets the root gradient show through. Resize the canvas only when dims actually change (`if (c.width !== W …)`), then `ctx.setTransform(DPR,0,0,DPR,0,0)`; no resize listener is needed.

    **5 — Wire the star (E-1, E-2, E-3, E-8).** Replace `site/main.js` entirely (it is a 9-line boot marker). Use a **static** import — `import { HeroStar, REDUCED, DPR, tintAt, rgba } from './star-engine.js'` — the module seam is already proven over Pages by 01-01 and `index.html` already loads `main.js` as `type="module"`; V4's `await import(...)` shape exists only because Claude Design needs async boot. Then:
      - `const star = new HeroStar(heroEl, { periodSec: 20, transparent: true })`. **`periodSec: 20` is mandatory** — the engine defaults to 15 at `:42`, and 20s is the approved tempo (`cycleSpeed` prop default "Slow", `Sirio Star.dc.html:203`) per decision A1.
      - `star.pal = { glow: [255, 196, 110], core: [255, 236, 200] }` — **this is the single highest-value line in the plan (D-02).** The engine hard-codes blue-white `[150,196,255]` at `:43` and its constructor accepts **no** `opts.pal` (verified: it reads only `opts.reduced` and `opts.periodSec`). Without this assignment the hero ships blue-white and the phase fails its acceptance standard. `_draw` reads `this.pal` fresh each frame (`:164`), so post-construction assignment needs zero engine edit.
      - **One rAF loop, many consumers** (`V4:345`): schedule `requestAnimationFrame` **first**, then call `drawDeep(now)` and `star.frame(now)`. Never two loops — that doubles the layout-read cost and lets the canvases tear against each other.
      - **Reduced-motion shape, copied from `V4:342-344`:** if `REDUCED`, render exactly **one** frame and never loop, and re-render on `resize`. Combined with the engine's internal `b = 0.72` (`:162`) this yields a **static, fully-lit gold star** — not a blank canvas. Reduced motion replaces motion, it does not delete the visual.
      - Guard the first measurement with `document.fonts.ready.then(...)` before starting the loop (E-8). `_measure` uses `measureText('o')` against the h1's computed font; before Space Grotesk lands the metrics come from the fallback and the star visibly jumps. The `.dc.html` guards this at `:102`; `HeroStar` does not.
      - Port `applyPara()` from `V4:381-391` for `[data-para]` elements and register the scroll listener `{ passive: true }` (`V4:340-341`). **Keep its ±400px cull** — it is the perf guard.

    **6 — D-04, the hero canvas composition.** This is the largest untested integration point in the phase: `Sirio Star.dc.html` was authored standalone and V4's hero was a mockup, so the two files have **never been composed**. `_draw` fills the hero canvas **opaquely** at `:185-187` (`bg = lerp3([4,5,12],[10,15,32], b)`), which would hide the fixed deep field across the entire first viewport and leave a visible starfield seam at the hero's bottom edge. Sirio delegated this decision; the resolution is **transparent hero canvas + no hero starfield**, in three moves:
      - **`site/star-engine.js` — one surgical, additive, backward-compatible edit.** There is no existing seam (verified: the constructor reads only `opts.reduced` and `opts.periodSec`). Add `this.transparent = !!opts.transparent` to the constructor and guard the background block at `:185-187` so that when `this.transparent` is true it calls `ctx.clearRect(0, 0, W, H)` instead of the opaque `fillStyle`/`fillRect` pair, and otherwise runs the existing fill unchanged. **Default stays false, so the design snapshot's standalone behaviour is preserved byte-for-byte.** Change nothing else in the engine — no refactor of the read/write interleave, no `opts.pal`, no `--dot-op`. The radial glow wash at `:189-192` fades to `rgba(0,0,0,0)` and composites correctly over a cleared canvas, so **the star itself is unchanged** (D-02 preserved).
      - **`site/main.js` — remove the hero's own starfield with zero engine edits:** set `star.stars = []` after construction. `this.stars` is an instance property (`:49-52`) and the draw loop at `:194` simply iterates nothing. Rationale for removing rather than thinning: the deep field's near layer (64 stars at r 0.8–1.9, alpha 0.58) already carries bright foreground stars across the hero viewport, and the hero field is drawn in hero-space so it would **stick in place while the deep field parallaxes** — a worse artifact than the density jump it was meant to avoid. This is the tuning knob at Task 3's checkpoint: re-seeding a smaller field is a one-line change.
      - **`site/styles.css` — preserve the breathing background tint without an opaque canvas.** The engine publishes `--b` on the hero element every frame (`:169`), so add a `.hero::before` overlay at `position:absolute; inset:0; z-index:0; pointer-events:none` with `background: rgba(10,15,32, calc(var(--b,0) * 0.42))`. The hero still breathes darker/lighter, and the deep field and root gradient show through continuously.

    **7 — Hero CSS.** `.hero` = `position:relative; min-height:100vh; min-height:100svh` with the design's flex centring (`V4:33`). The h1 must carry the design's exact font so `_measure` reads the right metrics: `font: 300 clamp(58px,12.5vw,184px)/0.98 var(--font-display); letter-spacing:-0.04em; color:var(--text-hi)`. `[data-star]` = `position:absolute; z-index:2; pointer-events:none; width:0; height:0` — **never hard-code px on it**; the engine owns its `left`/`top`/`fontSize`. `[data-planet]` = `position:absolute; border-radius:50%; pointer-events:none` (the engine owns width/height/left/top/transform/zIndex/opacity/background/boxShadow). Every CSS var consumer (`--b`, `--glow`, `--core`, `--core-blur`, `--halo-op`, `--glass-op`, `--flare-op`, `--flare-scale`, `--ring-op`) **must be a descendant of the hero element** — the engine writes them on the hero, not on `:root` (`:168`). Give each var a sensible fallback so the first paint is not naked.
  </action>

  <acceptance_criteria>
    - `site/main.js` matches `star\.pal\s*=\s*\{\s*glow:\s*\[255,\s*196,\s*110\]` (the GOLD assignment — D-02).
    - `site/main.js` matches `periodSec:\s*20` (A1; the engine default of 15 is wrong).
    - `site/main.js` matches `new HeroStar` and `star\.stars\s*=\s*\[\]` (D-04 hero field removed).
    - `site/main.js` contains `transparent: true` and `site/star-engine.js` contains `opts.transparent` and `clearRect` (D-04).
    - `site/star-engine.js` still contains `[150, 196, 255]` at its constructor default and `Array.from({ length: 130 }` — i.e. the engine edit was **additive only**; `git diff site/star-engine.js` touches only the constructor line and the background block at `:185-187`.
    - `site/main.js` contains exactly one `requestAnimationFrame` loop function and one `drawDeep` definition.
    - `site/main.js` contains `document.fonts.ready` (E-8) and `{ passive: true }` on the scroll listener.
    - `site/index.html` contains `data-hero`, `data-star`, `data-planet="near"`, `data-planet="far"`, `data-art`, `data-typer`, `data-deep`, `data-field`, `data-parallax` — one occurrence each except the planets.
    - `site/index.html` contains `&#305;` exactly **twice** (U+0131 dotless i) and `grep -c '<span[^>]*>i</span>' site/index.html` is 0 — a normal `i` silently kills the orbits.
    - At runtime `document.querySelectorAll('[data-hero] h1 > span').length === 3`.
    - `site/index.html` contains a `<canvas>` inside `[data-hero]` (omitting it bails `frame()` at `:63`).
    - `site/styles.css` contains `--gold-glow`, `--gold-core`, `--star-antares`, `--star-sirius`, `--star-aldebaran`, `--star-vega`, `--nav-h`, `--font-display`, `--font-mono` (FOUND-03).
    - `site/styles.css` contains `overflow-x: clip` and matches `html\s*,?\s*body[^}]*overflow:\s*hidden` **zero** times.
    - `site/styles.css` contains `var(--b` (the breathing overlay) and `scroll-behavior: smooth`.
    - `site/index.html` contains zero occurrences of `style-hover`, `x-dc`, `helmet`, `support.js`, `data-screen-label`, `data-props`, `DCLogic`, `data-proj`, `data-open`, `STATIC MOCKUP`. (Do not name `style-hover` in an HTML comment — it would self-invalidate this gate.)
    - No file under `site/` contains the string `app/` or `serve app`.
    - `site/index.html` inline `style="` attribute count is 0 (FOUND-03: tokens, not inline styles).
  </acceptance_criteria>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && fail=0; grep -Eq "star\.pal\s*=\s*\{\s*glow:\s*\[255,\s*196,\s*110\]" site/main.js || { echo "FAIL: GOLD palette not set (hero would ship blue-white)"; fail=1; }; grep -Eq "periodSec:\s*20" site/main.js || { echo "FAIL: periodSec 20 missing"; fail=1; }; grep -Eq "star\.stars\s*=\s*\[\]" site/main.js || { echo "FAIL: D-04 hero starfield not removed"; fail=1; }; grep -q "opts.transparent" site/star-engine.js || { echo "FAIL: D-04 transparent flag missing"; fail=1; }; grep -q "Array.from({ length: 130 }" site/star-engine.js || { echo "FAIL: engine edit was not additive"; fail=1; }; grep -q "document.fonts.ready" site/main.js || { echo "FAIL: E-8 font guard missing"; fail=1; }; [ "$(grep -c '&#305;' site/index.html)" = "2" ] || { echo "FAIL: need exactly 2 dotless-i (U+0131) - orbits fail silently otherwise"; fail=1; }; for t in data-hero data-star data-art data-typer data-deep data-field data-parallax; do grep -q "$t" site/index.html || { echo "FAIL: missing $t"; fail=1; }; done; for b in style-hover x-dc helmet support.js data-screen-label DCLogic data-proj data-open "STATIC MOCKUP"; do grep -q "$b" site/index.html && { echo "FAIL: framework/deferred scaffolding leaked: $b"; fail=1; }; done; grep -rq "app/" site/ && { echo "FAIL: stale app/ path"; fail=1; }; grep -Eq "overflow-x:\s*clip" site/styles.css || { echo "FAIL: overflow-x clip missing"; fail=1; }; for v in --gold-glow --gold-core --star-antares --nav-h --font-display; do grep -q -- "$v" site/styles.css || { echo "FAIL: token $v missing"; fail=1; }; done; exit $fail</automated>
  </verify>

  <done>The page renders the approved GOLD Sirio Star breathing on a 20s cycle, pinned to the "o", with orbiting dotless-i orbs and a running typewriter, over a continuous parallaxing deep field with no seam at the hero's bottom edge. Under `prefers-reduced-motion` it renders one static fully-lit gold frame. All acceptance greps pass and `site/star-engine.js` differs from the design snapshot only by the additive `opts.transparent` guard.</done>
</task>

<task type="auto">
  <name>Task 2: Reactivity and persistent access — pointer parallax (D-05) + docking nav (D-01) + CV/LinkedIn</name>

  <files>site/index.html, site/styles.css, site/main.js, site/README.md</files>

  <read_first>
    - `.planning/phases/01-the-design-build/design/Sirio V4 - Deep Field x Hyperlight.dc.html:34-40` — the top bar markup and typography to reuse, and its two `style-hover` attributes that must become real CSS. Also `:53,69,107,141,164` — every section carries `scroll-margin-top:70px`, the design's fingerprint of the intended fixed header.
    - `.planning/phases/01-the-design-build/design/Sirio Star.dc.html:49-55` — the same bar; note `rel="noopener"` is already present on both links.
    - `.planning/phases/01-the-design-build/01-RESEARCH.md` — "Gap N-1" (no persistent nav exists in either design file), "Gap H-2" (no pointer handler exists anywhere; Option B is the recommendation), and Pitfall 1 (layout thrashing).
    - `.planning/phases/01-the-design-build/01-PATTERNS.md` — "No Analog Found" (both features are genuine additions), "Read-then-write ordering (perf)", and the `style-hover` → real CSS translation table.
    - `site/star-engine.js:179-183` and `:283-291` — **what the engine writes every frame**, which dictates where pointer parallax may safely be applied.
    - `site/main.js` and `site/index.html` as left by Task 1.
    - `site/README.md:19` — the stale `npx --yes serve app` line (drift D-3).
  </read_first>

  <action>
    Add the two behaviours the approved design does not contain, plus permanent access to CV and LinkedIn.

    **1 — D-05, pointer parallax (HERO-02).** No `mousemove`/`pointermove` handler exists in `Sirio Star.dc.html` or `star-engine.js` — the star breathes on a timer only, and `HeroStar.frame()` reads scroll position solely to cull itself. This is added motion, so keep it in the design's language: **slow and ambient, no bounce.**
      - **Where to apply it — this is the constraint that decides the implementation.** The engine overwrites `[data-planet]`'s `transform` every frame (`:287` sets `translate(-50%,-50%)`) and owns `[data-star]`'s `left`/`top` (`:181-182`). So do **not** write transforms onto the planets. Instead translate the `[data-parallax]` wrapper created in Task 1, which holds the star layer and both planets and is `inset:0` on the hero — moving it drifts star and orbs together while preserving the engine's hero-relative coordinate space. Apply a smaller counter-drift (about 0.6×) to the hero `<canvas>` so the rotating and expanding rings drift with the star and read as depth; the canvas has no hero starfield left to smear (Task 1 set `star.stars = []`), and translating the element does not affect its internal drawing.
      - **Magnitude and easing:** normalise the pointer offset from the hero centre to −1..1, cap the star layer at about ±10px and the canvas at about ±6px, and ease the current offset toward the target inside the existing rAF loop at roughly `0.06` per frame so it drifts and settles rather than tracking. Add `will-change: transform` to both.
      - **Gates:** bail entirely when `REDUCED` is true, and gate on `matchMedia('(hover: hover)').matches` so touch devices (where there is no cursor) never pay for it. When either gate closes, leave both transforms unset.
      - **Perf (Pitfall 1):** the engine already interleaves reads and writes every frame; **do not add more.** Cache the hero rect on `resize` and `scroll` (both `{ passive: true }`) and read the cached value in the pointer handler — **never call `getBoundingClientRect` per pointer event or per frame.** The pointer handler should only store the target offset; the loop applies it.

    **2 — D-01, the docking nav (NAV-02, HERO-04, LINK-01, LINK-02).** Sirio explicitly chose a hero bar that **docks on scroll** over a plain always-fixed header. Neither design file contains a persistent nav — the bar is `position:absolute; top:0` inside the hero (`V4:34`) and scrolls away forever. But every section carries `scroll-margin-top:70px` (`V4:53,69,107,141,164`), which exists only to stop a fixed header covering an anchored heading: **the design was authored expecting a ~70px persistent header that was never drawn.** Build it, reusing the design's markup and typography rather than inventing a new one.
      - **Content:** `SIRIO VITTORIO FELTRIN` left (Space Mono, 12px, `letter-spacing:0.2em`, `rgba(228,236,255,0.55)`); `CV&nbsp;↗` and `LinkedIn&nbsp;↗` right (Space Mono, 12px, `0.1em`, `rgba(228,236,255,0.6)`, `gap:24px`). `CV` → `https://canva.link/5a5yj5bdg78axhv`; `LinkedIn` → `https://www.linkedin.com/in/sirio-vittorio-feltrin/`. Both **must** keep `target="_blank" rel="noopener"` (already correct in the design — preserve it; it mitigates reverse tabnabbing). Keep the `&nbsp;` before each `↗` so the arrow cannot orphan.
      - **Scope note — do not put the mission one-liner in the nav.** ROADMAP success criterion 4 reads "a persistent nav exposes working View CV and LinkedIn actions, **and** the identity one-liner … introduces the page" — two clauses. The one-liner introduces the *page* and lives in the Mission section (`V4:57`), shipped by Plan 01-03. The nav's identity marker is the `SIRIO VITTORIO FELTRIN` wordmark. HERO-03's hero-level identity is already carried by `Hello, I am` + the wordmark + the role typewriter from Task 1.
      - **Docking behaviour:** one `<header data-nav>` element with two states. At rest it is the design's in-hero bar (`position:absolute; top:0; left:0; right:0`, transparent, the design's `padding:30px clamp(22px,5vw,56px)`). Once the visitor scrolls **past the hero** — dock when `heroEl.getBoundingClientRect().bottom <= 70`, computed from the rect already cached for the pointer handler, in the scroll listener — add a `nav--docked` class that switches it to `position:fixed; top:0; height:var(--nav-h)` with `background:var(--glass-bg)`, `backdrop-filter:blur(14px)`, `border-bottom:1px solid var(--glass-border)`, and reduced vertical padding. The `70px` dock height is exactly the `scroll-margin-top` offset every section already honours, so anchored headings land correctly.
      - **Motion:** the docked state fades in over ~0.45s ease on `opacity`, `background`, `backdrop-filter` and `border-color` only — **no transform, no bounce.** Under `prefers-reduced-motion` the state flips with no transition (the existing block at `styles.css:38-47` already forces `transition-duration:0.001ms`, so simply do not add a competing rule).
      - Translate both nav-link `style-hover="color:rgba(228,236,255,0.95)"` attributes (`V4:37,38`) into a real `:hover` rule. Remember the paired `transition:` survives in the normal `style` attribute, which is exactly why forgetting this fails silently.

    **3 — Smooth scrolling and anchors (NAV-02).** `html{scroll-behavior:smooth}` and the token `--nav-h:70px` landed in Task 1; apply `scroll-margin-top:var(--nav-h)` to the section anchor targets. Phase 1 ships `#top` and `#index` as real targets — `#work`, `#experience`, `#publications` and `#contact` arrive with Plan 01-03, so **do not add nav links or Mission pills pointing at sections that do not exist yet**; the `[data-scrollhint]` → `#index` from Task 1 is the only jump this plan ships.

    **4 — Drift D-3.** `site/README.md:19` still says `npx --yes serve app`; the folder is `site/`. Change it to `npx --yes serve site`. It is the only stale `app` reference left under `site/` — the rename in commit 1b4b908 missed it.
  </action>

  <acceptance_criteria>
    - `site/index.html` contains `data-nav`, `canva.link/5a5yj5bdg78axhv`, and `linkedin.com/in/sirio-vittorio-feltrin`.
    - Every `target="_blank"` in `site/index.html` is paired with `rel="noopener"` — the counts are equal and non-zero.
    - `site/styles.css` contains a `nav--docked` rule with `position: fixed` and `backdrop-filter`, and uses `var(--nav-h)` for the docked height.
    - `site/styles.css` contains a real `:hover` rule for the nav links, and `site/index.html` contains zero `style-hover` occurrences.
    - `site/main.js` contains `hover: hover` and references `REDUCED` in the pointer path (both gates present).
    - `site/main.js` contains `pointermove` and `data-parallax`; it does **not** write `transform` onto `[data-planet]` (the engine overwrites it at `:287`) — `grep -c "planet.*style.transform" site/main.js` is 0.
    - `site/main.js` calls `getBoundingClientRect` **zero** times inside the pointer handler and the rAF loop body (cached rect only; Pitfall 1).
    - `site/styles.css` contains `scroll-margin-top` bound to `var(--nav-h)`.
    - `site/README.md` contains `serve site` and zero occurrences of `serve app`.
    - `site/index.html` contains no anchor whose `href` targets `#work`, `#experience`, `#publications` or `#contact` (those sections ship in 01-03).
  </acceptance_criteria>

  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && fail=0; grep -q "data-nav" site/index.html || { echo "FAIL: nav missing"; fail=1; }; grep -q "canva.link/5a5yj5bdg78axhv" site/index.html || { echo "FAIL: LINK-01 CV missing"; fail=1; }; grep -q "linkedin.com/in/sirio-vittorio-feltrin" site/index.html || { echo "FAIL: LINK-02 LinkedIn missing"; fail=1; }; tb=$(grep -o 'target="_blank"' site/index.html | wc -l); no=$(grep -o 'rel="noopener"' site/index.html | wc -l); [ "$tb" = "$no" ] && [ "$tb" != "0" ] || { echo "FAIL: target=_blank ($tb) != rel=noopener ($no) - reverse tabnabbing"; fail=1; }; grep -q "nav--docked" site/styles.css || { echo "FAIL: D-01 docked state missing"; fail=1; }; grep -q "backdrop-filter" site/styles.css || { echo "FAIL: docked glass treatment missing"; fail=1; }; grep -q "hover: hover" site/main.js || { echo "FAIL: D-05 touch gate missing"; fail=1; }; grep -q "pointermove" site/main.js || { echo "FAIL: D-05 pointer handler missing"; fail=1; }; grep -q "REDUCED" site/main.js || { echo "FAIL: reduced-motion gate missing"; fail=1; }; grep -q "style-hover" site/index.html && { echo "FAIL: style-hover is not real HTML - dead on hover"; fail=1; }; grep -q "scroll-margin-top" site/styles.css || { echo "FAIL: NAV-02 anchor offset missing"; fail=1; }; grep -q "serve app" site/README.md && { echo "FAIL: drift D-3 not fixed"; fail=1; }; grep -q "serve site" site/README.md || { echo "FAIL: README local-preview not corrected"; fail=1; }; grep -Eq 'href="#(work|experience|publications|contact)"' site/index.html && { echo "FAIL: links to sections that ship in 01-03"; fail=1; }; exit $fail</automated>
  </verify>

  <done>Moving the cursor over the hero drifts the star, orbs and rings a few px and lets them settle; the effect is absent on touch and under reduced motion. Scrolling past the hero fades the top bar into a fixed 70px glass nav whose CV and LinkedIn links open correctly in a new tab. `site/README.md` documents `serve site`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Sirio approves the rendered hero (D-02 fidelity, D-04 composition, D-05 reactivity)</name>

  <read_first>
    - `.planning/phases/01-the-design-build/design/Sirio Star.dc.html` — open it in a browser next to the built page. **This is the acceptance standard** (D-02: *"I just want the sirio star to look like that, in the gold palette"*).
    - `.planning/phases/01-the-design-build/01-RESEARCH.md` — Pitfall 3 and assumptions A5/A6 (both flagged MEDIUM confidence, both explicitly requiring Sirio's eyes on a render).
  </read_first>

  <what-built>
    The living GOLD Sirio Star hero (glow `255,196,110` / core `255,236,200`, 20s breath, orbiting dotless-i orbs, role typewriter) composed onto the fixed deep-field starfield, with pointer parallax and a nav that docks on scroll.

    **Two of these compositions have never been seen by anyone**, which is why this checkpoint is blocking:
    - **D-04 — hero canvas composition (RESEARCH A6, "the biggest open visual decision").** `Sirio Star.dc.html` and the V4 page were each approved *separately* and were never composed. The star file was authored standalone with an opaque background; the V4 deep field expects to show through. Task 1 made the hero canvas transparent and removed the hero's own 130-star field so the deep field's 484 stars read continuously from the hero into the page, and moved the breathing background tint into a `--b`-driven CSS overlay.
    - **D-05 — pointer parallax (RESEARCH A5).** No pointer handler exists anywhere in the approved design; the star has only ever breathed on a timer. This is added motion.
  </what-built>

  <how-to-verify>
    1. Serve the site locally: `python -m http.server -d site 8080` (or `npx --yes serve site`), then open `http://localhost:8080`.
    2. Open `.planning/phases/01-the-design-build/design/Sirio Star.dc.html` in a second tab for side-by-side comparison.
    3. **D-02 — is it the approved star, in gold?** The star should be warm gold (`255,196,110`), *not* blue-white. It should sit exactly on the "o" of `Sırıo` (the wordmark reads `Sır` + a glowing star), swell from dark to bright over ~20 seconds, and the two i-dots should each launch one orbit per breath — the near one on a tilted path that passes in front of and behind the star, the far one on a flat ring — both returning home. Compare against the reference tab. **Fidelity to that snapshot is the bar.**
    4. **D-04 — is the cosmos continuous?** Scroll slowly from the hero down the page and watch the hero's bottom edge. There should be **no horizontal line where star density or brightness visibly changes**, and the background should still breathe subtly with the star. *If the hero now reads too sparse, the fix is one line — re-seed `star.stars` in `site/main.js` with a smaller field (e.g. 30–40 stars) instead of `[]`. Say so and it will be tuned.*
    5. **D-05 — does the reactivity feel right?** Move the cursor around the hero. The star, orbs and rings should drift a few pixels toward it and settle slowly — ambient, no snapping or bouncing. Too subtle or too strong is a one-number change.
    6. **Reduced motion:** enable the OS "reduce motion" setting and reload. The star should render **one static, fully-lit gold frame** with a single static role in the typewriter — not a blank canvas, no drift.
    7. **The nav:** scroll past the hero — the top bar should fade into a fixed glass bar at the top. Click `CV ↗` and `LinkedIn ↗`; both should open in a new tab and resolve.
  </how-to-verify>

  <acceptance_criteria>
    **This task is `autonomous: false`. Its only acceptance criterion is Sirio's explicit approval of the rendered page — it cannot be self-certified, and no grep can substitute for it.** D-04 and D-05 are compositions and motions that exist in no approved snapshot (RESEARCH A5/A6, both MEDIUM confidence, both explicitly requiring his eyes on a render), and CLAUDE.md is explicit that Claude leads design while Sirio approves each step.

    - Sirio has opened the rendered page and compared the hero side-by-side against `design/Sirio Star.dc.html`.
    - Sirio explicitly approves — or names the tune he wants — on each of: **D-02** (it is the approved star, in gold), **D-04** (the cosmos is continuous through the hero, no density seam), **D-05** (the pointer drift is ambient and correctly scaled).
    - The resume signal and any tuned value (`star.stars` count, parallax magnitude, dock threshold) are recorded in `01-02-SUMMARY.md`, so Plan 01-03 and Phase 3 inherit the decision rather than re-deriving it.
    - **Do not proceed to Plan 01-03 on an unanswered checkpoint.** If Sirio requests a change, apply it and re-present: the star's fidelity to the snapshot is the phase's acceptance standard (D-02), not a nice-to-have.
  </acceptance_criteria>

  <resume-signal>Type "approved", or describe what is off (e.g. "star too sparse now", "parallax too strong", "gold too orange", "nav docks too late") — each of these is a one-value tune, not a rebuild.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser ← GitHub Pages CDN | Static, read-only, public by intent. No server runtime, no auth, no data store, no user input in this phase. |
| page → third-party CDN (Google Fonts) | `fonts.googleapis.com` / `fonts.gstatic.com` — a runtime dependency shipped by 01-01. |
| page → external link targets | Canva (CV) and LinkedIn open in a new tab via `target="_blank"`. |
| repo → GitHub Actions → Pages | Build/deploy pipeline, shipped and proven green by 01-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Nav CV + LinkedIn anchors (`target="_blank"`) | mitigate | Preserve `rel="noopener"` on both (already correct in the design at `V4:37,38` / `Star:52,53`). Task 2 asserts `target="_blank"` count equals `rel="noopener"` count. Mitigates reverse tabnabbing. |
| T-01-02 | Tampering | Google Fonts CDN | accept | Third-party runtime dep already shipped by 01-01. No secrets and no user data cross this boundary; worst case is degraded typography. Self-hosting / SRI / CSP is explicitly deferred to Phase 5 — do not expand scope here. |
| T-01-03 | Tampering | Typewriter DOM sink (`star-engine.js:103-104`) | mitigate | The engine writes role text with `textContent`, which is correct and safe. This plan's only engine edit is the additive `opts.transparent` guard on the background fill. **Do not "improve" the typewriter to `innerHTML`** — that would add an injection sink for free. |
| T-01-04 | Elevation of Privilege | `.github/workflows/deploy.yml` | accept | Only pinned first-party `actions/*` (checkout@v4, configure-pages@v5, upload-pages-artifact@v3, deploy-pages@v4) at least privilege (`contents:read, pages:write, id-token:write`). Shipped and verified by 01-01. **Out of scope for this plan — do not modify.** |
| T-01-05 | Information disclosure | Repo contents | accept | No secrets exist and none ship. The site is static with no API key until Phase 6, which explicitly forbids a client-side key (ASKAI-06). |
| T-01-06 | Denial of service | Hero rAF loop / canvas | accept | Self-limiting: the engine culls when `hero.bottom < -80` (`:64-66`) and caps DPR at 2 (`:13`). Task 2 forbids per-frame and per-event `getBoundingClientRect`. Formal mobile perf work is Phase 3 (PERF-01). |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No packages are installed by this plan.** RESEARCH's Package Legitimacy Audit table is empty by design: there is no `package.json`, no bundler, and no dependency. The only stack additions are first-party (`site/star-engine.js`) and native web-platform APIs. If any task would introduce a package manager install, **stop and re-run the Package Legitimacy Gate protocol** before proceeding. |

**Net:** ASVS L1 categories V2 (auth), V3 (session), V4 (access control), V5 (input validation) and V6 (cryptography) are structurally N/A — this is a read-only static page with zero user input. V14 (configuration) is satisfied by 01-01 and must not be weakened. No security work is required in this plan beyond *preserving* `rel="noopener"` and `textContent`.
</threat_model>

<artifacts_this_phase_produces>
Every symbol created by this plan. Plan 01-03 consumes the tokens and the shell; do not rename these.

**New file paths:** none (01-02 modifies files shipped by 01-01).

**CSS custom properties authored on `:root` (FOUND-03):**
`--star-sirius` (150,196,255) · `--star-antares` (244,182,89) · `--star-aldebaran` (255,122,89) · `--star-vega` (171,140,255) · `--gold-glow` (255,196,110) · `--gold-core` (255,236,200) · `--bg-0` (#04050c) · `--text-hi` (#eef2ff) · `--text-body` · `--glass-bg` · `--glass-border` · `--font-display` · `--font-body` · `--font-mono` · `--pad-section` · `--max-w` (1180px) · `--nav-h` (70px)

**CSS custom properties written by the engine on the hero element each frame (consumed, not authored — `star-engine.js:168-177`):**
`--b` · `--glow` · `--core` · `--core-blur` · `--halo-op` · `--glass-op` · `--flare-op` · `--flare-scale` · `--ring-op`
*(`--dot-op` is set by `Sirio Star.dc.html:247` but NOT by `site/star-engine.js`. Nothing consumes it. Do not add it back.)*

**Data attributes:**
`data-hero` (the HeroStar root) · `data-star` · `data-planet="near"` · `data-planet="far"` · `data-art` · `data-typer` · `data-deep` (fixed deep-field canvas) · `data-field` (z-index:1 section wrapper) · `data-parallax` (**new** — D-05 pointer-drift wrapper around the star layer and planets) · `data-nav` (**new** — D-01 docking bar) · `data-scrollhint` · `data-jump` · `data-para`

**CSS classes:** `.page` · `.hero` · `.hero::before` (the `--b`-driven breathing overlay, D-04) · `.hero__intro` · `.hero__type` · `.wordmark__o` · `.typer` · `.caret` · `.nav` · `.nav--docked` (**new** — D-01) · `.nav__name` · `.nav__links` · `.scrollhint` · `.corner-mark`

**Keyframes:** `spin` · `spinRev` · `blink` (from `Sirio Star.dc.html:18-21`) · `scrollDot` (from `V4:22`)

**`site/main.js` module symbols:** `GOLD` (const) · `star` (HeroStar instance) · `heroEl` · `layers` · `mk(n, rM, rX)` · `drawDeep(now)` · `applyPara()` · `syncNav()` · `onPointerMove(e)` · `loop(now)`

**`site/star-engine.js` additions (additive, backward-compatible — default preserves current behaviour):** `opts.transparent` → `this.transparent`; the background block at `:185-187` guarded to `clearRect` when set.

**Section ids:** `#top` (hero). *(`#index`, `#work`, `#experience`, `#publications`, `#contact` ship in Plan 01-03.)*
</artifacts_this_phase_produces>

<verification>
- `python -m http.server -d site 8080` and open `http://localhost:8080`: the gold star breathes, orbs orbit, typewriter runs, deep field parallaxes through the hero.
- Console is clean — **no errors is not sufficient evidence**: the engine's failure modes are all silent. Explicitly confirm at runtime: `document.querySelectorAll('[data-hero] h1 > span').length === 3`, `!!document.querySelector('[data-hero] canvas')`, `!!document.querySelector('[data-typer]')`.
- `diff site/star-engine.js ".planning/phases/01-the-design-build/design/star-engine.js"` shows **only** the additive `opts.transparent` constructor line and the guarded background block. Nothing else.
- `grep -rn "app/" site/` returns nothing.
- Both nav links resolve: `https://canva.link/5a5yj5bdg78axhv` and `https://www.linkedin.com/in/sirio-vittorio-feltrin/`.
- With OS reduce-motion on: one static fully-lit gold frame, static role, no drift, no rAF loop running.
- Push and confirm the Actions run goes green and the change is live at `https://sirsirio.github.io/MyPortfolio/` (FOUND-02's repeatable deploy, proven by 01-01, still holds).
</verification>

<success_criteria>
- [ ] The hero star is **GOLD** (`255,196,110` / `255,236,200`), not the engine's default blue-white — D-02 satisfied and approved by Sirio at Task 3.
- [ ] The star breathes on a **20s** cycle (`periodSec: 20`), pinned to the optical centre of the transparent "o", scaling with the h1 (HERO-01, HERO-03).
- [ ] Both dotless-i orbs orbit once per breath; the typewriter cycles 8 roles with per-term gradients (HERO-03).
- [ ] The deep field is visible **through** the hero with no density seam at its bottom edge (D-04, approved at Task 3).
- [ ] The star, orbs and rings drift toward the cursor and settle; suppressed on touch and under reduced motion (HERO-02, D-05, approved at Task 3).
- [ ] The hero bar docks into a fixed 70px glass nav past the hero, honouring the design's `scroll-margin-top:70px` (NAV-02, D-01).
- [ ] `CV ↗` and `LinkedIn ↗` work from the docked nav, both `target="_blank" rel="noopener"` (HERO-04, LINK-01, LINK-02).
- [ ] `:root` carries the full design-token system and `site/index.html` has zero inline `style="` attributes (FOUND-03).
- [ ] Zero `app/` paths, zero `style-hover`, zero Claude Design scaffolding, zero warp/overlay machinery anywhere under `site/`.
- [ ] `site/star-engine.js` differs from the design snapshot **only** by the additive `opts.transparent` guard.
</success_criteria>

<output>
Create `.planning/phases/01-the-design-build/01-02-SUMMARY.md` when done.

Record for Plan 01-03: the final token names, whether Sirio tuned `star.stars` / the parallax magnitude / the dock threshold at Task 3, and the exact class names for `.section`-adjacent styling so 01-03 does not re-derive them.
</output>
</content>
