---
phase: 02.1-the-automation-deep-dive
plan: 02
type: execute
wave: 2
depends_on: [02.1-01]
files_modified:
  - site/index.html
  - site/styles.css
  - site/assets/media/ot2-slots-allocation.png
  - site/assets/media/ot2-ui-runner.png
autonomous: false
requirements: [NAV-03]
user_setup: []

must_haves:
  truths:
    - "The deck-map (slots allocation) and the protocol-runner UI appear on the deep-dive with a blueprint-plate / device-frame treatment that sits on the dark theme (D-14, D-15)"
    - "The APPROACH section reads as its own visual spotlight — the design-thinking differentiator (D-09)"
    - "The engineering-room accent and warp tint are confirmed with Sirio on a real render (D-27, open-question 3)"
    - "The 'what I started with' before-visual is resolved with Sirio (bench photo / line-art / dropped) — not guessed (D-16, open-question 1)"
    - "No uncleared vendor marketing render (Opentrons-OT2.png, RunOne...png) ships on the page (R-02)"
  artifacts:
    - path: "site/assets/media/ot2-slots-allocation.png"
      provides: "The OT-2 deck-allocation map (first-party — Sirio's own protocol deck layout)"
    - path: "site/assets/media/ot2-ui-runner.png"
      provides: "The parameterised-run UI screenshot (first-party — evidence of what the protocol does)"
    - path: "site/styles.css"
      provides: "Blueprint-plate / device-frame treatment for the two opaque-white PNGs + APPROACH spotlight styling"
      contains: "device-frame"
  key_links:
    - from: "the two opaque-white PNGs"
      to: "the dark engineering-room theme"
      via: "blueprint-plate / device-frame CSS (never a raw drop)"
      pattern: "device-frame|blueprint"
    - from: "the vendor marketing renders"
      to: "NOT the page"
      via: "excluded — never copied into site/assets/media"
      pattern: "Opentrons-OT2.png"
---

<objective>
Deepen the OT-2 deep-dive's media and cement its "engineering room" character. Plan 01
shipped the real story with the timelapse + CAD blueprint; this plan adds the two
supporting diagrams — the OT-2 deck-allocation map and the parameterised-run UI — with a
blueprint-plate / device-frame treatment so they sit on the dark theme instead of
punching an opaque-white hole in it (D-15). It elevates the design-thinking APPROACH into
its own spotlight (D-09), and it resolves — WITH Sirio, not by guessing — the two open
design questions: the "what I started with" before-visual (the vendor renders are not
cleared — R-02/D-16) and the engineering-room accent + warp tint value (D-27).

Purpose: turn a readable case study into a media-forward one that reads as an engineering
space, while honoring Sirio's "I steer by reacting to the render" working model (D-03).
Output: two treated diagrams on the page, a spotlighted APPROACH, and Sirio-confirmed
accent/tint and before-visual decisions.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-the-automation-deep-dive/02.1-CONTEXT.md
@.planning/phases/02.1-the-automation-deep-dive/02.1-RESEARCH.md
@.planning/phases/02.1-the-automation-deep-dive/02.1-PATTERNS.md
@.planning/phases/02.1-the-automation-deep-dive/02.1-01-SUMMARY.md

@site/index.html
@site/styles.css

# Design judgment (taste, NOT stack): D-25 design read, D-26 dials, D-27 engineering room
@.claude/skills/design-taste-frontend/SKILL.md

# Source media (first-party diagrams to copy in; vendor renders to EXCLUDE):
# Opentrons-OT2 DALSA media/Opentrons Slots allocation.png   → site/assets/media/ot2-slots-allocation.png
# Opentrons-OT2 DALSA media/UI protocol runner.png           → site/assets/media/ot2-ui-runner.png
# NEVER copy: Opentrons-OT2.png, RunOne Gel Electrophoresis System.png (R-02: not cleared)
</context>

<artifacts_produced>
NEW symbols/paths this plan creates (Phase 2.1 contribution — Wave 2):
- `site/assets/media/ot2-slots-allocation.png` — the OT-2 deck-allocation map.
- `site/assets/media/ot2-ui-runner.png` — the parameterised-run UI screenshot (371×888 portrait).
- `site/styles.css`: a `device-frame` / blueprint-plate treatment class for opaque-white
  imagery on the dark theme, and APPROACH-spotlight layout rules.
- `site/index.html`: the two diagrams placed in the APPROACH (deck map) and BUILD (UI
  runner) sections; optionally a Sirio-supplied "before" visual per the checkpoint outcome.
</artifacts_produced>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the "what I started with" before-visual (open-question 1 / D-16)</name>
  <decision>
    What image (if any) represents "what I started with" for the "before → what I built"
    contrast? The manufacturers' marketing renders (Opentrons-OT2.png, RunOne...png) are
    NOT cleared for editorial use (R-02) and must not ship. Naming the products in text is
    fine (nominative); reproducing their renders is not.
  </decision>
  <context>
    The PROBLEM/BUILD narrative could open on a "before" visual (the manual/bench starting
    point) contrasted with Sirio's automated build. Without a cleared image the section
    stands perfectly well on the CAD renders + timelapse alone. This is Sirio's call — do
    not let an executor guess or reach for a vendor render.
  </context>
  <options>
    <option id="bench-photo">
      <name>Sirio supplies a first-party bench photo</name>
      <pros>Authentic, cleared, strengthens the before→after contrast.</pros>
      <cons>Requires Sirio to locate/take + drop a photo into site/assets/media/.</cons>
    </option>
    <option id="line-art">
      <name>Commission simple line-art of the manual setup</name>
      <pros>On-theme (blueprint/schematic), no licensing issue.</pros>
      <cons>Extra design work; may be overkill for a portfolio slice.</cons>
    </option>
    <option id="drop">
      <name>Drop the "before" contrast entirely</name>
      <pros>Zero risk, ships now; the section stands on the CAD renders + timelapse.</pros>
      <cons>Loses the explicit before→after beat.</cons>
    </option>
  </options>
  <read_first>
    - @.planning/phases/02.1-the-automation-deep-dive/02.1-RESEARCH.md (§ R-02 vendor-image licensing, Open Question 1)
    - @.planning/phases/02.1-the-automation-deep-dive/02.1-CONTEXT.md (D-16)
  </read_first>
  <action>Pause and present the before-visual decision to Sirio with the three options above. Do NOT guess and do NOT reach for a vendor render. Record the chosen option (and, if bench-photo/line-art, the asset path Sirio supplies) so Task 2 can honor it; then resume.</action>
  <acceptance_criteria>
    - Sirio has selected bench-photo, line-art, or drop.
    - If bench-photo/line-art: the asset path Sirio will supply (or has supplied) is recorded for Task 2; if dropped, that is recorded so Task 2 does not add a before-visual.
    - Under no outcome do `Opentrons-OT2.png` or `RunOne Gel Electrophoresis System.png` enter `site/assets/media/`.
  </acceptance_criteria>
  <done>Sirio has chosen bench-photo, line-art, or drop; the decision (and any supplied asset path) is recorded for Task 2, and no vendor render will be introduced.</done>
  <resume-signal>Reply with: bench-photo (and attach/drop the file), line-art, or drop.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Copy + device-frame the two diagrams; spotlight the APPROACH section</name>
  <files>site/index.html, site/styles.css, site/assets/media/ot2-slots-allocation.png, site/assets/media/ot2-ui-runner.png</files>
  <read_first>
    - @site/index.html (the `[data-proj="ot2"]` overlay + its APPROACH and BUILD sections built in Plan 01)
    - @site/styles.css (`.media-slot`/`:has()` :1652-1692, `.blueprint` :1740-1766, `.media-slot--composite` :1706-1729, the `--accent` system :621-666)
    - @.planning/phases/02.1-the-automation-deep-dive/02.1-PATTERNS.md (§ "engineering-room accent + overlay styling" — `.blueprint` is the closest analog for the treatment)
    - @site/assets/media/README.md (MEDIA-01 contract: first-party only, committed, relative paths, always poster a video)
  </read_first>
  <action>
    Copy the two FIRST-PARTY diagrams (Sirio's own protocol deck layout + his protocol's
    run UI — distinct from the vendor marketing renders) into `site/assets/media/` with
    web-safe names: `Opentrons-OT2 DALSA media/Opentrons Slots allocation.png` →
    `site/assets/media/ot2-slots-allocation.png`, and `UI protocol runner.png` →
    `site/assets/media/ot2-ui-runner.png`. Do NOT copy `Opentrons-OT2.png` or
    `RunOne Gel Electrophoresis System.png` (R-02, Pitfall 5).

    Both PNGs are genuinely opaque-white (D-15) — a raw drop punches a white hole in the
    dark theme. Add a `device-frame` treatment in `site/styles.css` (a new class, modeled
    on the existing `.blueprint` plate at :1740 and deriving all colour from the section's
    `--accent`, never a hard-coded hue): a dark inset ground, a faint accent grid/border,
    and inner glow so the screenshot reads as a framed instrument panel / schematic rather
    than a bare image. The UI-runner shot is 371×888 portrait — frame it with
    `object-fit:contain` (never crop) at a constrained height, like the composite clip.

    Place `ot2-slots-allocation.png` in the APPROACH or BUILD section as the deck-map
    (what goes where on the OT-2 deck), and `ot2-ui-runner.png` in the BUILD section as
    evidence of the parameterised-run interface (D-14 — "what the protocol does", not how
    long it is). Give each a descriptive first-party `alt` (e.g. "Sirio's OT-2 deck
    allocation map for the gel-electrophoresis protocol"; "The parameterised-run interface
    of Sirio's OT-2 protocol").

    Elevate the APPROACH section into its own spotlight (D-09): give it distinct layout
    weight (an asymmetric blueprint-plate composition per dial DESIGN_VARIANCE 8, D-26) so
    the design-thinking process — Double Diamond, interviews, the 19-system survey → brief
    — reads as the differentiator that signals "designer, not just programmer." Keep the
    site's em-dash mono-label voice (D-28). VISUAL_DENSITY stays 3-4 (curated, airy).

    Honor Task 1's outcome: if Sirio supplied a bench photo / line-art, place it as the
    "before" visual with the same device-frame; if dropped, add no before-visual.
  </action>
  <verify>
    <automated>cd "D:/06. Job & Applications/05_Portfolio" && test -f site/assets/media/ot2-slots-allocation.png && test -f site/assets/media/ot2-ui-runner.png && ! test -f site/assets/media/Opentrons-OT2.png && ! test -f "site/assets/media/RunOne Gel Electrophoresis System.png" && grep -q 'ot2-slots-allocation.png' site/index.html && grep -q 'ot2-ui-runner.png' site/index.html && ! grep -qi 'Opentrons-OT2.png\|RunOne' site/index.html && grep -qi 'device-frame\|blueprint' site/styles.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `site/assets/media/ot2-slots-allocation.png` and `ot2-ui-runner.png` exist and are referenced in `site/index.html`.
    - Neither `Opentrons-OT2.png` nor `RunOne Gel Electrophoresis System.png` exists in `site/assets/media/` or appears in `site/index.html`.
    - `site/styles.css` defines a device-frame / blueprint treatment; both diagrams render on a dark framed ground (no raw opaque-white box) — confirmed on a browser render.
    - The APPROACH section reads as a distinct spotlight (asymmetric composition), not a plain paragraph block.
    - The before-visual matches Task 1's decision (present with device-frame, or absent).
  </acceptance_criteria>
  <done>The two diagrams sit framed on the dark theme, the design-thinking approach is a visual spotlight, and no uncleared vendor render ships. Success criterion 3 is fully met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the engineering-room accent + warp tint on a real render (open-question 3 / D-27)</name>
  <what-built>
    Plan 01 shipped the overlay with a default engineering-room accent (`--star-rigel`
    cyan) and a default warp tint (`data-pos="0.02"`). The blueprint grid now comes
    forward and the starfield recedes on arrival. This checkpoint lets Sirio react to the
    rendered warp + engineering-room look and tune the accent value and the `data-pos`
    warp-tint destination (Claude's discretion, sketch-and-show — Sirio steers by reacting).
  </what-built>
  <how-to-verify>
    1. Run the site locally (or open the deployed preview).
    2. Click the OT-2 card and watch the warp tint + the arrival into the engineering room.
    3. Judge: does the cyan technical accent feel right, or should it reuse an existing
       star-spectrum stop? Is the `data-pos` warp tint landing on the intended sector
       (low ≈ blue/gold end)? Does the arrival "earn" the shift (grid forward, starfield
       receded) per D-27?
    4. Reply with either "accent + tint approved" or specific steer (e.g. "warmer accent",
       "stronger tint", "grid too faint"). If steered, the executor tunes the
       `--accent` / `data-accent` and `data-pos` values and re-shows.
  </how-to-verify>
  <read_first>
    - @.planning/phases/02.1-the-automation-deep-dive/02.1-CONTEXT.md (D-27 engineering room, Claude's Discretion — accent/tint)
    - @.planning/phases/02.1-the-automation-deep-dive/02.1-RESEARCH.md (Open Question 3; `data-pos`/`tintAt` note in Code Examples)
    - @site/styles.css (the overlay `--accent` rule from Plan 01; `--star-rigel` :36)
  </read_first>
  <action>Run the site, walk Sirio through the rendered warp tint + engineering-room arrival, and capture his verdict. If he steers, tune the `--accent`/`data-accent` and `data-pos` values and re-show until approved. Keep the accent a `--accent`/section-scoped value only — NEVER add it to `PAL` in `star-engine.js`.</action>
  <acceptance_criteria>
    - Sirio has approved the accent + warp tint, OR given steer that the executor applied and re-showed until approved.
    - The final accent is a `--accent` / section-scoped value only — NOT added to `PAL` in `star-engine.js` (styles.css:13-20 warning).
    - The overlay's `--accent` remains set (never unset → never transparent, Pitfall 6).
  </acceptance_criteria>
  <done>The engineering-room accent and warp tint are Sirio-approved on a real render; the accent stays a section-scoped variable and never enters PAL.</done>
  <resume-signal>Reply "accent + tint approved" or describe the steer.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → shipped media | Only first-party, cleared assets may enter `site/assets/media/` and the page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-04 | Repudiation / IP | Shipping copyrighted vendor marketing renders with no use grant | mitigate | Never copy `Opentrons-OT2.png` / `RunOne...png` into the repo or page (R-02). Only the two first-party diagrams ship. Verified by grep + file-absence checks. Before-visual gated on a human decision (Task 1). |
| T-02.1-05 | Spoofing | Passing a vendor image off as first-party | accept→mitigate | The two shipped diagrams are Sirio's own protocol deck layout + run UI (his work product), labelled with first-party `alt` text; no manufacturer render is reproduced. |
| T-02.1-SC | Tampering | Supply-chain | mitigate | No new library or remote asset in this plan; assets are committed, relative-path, first-party (MEDIA-01). |
</threat_model>

<verification>
- File checks: the two diagrams exist; the two vendor renders do not.
- Grep: page references the two diagrams and neither vendor render; device-frame CSS present.
- Human render check (Task 3): the diagrams read as framed instrument panels on the dark theme; the APPROACH spotlight reads as the design differentiator; the warp tint + engineering-room accent are Sirio-approved.
</verification>

<success_criteria>
- Phase success criterion 3: the page carries Sirio's real OT-2 media (timelapse from Plan 01 + the two treated diagrams) and external links resolve. FULLY MET.
- Design intent D-09 (design-thinking spotlight) and D-27 (engineering room) are visible and Sirio-confirmed.
- Open questions 1 and 3 are resolved with Sirio, not guessed.
</success_criteria>

<output>
Create `.planning/phases/02.1-the-automation-deep-dive/02.1-02-SUMMARY.md` when done.
</output>
