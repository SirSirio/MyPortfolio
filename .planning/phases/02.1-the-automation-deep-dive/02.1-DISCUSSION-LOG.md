# Phase 2.1: The Automation Deep-Dive - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-18
**Phase:** 2.1-the-automation-deep-dive
**Areas discussed:** Template scope, Page story & structure, The moving parts, Media & vendor images, Tech direction, UI direction (design-taste skill)

---

## Template scope

| Option | Description | Selected |
|--------|-------------|----------|
| Hand-craft, reusable shape | Bespoke OT-2 page + clean named section skeleton Phase 4 lifts | ✓ (extended) |
| Parameterized template now | Data-driven render engine, OT-2 as instance #1 | |

**User's choice:** Avoid a template entirely — the taste-skill frames pages differently. OT-2 is a **pilot**, discussed in detail; the *rest* get more automation in creation. Sirio's effort goes into media + content; style he gives general direction on.
**Notes:** Resolved to: no code template; hand-crafted pilot; Phase 4 = automated per-page creation. Division of labor captured (Sirio: media+content; Claude: style). Only OT-2 card clickable now.

## Page story & structure

| Option | Description | Selected |
|--------|-------------|----------|
| Hook → then process | Open on the payoff, then unfold the story | |
| Chronological process | Double Diamond start to finish | (informed) |
| Artifact-driven | Organize by what was built | |

**User's choice:** **Problem-first** — start from the pain of manual gel loading; source the problem description from the DALSA report in `06_Materials` (richer than profile.md). Frame it well.
**Notes:** Also: **drop the "~785 lines" of the Python script** ("What's the use of that?"). Depth = **Curated case study** (chosen over Comprehensive / Punchy). Design-thinking process = **its own spotlight beat** (chosen over "woven in as texture").

## The moving parts

| Option | Description | Selected |
|--------|-------------|----------|
| The robot's logic, animated | Scroll-driven animated OT-2 deck / pipette path | (as optional sketch) |
| Parts assemble on scroll | 5 CAD parts fly onto the deck | |
| Real timelapse is the star | Actual run video carries the motion | ✓ |

**User's choice:** Liked the animated-robot-logic idea but questioned the odds of nailing it → **go with the real timelapse**.
**Notes:** Given honest probability breakdown (motion ~90%, protocol-accuracy ~80%, first-pass polish ~60–70%). Resolved: timelapse is the signature motion; the deck animation becomes a build-time **throwaway sketch** (upside-only). GSAP chosen later drives the scroll-telling.

## Media & vendor images

| Option | Description | Selected |
|--------|-------------|----------|
| Your own photos / line-art | Replace vendor renders with first-party material | (fallback) |
| Check press-kit terms | Researcher checks Opentrons/Embi Tec editorial-use terms | ✓ |
| Drop them entirely | No vendor imagery at all | |

**User's choice (vendor images):** **Check press-kit terms** (R-02); fall back to own photo/line-art if not permitted.
**Which assets feature (multiselect):** **Slots-allocation diagram** + **Protocol-runner UI screenshot** (on top of timelapse + 4 CAD renders). Bench photo not selected (relying on press-kit check first).
**Notes:** Both selected images are opaque-white → need blueprint-plate / device-frame treatment.

## Tech direction

| Option | Description | Selected |
|--------|-------------|----------|
| Static + GSAP scroll-telling | Keep static/GitHub-Pages foundation; add GSAP + ScrollTrigger (CDN) | ✓ |
| Also add a 3D showpiece | Above + Three.js / model-viewer 3D element | (later opt-in) |
| Framework rewrite | Rebuild into React/Next + Tailwind | |

**User's choice:** **Static + GSAP scroll-telling.**
**Notes:** Sirio challenged the "locked to vanilla" framing — wants the site to showcase AI-build capability, advanced resources welcome, but not overcomplicated, on GitHub Pages. Corrected: hard constraint is static/GitHub-Pages, not "no libraries." GSAP fits cleanly because the overlay pauses the star engine. 3D = later opt-in. Framework rewrite rejected (no visible payoff, fights overlay architecture). Deliberately revisits Phase-2 "no motion library" note.

## UI direction (design-taste-frontend skill)

| Option | Description | Selected |
|--------|-------------|----------|
| The 'engineering room' | Warp earns a shift: blueprint grid forward, starfield recedes, schematic/CAD, GSAP scroll-telling | ✓ |
| Seamless continuation | Same cosmic world, GSAP enriches motion | |

**User's choice:** **The 'engineering room.'**
**Notes:** Sirio asked to use the taste-skill in planning and set the UI direction during discussion. Skill installed at `.claude/skills/design-taste-frontend/`. Applied its brief-inference: Design Read = preserve-and-extend Deep Field × Hyperlight; dials VARIANCE 8 / MOTION 7 / DENSITY 3–4. Reconciliation captured: take the skill's taste, not its React/Tailwind stack. Em-dash ban overridden (keep the site's established voice).

## Claude's Discretion

- Warp-transition port mechanism, feel/intensity, return-home symmetry (machinery exists in V4).
- Timelapse encoding approach (<2MB + poster).
- Blueprint-plate / device-frame construction for the two opaque-white images.
- Exact GSAP scroll-telling choreography (sketch first).
- Whether the "engineering room" adds a dedicated technical accent within the star-spectrum.

## Deferred Ideas

- Phase 4 automated per-page creation model (taste-skill + general style direction).
- 3D / Three.js showpiece as an opt-in stretch.
- The animated OT-2 deck as a build-time throwaway sketch.
- CONT-02 (other home cards clickable) → Phase 4.
- Project-doc reconciliation (CLAUDE.md / PROJECT.md stack framing) → flag to Sirio.
- The 13 taste-skill aesthetic variants remain in the source repo.
