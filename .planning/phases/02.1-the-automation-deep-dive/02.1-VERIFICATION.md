---
phase: 02.1-the-automation-deep-dive
verified: 2026-07-18T14:00:00Z
status: passed
score: 32/35 must-haves verified (+1 override, 2 uncertain)
overrides_applied: 1
overrides:
  - must_have: "No uncleared vendor marketing render (Opentrons-OT2.png, RunOne...png) ships on the page (R-02)"
    reason: "Project owner (Sirio) consciously waived R-02 for a temporary, swappable before-visual placeholder (site/assets/media/ot2-platform-stock.png — a renamed copy of the uncleared Opentrons manufacturer render), shipped only as a stop-gap 'what I started with' beat. It is flagged in site/assets/media/README.md (§ 'Temporary, uncleared placeholder'), flagged with an HTML comment directly above the <figure> in site/index.html:805, and a replacement todo is logged at .planning/todos/pending/2026-07-18-replace-ot2-before-visual-placeholder.md (resolves_phase: 4, blocks: none). Neither Opentrons-OT2.png nor 'RunOne Gel Electrophoresis System.png' (the literal filenames the must-have names) ship — the letter of the must-have holds; the spirit (no uncleared vendor imagery at all) is a named, accepted, tracked deviation."
    accepted_by: "Sirio (project owner) — checkpoint decision recorded in 02.1-02-PLAN.md Task 1 / 02.1-02-SUMMARY.md key-decisions"
    accepted_at: "2026-07-18"
gaps: []
human_verification:
  - test: "Open the live site, click the OT-2 'Automated Gel Electrophoresis' card, and watch the warp tint + arrival into the cyan engineering-room deep-dive. Judge whether the cyan --star-rigel accent and the data-pos=\"0.02\" warp-tint destination feel right, or should be retuned."
    expected: "The cyan technical accent reads as intentional (not arbitrary), the warp tint lands convincingly, and the grid-forward/starfield-recedes arrival 'earns' the shift (D-27)."
    why_human: "Plan 02.1-02 Task 3 ('checkpoint:human-verify', gate: blocking) requires Sirio's own reaction to a real render before the accent/tint is considered final. The only feedback on record ('too plain... make it less plain, more dynamic') triggered the full redesign captured in REDESIGN-DIRECTIVE.md; there is no evidence Sirio has seen or approved the POST-redesign render — only that Claude self-verified it with Playwright (a mechanical/structural check, not an aesthetic judgment call). This is exactly the class of check (visual appearance, taste) that cannot be verified by grep or DOM inspection."
  - test: "Scroll the open OT-2 overlay slowly from the masthead through PROBLEM / APPROACH / BUILD / RUN / OUTCOME and judge the GSAP scroll-telling choreography (gel-motif draw-on + pipette loop, deck-plan draw-on, CAD parts stagger-assemble, RUN band lift, OUTCOME numbers rise)."
    expected: "The motion reads as 'automation' / 'a machine assembling itself', feels motivated rather than gratuitous (MOTION_INTENSITY 7, D-26), and no beat feels janky, mistimed, or excessive on repeat scroll-back."
    why_human: "Plan 02.1-03 Task 2 ('checkpoint:human-verify', gate: blocking) explicitly required Sirio to view a throwaway choreography sketch and approve a direction before it was committed to the real page. Per 02.1-03-SUMMARY.md ('Deviations from Plan'), this checkpoint was resolved by Claude's own judgement — Sirio was away and said 'no questions, use the skills' — and the choreography went straight to the live page, verified only by Claude's own Playwright pass. The choreography itself has never been reacted to by Sirio, which is the explicit purpose of this gate."
---

# Phase 02.1: The Automation Deep-Dive Verification Report

**Phase Goal:** Prove the case-study template with one real page — clicking the OT-2 "Automated Gel Electrophoresis" card on the home page triggers a deliberate warp/hyperlight transition and lands on a full DALSA OT-2 deep-dive (the story, the explanations, the moving parts, the real media); returning home is equally intentional.
**Verified:** 2026-07-18T14:00:00Z
**Status:** passed — the two aesthetic sign-offs were closed by Sirio's iterative review + final "finalize this phase" approval on 2026-07-20 (see 02.1-UAT.md).
**Re-verification:** No — initial verification

## Context note

This phase was executed then redesigned mid-flight: at the Wave-2 render checkpoint Sirio judged the first result "too plain" and steered a full visual + motion overhaul (`REDESIGN-DIRECTIVE.md`). The redesign is present and complete in the current codebase; this report verifies the CURRENT state, not the superseded first pass. All grep/`node --check` gates and the codebase inspection below were run directly against `site/index.html`, `site/main.js`, `site/styles.css`, `site/assets/vendor/ScrollTrigger.min.js`, and `site/assets/media/*` — not against SUMMARY.md claims.

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth (Roadmap SC) | Status | Evidence |
|---|---|---|---|
| 1 | Clicking the OT-2 card opens the deep-dive through a deliberate warp transition; returning home is equally intentional (NAV-03) | ✓ VERIFIED | `site/index.html:241-243` — `<article data-open="ot2" role="button" tabindex="0" aria-label="...">` (ONLY this card; the :200 dispenser plate has no `data-open`). `site/main.js:527-546` — `boot()` wires click + Enter/Space → `openProj(id)` once. `openProj`/`closeProj`/`applyTrans`/`drawWarp` all present and syntactically valid (`node --check site/main.js` → exit 0). The mid-point masked DOM swap (`applyTrans:396-404`) toggles `visibility`/`opacity`/`body.style.overflow` symmetrically for open and close. Return link `[data-close] href="#work"` present at `index.html:684` and `:917`, wired to `closeProj()` at `main.js:547-549`. |
| 2 | The deep-dive presents the full OT-2 story (what it does, how built, why) well beyond the home card | ✓ VERIFIED | Five real sections present in `index.html:746-918`: `01 THE PROBLEM` (:746), `02 THE APPROACH` (:762, design-thinking spotlight), `03 THE BUILD` (:792, 5-part CAD breakdown + protocol description), `04 THE RUN` (:870, timelapse), `05 THE OUTCOME` (:893, 144/~10min/12-12 stats + future perspectives). Content traces to `profile.md`/DALSA report per plan (Double Diamond, 19-system survey, cassette→RunOne pivot, 5 Fusion360 parts). `"785"` string absent from `index.html` (confirmed via grep — zero matches). |
| 3 | The page carries Sirio's real OT-2 media and external links resolve | ✓ VERIFIED (with 1 accepted override) | All media files exist on disk: `ot2-timelapse.mp4` (2.05MB), `ot2-timelapse-poster.jpg`, 4× `ot2-part-*.png` (CAD renders), `ot2-slots-allocation.png`, `ot2-ui-runner.png` — all referenced in `index.html` with descriptive first-party `alt` text. DALSA link `href="https://dalsa.dtu.dk/" target="_blank" rel="noopener"` present at `index.html:778`. **Override applied:** `ot2-platform-stock.png` (a renamed, uncleared vendor render) ships as a flagged, swappable before-visual placeholder — see `overrides` in frontmatter. |
| 4 | "Moving parts" — advanced GSAP scroll-telling conveying automation, suppressed under prefers-reduced-motion | ✓ VERIFIED (functionally; aesthetic approval pending — see human_verification) | `site/assets/vendor/ScrollTrigger.min.js` exists (44.5KB) and is script-tagged locally at `index.html:931` (after `gsap.min.js:930`, before `main.js:933` — exactly one gsap-core tag). `initOt2Motion()` (`main.js:311-366`) returns early on `REDUCED`, registers the plugin, opens `gsap.context(fn, scroller)` scoped to `document.querySelector('[data-proj="ot2"]')` — never the window. Four real `scrollTrigger` blocks (masthead, deck-plan draw-on, CAD assembly, RUN band, OUTCOME numbers), all set `scroller` explicitly, all use `transform`/`opacity`/`strokeDashoffset` (positive offset) only — no layout-thrashing properties. `killOt2Motion()` (`main.js:368-370`) calls `ot2Ctx.revert()`. Hooked at `applyTrans` `t>=1` for `into.id==='ot2'` (`main.js:426`) and torn down in `closeProj` (`main.js:269`). No `matchMedia` reduced-motion source was added (the one pre-existing `matchMedia('(hover: hover)')` at `main.js:173` is an unrelated hover-capability check from Phase 1, confirmed via `git log -S`). Default (no-JS/reduced) CSS state for the animated SVG strokes (`.gel-motif__lane`, `.ot2-deck__line`) has no dashoffset hiding — they render fully by default; only `initOt2Motion`'s `seedDraw()` hides them for the draw-on animation, and only when `REDUCED` is false. `@media (prefers-reduced-motion: reduce)` block present at `styles.css:2853`. `node --check site/main.js` passes. |
| 5 | The page is a hand-crafted pilot whose shape Phase 4 reuses | ✓ VERIFIED (structural evidence; final judgment is subjective) | The warp lifecycle (`openProj`/`closeProj`/`applyTrans`), the engine-pause guard (`loop()`), the `.section` chrome reuse pattern, and the scoped `gsap.context` lifecycle (`initOt2Motion`/`killOt2Motion`) are all generic mechanisms operating on `data-proj`/`data-open` attributes and a `scroller` lookup — not hardcoded to "ot2" except where content requires it. This is a reasonable, reusable shape for Phase 4. |

**Score:** 4/5 roadmap truths fully code-verified; SC4 and SC5 carry a residual human-judgment component (see Human Verification below) that the code cannot resolve.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `site/index.html` | `[data-proj="ot2"]` overlay, `[data-warpov]` warp overlay, `data-open="ot2"` card, DALSA link | ✓ VERIFIED | All present and wired (see truths 1-3 above). |
| `site/main.js` | Ported `openProj`/`closeProj`/`applyTrans`/`drawWarp`/`startBurst` + `curProj`/`trans` state + loop guard + open/close wiring | ✓ VERIFIED | All functions present at module scope; `curProj && !trans` guard at `loop():504`; wiring at `boot():541-546`. |
| `site/styles.css` | Overlay layout + engineering-room accent styling | ✓ VERIFIED | `[data-proj="ot2"] { --accent: var(--star-rigel); ... }` at `:2329-2342`; `--star-rigel: 96, 214, 255` defined at `:36`; **not** added to `PAL` in `star-engine.js` (confirmed — `PAL` array has no `96, 214, 255` triplet). |
| `site/assets/media/ot2-slots-allocation.png` | Deck-allocation map (first-party) | ✓ VERIFIED | File exists (83.7KB), referenced at `index.html:783` with first-party alt text, `.device-frame` treated. |
| `site/assets/media/ot2-ui-runner.png` | Parameterised-run UI screenshot (first-party) | ✓ VERIFIED | File exists (86.0KB), referenced at `index.html:861`, `.device-frame--portrait` treated (`object-fit:contain`, never cropped). |
| `site/assets/vendor/ScrollTrigger.min.js` | Vendored, pinned GSAP ScrollTrigger 3.15.0 | ✓ VERIFIED | File exists (44.5KB) at the expected path, script-tagged locally, no CDN reference. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `index.html` OT-2 card (`data-open="ot2"`) | `openProj('ot2')` → `[data-proj="ot2"]` | click/keydown handler at `boot()` | ✓ WIRED | `main.js:541-546`. |
| `main.js loop()` | `drawDeep`/`applyPointer` gated | `if (!(curProj && !trans))` | ✓ WIRED | `main.js:504`. Confirmed the deep-field/hero engine pauses while the overlay is open. |
| All warp branches | Single reduced-motion source | `REDUCED` import | ✓ WIRED | `main.js:11` imports `REDUCED`; used consistently in `openProj`, `closeProj`, `initOt2Motion`; no competing `matchMedia`-based reduced-motion source introduced. |
| Every ScrollTrigger | `[data-proj="ot2"]` element | `scroller` property | ✓ WIRED | All 4 `scrollTrigger` blocks in `initOt2Motion` (`main.js:345,349,355,361`) set `scroller` to the resolved overlay element, never `window`. |
| `applyTrans` completion (`t>=1`, `into.id==='ot2'`) | `initOt2Motion()` | hook after transform/filter cleared, then `ScrollTrigger.refresh()` | ✓ WIRED | `main.js:415-427`; `initOt2Motion` ends with `ScrollTrigger.refresh()` at `:365`. |
| `closeProj()` | `killOt2Motion()` → `ctx.revert()` | teardown on close | ✓ WIRED | `main.js:269` calls `killOt2Motion()` unconditionally (both reduced and animated branches), before the `REDUCED` early-return. |
| The two opaque-white PNGs | The dark engineering-room theme | `device-frame` CSS | ✓ WIRED | `.device-frame` class defined at `styles.css:2626-2679`, applied at `index.html:782,815,860`. |
| The vendor marketing renders | NOT the page | excluded — never copied | ✓ WIRED (with override) | `Opentrons-OT2.png` / `RunOne Gel Electrophoresis System.png` are absent from `site/assets/media/` and from `index.html`. **Note:** `ot2-platform-stock.png` is a renamed copy of the same uncleared render, shipped as a flagged, accepted deviation (see overrides). |

### Data-Flow Trace (Level 4)

Not applicable in the conventional sense — this phase ships static, build-time-authored content (no client-fetched or database-backed data). The relevant "data flow" is: DOM attributes (`data-open`, `data-proj`, `data-pos`, `data-accent`) → JS state (`curProj`/`trans`) → canvas paint (`drawWarp`) and CSS custom-property resolution (`--accent`). All traced above under Key Link Verification; no hollow props or static-empty-return patterns found.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| `main.js` is syntactically valid | `node --check site/main.js` | exit 0 | ✓ PASS |
| No duplicate/second reduced-motion source | `grep -n matchMedia site/main.js` | 2 matches: line 140 (comment) + line 173 (`POINTER_OK`, pre-existing hover-capability check from Phase 1, confirmed via `git log -S POINTER_OK`) | ✓ PASS (intent satisfied — no *reduced-motion* matchMedia added) |
| `"785"` framing absent | `grep -c 785 site/index.html` | 0 matches | ✓ PASS |
| Vendor renders excluded | `grep -i "Opentrons-OT2.png\|RunOne" site/index.html` + file existence check | 0 matches; files absent from `site/assets/media/` | ✓ PASS |
| GSAP core exactly once, ScrollTrigger present, correct load order | inspection of `index.html:929-933` | `gsap.min.js` → `ScrollTrigger.min.js` → `pump-anim.js` → `main.js` (module) | ✓ PASS |
| All 3 plan commits exist in history | `git log --oneline` | `9011d87`, `a95f56b`, `630eaf3`, `1ca4fd0`, `21ddff0`, `b60d5f7`, `21ae5ef` all present | ✓ PASS |

### Probe Execution

Not applicable — this phase has no `scripts/*/tests/probe-*.sh` convention and none were declared in the PLAN/SUMMARY files. Step 7c: SKIPPED (no runnable entry points / no declared probes).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| NAV-03 | 02.1-01, 02.1-02, 02.1-03 (all three plans declare it) | Tasteful transitions when moving between home and deeper pages | ✓ SATISFIED (code-level) | Cross-referenced against `.planning/REQUIREMENTS.md:25` (`[x] NAV-03`) and the traceability table (`NAV-03 \| Phase 2.1 — The Automation Deep-Dive \| Complete`). The warp/hyperlight transition mechanism (truth 1 above) is real, symmetric, reduced-motion-aware, and functionally verified. The residual open item is aesthetic sign-off on the "tasteful" judgment call (see human_verification), not the mechanism's existence. |

No orphaned requirements found for this phase — NAV-03 is the only REQ-ID mapped to Phase 2.1 in REQUIREMENTS.md, and it appears in all three plans' `requirements:` frontmatter.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `site/index.html` | 805 | `<!-- BEFORE-VISUAL — TEMPORARY, UNCLEARED PLACEHOLDER (owner override). ... -->` | ℹ️ Info | Not a debt marker (no TBD/FIXME/XXX) — a deliberately flagged, tracked, owner-accepted deviation with a logged todo. Covered by the override entry above. |
| `site/assets/media/README.md` | 99-116 | "Temporary, uncleared placeholder" section | ℹ️ Info | Same deviation, documented in the media contract file. Consistent cross-referencing (HTML comment ↔ README ↔ todo file) confirms this is honestly tracked, not silently smuggled in. |

No TBD/FIXME/XXX/TODO/HACK markers found in any of the three modified files (`grep -in "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` returned only the pre-existing, documented `placeholder` occurrences tied to the `.media-slot` empty-state system and the one flagged R-02 deviation — none are unresolved debt). No stub returns, empty handlers, or console.log-only implementations found in `main.js`, `index.html`, or `styles.css`.

### Human Verification Required

### 1. Engineering-room accent + warp tint — final aesthetic sign-off

**Test:** Open the live site, click the OT-2 card, and watch the warp tint + arrival into the cyan engineering-room deep-dive.
**Expected:** The cyan `--star-rigel` accent and the warp-tint destination feel intentional and "earn" the shift (D-27), per Plan 02.1-02 Task 3's own acceptance criteria.
**Why human:** This is a formally blocking `checkpoint:human-verify` task (02.1-02-PLAN.md Task 3). The only recorded Sirio feedback on record is the "too plain... make it less plain, more dynamic" verdict that triggered the redesign (`REDESIGN-DIRECTIVE.md`) — there is no evidence he has seen or approved the POST-redesign render. Only Claude's own Playwright pass verified it (structurally, not aesthetically).

### 2. GSAP scroll-telling choreography — approval

**Test:** Scroll the open OT-2 overlay slowly through all five sections and judge the parts-assemble / draw-on / reveal choreography.
**Expected:** Motion reads as "automation" and feels motivated, not gratuitous (MOTION_INTENSITY 7, D-26).
**Why human:** Plan 02.1-03 Task 2 is a formally blocking `checkpoint:human-verify` task requiring Sirio to view a throwaway sketch and approve a direction BEFORE it ships to the real page. Per 02.1-03-SUMMARY.md's own "Deviations from Plan" section, this checkpoint was resolved by Claude's judgement alone (Sirio was away and explicitly said "no questions, use the skills") and went straight to the live page. This is an honestly-recorded deviation, not a concealed one, but the gate's actual purpose — Sirio reacting to the choreography before it ships — has not yet happened.

Both items can reasonably be resolved in a single walkthrough of the live/local site.

### Gaps Summary

No BLOCKER-level gaps. Every artifact, key link, and roadmap success criterion required by this phase's three plans exists in the current codebase, is substantive (not a stub), and is wired correctly — confirmed by direct inspection of `site/index.html`, `site/main.js`, `site/styles.css`, `site/assets/vendor/ScrollTrigger.min.js`, and `site/assets/media/*`, not by trusting SUMMARY.md claims. `node --check site/main.js` passes; all documented commits exist in git history.

One deviation (R-02, the vendor-render before-visual placeholder) is a named, tracked, owner-accepted exception with a logged replacement todo — handled via override, not a gap.

Two items route this report to `human_needed` rather than `passed`: both of this phase's own `checkpoint:human-verify` gates (accent/tint approval, choreography approval) were resolved by Claude's own judgment rather than Sirio's actual reaction to the finished, redesigned render — an honestly-documented deviation in both SUMMARYs, consistent with Sirio's own "don't ask, proceed" instruction at the time, but the gates' underlying purpose (a human aesthetic verdict) has not yet been closed. Per this project's own CLAUDE.md convention ("Design ownership: Claude leads design, Sirio approves/steers each step"), a final walkthrough by Sirio is the appropriate way to close this out — not a code fix.

---

_Verified: 2026-07-18T14:00:00Z_
_Verifier: Claude (gsd-verifier)_
