---
phase: 02.1-the-automation-deep-dive
plan: 02
subsystem: ui
tags: [css, html, canvas, engineering-room, redesign, device-frame, media]

requires:
  - phase: 02.1-01
    provides: the [data-proj="ot2"] warp overlay + the five-section OT-2 case study to deepen
provides:
  - The two first-party diagrams (deck-allocation map, parameterised-run UI) device-framed on the dark theme
  - A full visual redesign of the OT-2 deep-dive onto the landing page's own .section chrome (ghost numerals, accent blob glow, mono label+rule, display h2) in the cyan engineering-room register
  - Engineering-room depth (accent radial glows + vignette-masked grid) replacing the flat uniform grid
  - An asymmetric masthead with a signature hand-built gel-loading SVG hero visual
  - Diversified section layouts (spotlight, full-width assembly figure, cinematic RUN band, bold display-number OUTCOME)
affects: [phase-04-showcase, media-fill]

tech-stack:
  added: []
  patterns:
    - "Overlay sections REUSE the landing .section chrome (.section__numeral/.section__label/.section__rule) scoped under [data-proj='ot2'] with a section-scoped cyan --accent — not bespoke chrome"
    - "device-frame treatment seats opaque-white first-party imagery on the dark theme"
    - "MEDIA-01 swappable placeholder pattern for a temporary uncleared asset (README + HTML flag)"

key-files:
  created:
    - site/assets/media/ot2-slots-allocation.png
    - site/assets/media/ot2-ui-runner.png
    - site/assets/media/ot2-platform-stock.png
    - .planning/phases/02.1-the-automation-deep-dive/REDESIGN-DIRECTIVE.md
  modified:
    - site/index.html
    - site/styles.css
    - site/assets/media/README.md

key-decisions:
  - "R-02 waived by the project owner: the OT-2 platform manufacturer render ships as a TEMPORARY, swappable before-visual placeholder (ot2-platform-stock.png), flagged in README + an HTML comment, pending replacement with a cleared bench photo / licensed image. Todo logged."
  - "Sirio rejected the first (plain) render at the checkpoint and steered a full redesign: less plain, more dynamic, closer to the landing UI. Resolved by rebuilding on the landing's own .section components instead of the overlay's weaker bespoke chrome."
  - "Preserve-and-extend: all case-study copy and the em-dash mono-label voice kept verbatim (redesign-preserve); this was a visual + motion overhaul, not a content rewrite."

patterns-established:
  - "Engineering-room = the landing's cosmic-dark language in the cyan (--star-rigel) register: same numerals/labels/glow system, different accent"

requirements-completed: [NAV-03]

duration: ~50min (incl. checkpoint steer + render-verify)
completed: 2026-07-18
---

# Phase 02.1 Plan 02: OT-2 deep-dive media + engineering-room redesign

**The OT-2 deep-dive rebuilt on the landing page's own section chrome in the cyan engineering-room register — ghost numerals, depth glows, a signature gel-loading SVG masthead, device-framed diagrams, and diversified layouts — resolving Sirio's "too plain" checkpoint steer.**

## Accomplishments
- Copied + device-framed the two first-party diagrams (deck-allocation map in APPROACH, run-UI in BUILD); shipped the temporary owner-approved before-visual placeholder with full swap flagging.
- Rebuilt the overlay to REUSE the landing's `.section__numeral` / `.section__label` / `.section__rule` / `.section h2` system (the root cause of the "plain/off-theme" verdict was bespoke weaker chrome).
- Added real depth (cyan radial glows + vignette-masked grid) and an asymmetric masthead with a hand-built gel-loading SVG hero visual.
- Diversified section layouts: APPROACH spotlight, a full-width assembly figure, a cinematic near-full-bleed RUN band, and a bold display-number OUTCOME row.

## Task Commits
1. **Diagrams + APPROACH spotlight (initial)** - `630eaf3` (feat)
2. **Rebuild overlay on landing chrome + engineering-room depth** - `1ca4fd0` (feat)
3. **Masthead title fit + mobile collapse (render-verify fix)** - `21ae5ef` (fix)

## Files Created/Modified
- `site/index.html` - overlay markup rebuilt on `.section` chrome; masthead split + gel-loading SVG; diversified section wrappers.
- `site/styles.css` - engineering-room depth, `.section`-based overlay chrome, device-frame, assembly/RUN/OUTCOME layouts, masthead fit + mobile collapse.
- `site/assets/media/{ot2-slots-allocation,ot2-ui-runner,ot2-platform-stock}.png` + `README.md` - diagrams + flagged placeholder.

## Decisions Made
- See key-decisions (R-02 owner waiver; redesign steer; preserve-and-extend).

## Deviations from Plan
The plan's Task 2 verify gate asserts the vendor renders are absent (R-02). The owner **consciously waived R-02** for the temporary before-visual placeholder — a named, accepted deviation, honestly recorded (not passed off as compliant). A replacement todo is logged. All other gates hold (both first-party diagrams present + first-party alt; device-frame CSS present; `785` absent; PAL untouched). The Task 3 checkpoint (accent/tint) escalated into a full redesign per Sirio's steer.

## Issues Encountered
- Render-verify (Playwright, desktop 1440 + mobile 390) caught a masthead title overflow and a missing mobile collapse; both fixed in `21ae5ef` and re-verified.

## Next Phase Readiness
- The case-study shape (warp overlay + engine-pause + landing-chrome sections + engineering-room depth) is the reusable pilot for Phase 4 Showcase.
- Open follow-up: replace `ot2-platform-stock.png` with a cleared image (todo logged).

---
*Phase: 02.1-the-automation-deep-dive*
*Completed: 2026-07-18*
